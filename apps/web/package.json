{
  "name": "@zeros/web",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0"
  },
  "type": "module",
  "description": "Cloudflare Pages hub for authentication handoff, app launch, and host-routed marketing delivery.",
  "scripts": {
    "build": "node scripts/verify-deployment-env.mjs && node scripts/assemble-marketing.mjs",
    "build:standalone": "node scripts/verify-deployment-env.mjs && node scripts/assemble-marketing.mjs --standalone-install",
    "assemble": "node scripts/assemble-marketing.mjs --skip-build",
    "dev": "npm run build && wrangler pages dev dist --port 8788 --ip 127.0.0.1 --compatibility-date=2026-07-10 --kv SESSIONS --binding MARKETING_HOSTS=127.0.0.1,localhost",
    "dev:app": "npm run build && wrangler pages dev dist --port 8788 --ip 127.0.0.1 --compatibility-date=2026-07-10 --kv SESSIONS",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test lib/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260823.1",
    "typescript": "^5.6.3",
    "wrangler": "4.125.0"
  }
}
