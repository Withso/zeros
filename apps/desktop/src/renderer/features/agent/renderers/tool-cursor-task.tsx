// Persisted Cursor `task` kinds keep their wire identity and share the same
// nested Agent conversation presentation as Claude and Codex.
export { SubagentCard as CursorTaskCard } from "./tool-subagent";
