// Stable entrypoint retained for callers that imported the original shared
// controls. The canonical implementations live with the other UI primitives.
export { Input } from "./primitives/input";
export { Textarea } from "./primitives/textarea";
