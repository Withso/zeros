diff --git a/dist/cli.js b/dist/cli.js
old mode 100644
new mode 100755
diff --git a/dist/sandbox/linux-sandbox-utils.d.ts b/dist/sandbox/linux-sandbox-utils.d.ts
index 88cc67f6cdb6ed2cb4d019bcbbf077e4d081706b..d6e34e4c9bfa82e157e53de14c75ee9bb94ffd89 100644
--- a/dist/sandbox/linux-sandbox-utils.d.ts
+++ b/dist/sandbox/linux-sandbox-utils.d.ts
@@ -45,6 +45,14 @@ export interface LinuxSandboxParams {
      */
     maskedFileStoreDir?: string;
     enableWeakerNestedSandbox?: boolean;
+    /** Trusted root-supervisor mode for entering an exact non-root workload identity. */
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    };
+    /** Preserve ordinary host process, device, IPC, and network behavior. */
+    hostParity?: boolean;
     allowAllUnixSockets?: boolean;
     binShell?: string;
     ripgrepConfig?: {
@@ -55,6 +63,8 @@ export interface LinuxSandboxParams {
     mandatoryDenySearchDepth?: number;
     /** Allow writes to .git/config files (default: false) */
     allowGitConfig?: boolean;
+    /** Skip SRT's built-in write deny list while retaining explicit policy. */
+    disableMandatoryWriteProtection?: boolean;
     /**
      * Directories to emit as `safe.directory` via `GIT_CONFIG_*` env
      * vars (see {@link buildPosixGitSafeDirEnv}). Under `--unshare-user`
diff --git a/dist/sandbox/linux-sandbox-utils.js b/dist/sandbox/linux-sandbox-utils.js
index 2a2d53cab6d985a02487a10b224659cb31114c4a..7c864c159e0982786d7746a3b840d7d745a7fd9f 100644
--- a/dist/sandbox/linux-sandbox-utils.js
+++ b/dist/sandbox/linux-sandbox-utils.js
@@ -30,7 +30,7 @@ function findSymlinkInPath(targetPath, allowedWritePaths) {
             const stats = fs.lstatSync(nextPath);
             if (stats.isSymbolicLink()) {
                 // Check if this symlink is within an allowed write path
-                const isWithinAllowedPath = allowedWritePaths.some(allowedPath => nextPath.startsWith(allowedPath + '/') || nextPath === allowedPath);
+                const isWithinAllowedPath = allowedWritePaths.some(allowedPath => allowedPath === '/' || nextPath.startsWith(allowedPath + '/') || nextPath === allowedPath);
                 if (isWithinAllowedPath) {
                     return nextPath;
                 }
@@ -169,7 +169,9 @@ function findFirstNonExistentComponent(targetPath) {
  * Uses a SINGLE ripgrep call with multiple glob patterns for efficiency.
  * With --max-depth limiting, this is fast enough to run on each command without memoization.
  */
-async function linuxGetMandatoryDenyPaths(ripgrepConfig = { command: 'rg' }, maxDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, abortSignal) {
+async function linuxGetMandatoryDenyPaths(ripgrepConfig = { command: 'rg' }, maxDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, abortSignal) {
+    if (disableMandatoryWriteProtection)
+        return [];
     const cwd = process.cwd();
     // Use provided signal or create a fallback controller
     const fallbackController = new AbortController();
@@ -616,6 +618,25 @@ function resolveApplySeccompPrefix(applyPath, argv0) {
     const binary = getApplySeccompBinaryPath(applyPath);
     return binary ? `${quote([binary])} ` : undefined;
 }
+function privilegedWorkerCommand(userCommand, shellPath, worker) {
+    if (!worker)
+        return userCommand;
+    const args = [
+        worker.setprivPath,
+        '--securebits=+noroot,+noroot_locked,+no_setuid_fixup,+no_setuid_fixup_locked',
+        '--bounding-set=-all',
+        '--inh-caps=-all',
+        '--ambient-caps=-all',
+        '--no-new-privs',
+        `--reuid=${worker.uid}`,
+        `--regid=${worker.gid}`,
+        '--clear-groups',
+        shellPath,
+        '-c',
+        userCommand,
+    ];
+    return quote(args);
+}
 /**
  * Build the command that runs inside the sandbox.
  * Sets up HTTP proxy on port 3128 and SOCKS proxy on port 1080
@@ -674,7 +695,24 @@ function resolveSymlinkDenyDest(normalizedPath) {
  */
 function pushReadDenyDirMounts(args, normalizedPath, allowedWritePaths, readAllowPaths) {
     const denySep = normalizedPath === '/' ? '/' : normalizedPath + '/';
-    args.push('--tmpfs', normalizedPath);
+    args.push('--tmpfs', normalizedPath, '--chmod', '0711', normalizedPath);
+    // A distinct worker UID cannot traverse root-private host ancestors. These
+    // are new directories in the empty deny mask, never chmods of host state.
+    // Create ALL ancestors before restoring ANY bind: after a restoration a
+    // chmod or --dir could reach an intentionally exposed host directory.
+    const syntheticParents = new Set();
+    for (const allowedPath of [...allowedWritePaths, ...readAllowPaths]) {
+        if (!allowedPath.startsWith(denySep) || allowedPath === normalizedPath)
+            continue;
+        let parent = path.dirname(allowedPath);
+        while (parent !== normalizedPath && parent.startsWith(denySep)) {
+            syntheticParents.add(parent);
+            parent = path.dirname(parent);
+        }
+    }
+    for (const parent of [...syntheticParents].sort((a, b) => a.split('/').length - b.split('/').length)) {
+        args.push('--perms', '0711', '--dir', parent);
+    }
     // tmpfs wiped any earlier write binds under this path — restore them.
     for (const writePath of allowedWritePaths) {
         if (writePath.startsWith(denySep) || writePath === normalizedPath) {
@@ -708,7 +746,7 @@ function pushReadDenyDirMounts(args, normalizedPath, allowedWritePaths, readAllo
 /**
  * Generate filesystem bind mount arguments for bwrap
  */
-async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, abortSignal) {
+async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, abortSignal) {
     const args = [];
     // fs already imported
     // Collect normalized allowed write paths. Populated in the writeConfig
@@ -717,6 +755,7 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
     // denyWrite binds are buffered and emitted after denyRead processing so that
     // a denyRead tmpfs over an ancestor directory doesn't wipe them out.
     const denyWriteArgs = [];
+    const missingWriteMounts = new Map();
     // Directories that a deny entry re-binds read-only inside the sandbox
     // (--ro-bind <dir> <dir>), keyed by resolved dest, with every raw
     // (pre-resolution) spelling each was reached through. A non-existent deny
@@ -896,12 +935,12 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
         // directory missing from the record only costs an abort). No spelling
         // handling is needed here: allowedWritePaths entries are recorded with
         // trailing slashes stripped, and candidates are resolved deny dests.
-        const isWithinAnyAllowedWritePath = (candidatePath) => allowedWritePaths.some(allowedPath => candidatePath.startsWith(allowedPath + '/') ||
+        const isWithinAnyAllowedWritePath = (candidatePath) => allowedWritePaths.some(allowedPath => allowedPath === '/' || candidatePath.startsWith(allowedPath + '/') ||
             candidatePath === allowedPath);
         // Deny writes within allowed paths (user-specified + mandatory denies)
         const denyPaths = [
             ...(writeConfig.denyWithinAllow || []),
-            ...(await linuxGetMandatoryDenyPaths(ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, abortSignal)),
+            ...(await linuxGetMandatoryDenyPaths(ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, disableMandatoryWriteProtection, abortSignal)),
         ];
         // Duplicate deny entries must be collapsed: a duplicate
         // --ro-bind /dev/null <dest> hits a char device on the second pass and
@@ -1144,6 +1183,21 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
                 const ancestorIsWithinReadOnlyDeny = coveredBySafeReadOnlyDenyDir(normalizedPath);
                 if (ancestorIsWithinAllowedPath && !ancestorIsWithinReadOnlyDeny) {
                     const firstNonExistent = findFirstNonExistentComponent(normalizedPath);
+                    // Absent private roots can appear both directly and through
+                    // multiple children. Emit one mount per missing component;
+                    // a directory requirement dominates a file placeholder in
+                    // either input order. Stacking a directory over /dev/null
+                    // makes bwrap fail with ENOTDIR before the actor can start.
+                    const previous = missingWriteMounts.get(firstNonExistent);
+                    if (previous !== undefined) {
+                        if (firstNonExistent !== normalizedPath && denyWriteArgs[previous + 1] === '/dev/null') {
+                            const emptyDir = fs.mkdtempSync(path.join(tmpdir(), 'claude-empty-'));
+                            denyWriteArgs[previous + 1] = emptyDir;
+                            bwrapMountPoints.add(emptyDir);
+                        }
+                        continue;
+                    }
+                    missingWriteMounts.set(firstNonExistent, denyWriteArgs.length);
                     // Fix 2: If firstNonExistent is an intermediate component (not the
                     // leaf deny path itself), mount a read-only empty directory instead
                     // of /dev/null. This prevents the component from appearing as a file
@@ -1151,6 +1205,7 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
                     if (firstNonExistent !== normalizedPath) {
                         const emptyDir = fs.mkdtempSync(path.join(tmpdir(), 'claude-empty-'));
                         denyWriteArgs.push('--ro-bind', emptyDir, firstNonExistent);
+                        bwrapMountPoints.add(emptyDir);
                         denyWriteRawDests.set(firstNonExistent, rawPath);
                         bwrapMountPoints.add(firstNonExistent);
                         registerExitCleanupHandler();
@@ -1333,6 +1388,31 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
         if (rawDest !== dest)
             emittedDenyWriteDests.push(rawDest);
     }
+    // Re-bind explicit writable islands after a broader denyWrite ro-bind.
+    // Only paths already present in allowOnly qualify, so this changes
+    // precedence rather than adding authority. Re-apply narrower read/write
+    // denies that fall inside an exception immediately afterward.
+    const lateWritePaths = (writeConfig?.allowWithinDeny || [])
+        .map(candidate => normalizePathForSandbox(candidate).replace(/\/+$/, '') || '/')
+        .filter(candidate => allowedWritePaths.includes(candidate) && fs.existsSync(candidate));
+    for (const lateWritePath of lateWritePaths) {
+        args.push('--bind', lateWritePath, lateWritePath);
+        for (const deniedDest of emittedDenyWriteDests) {
+            if (deniedDest === lateWritePath || deniedDest.startsWith(lateWritePath + '/')) {
+                args.push('--ro-bind', deniedDest, deniedDest);
+            }
+        }
+        for (const [maskedFile, source] of maskedFiles) {
+            if (maskedFile === lateWritePath || maskedFile.startsWith(lateWritePath + '/')) {
+                if (resolveSymlinkDenyDest(maskedFile) === maskedFile)
+                    args.push('--ro-bind', source, maskedFile);
+            }
+        }
+        for (const tmpfsDir of tmpfsDirs) {
+            if (tmpfsDir === lateWritePath || tmpfsDir.startsWith(lateWritePath + '/'))
+                pushReadDenyDirMounts(args, tmpfsDir, allowedWritePaths, readAllowPaths);
+        }
+    }
     // The inverse stacking problem: a denyWrite ro-bind whose dest strictly
     // contains a read-denied dir re-exposes that dir's real contents (the bind
     // landed after the tmpfs). Re-apply the tmpfs on top, with the same write
@@ -1422,7 +1502,7 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
  * Dependencies are checked by checkLinuxDependencies() before enabling the sandbox.
  */
 export async function wrapCommandWithSandboxLinux(params) {
-    const { command, commandId, needsNetworkRestriction, httpSocketPath, socksSocketPath, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, javaAgentJarPath, readConfig, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, maskedFileStoreDir, enableWeakerNestedSandbox, allowAllUnixSockets, binShell, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, gitSafeDirectories, seccompConfig, bwrapPath, socatPath, observeSocketPath, abortSignal, } = params;
+    const { command, commandId, needsNetworkRestriction, httpSocketPath, socksSocketPath, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, javaAgentJarPath, readConfig, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, maskedFileStoreDir, enableWeakerNestedSandbox, linuxPrivilegedWorker, allowAllUnixSockets, hostParity = false, binShell, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, gitSafeDirectories, seccompConfig, bwrapPath, socatPath, observeSocketPath, abortSignal, } = params;
     // Determine if we have restrictions to apply
     // Read: denyOnly pattern - empty array means no restrictions
     // Write: allowOnly pattern - undefined means no restrictions, any config means restrictions
@@ -1437,9 +1517,32 @@ export async function wrapCommandWithSandboxLinux(params) {
         !hasReadRestrictions &&
         !hasWriteRestrictions &&
         !hasEnvRestrictions &&
-        !hasGitConfig) {
+        !hasGitConfig &&
+        !linuxPrivilegedWorker) {
         return command;
     }
+    if (linuxPrivilegedWorker) {
+        if (needsNetworkRestriction || httpSocketPath || socksSocketPath) {
+            throw new Error('Privileged cloud-worker proxy composition is not qualified');
+        }
+        if (!hostParity || enableWeakerNestedSandbox) {
+            throw new Error('Privileged cloud-worker mode requires host parity without weaker nesting');
+        }
+        if (typeof process.geteuid !== 'function' || process.geteuid() !== 0) {
+            throw new Error('Privileged cloud-worker mode requires a root supervisor');
+        }
+        if (!Number.isInteger(linuxPrivilegedWorker.uid) || linuxPrivilegedWorker.uid <= 0 ||
+            !Number.isInteger(linuxPrivilegedWorker.gid) || linuxPrivilegedWorker.gid <= 0 ||
+            !path.isAbsolute(linuxPrivilegedWorker.setprivPath)) {
+            throw new Error('Invalid privileged cloud-worker identity');
+        }
+        const setprivStat = fs.lstatSync(linuxPrivilegedWorker.setprivPath);
+        if (!setprivStat.isFile() || setprivStat.isSymbolicLink() ||
+            fs.realpathSync(linuxPrivilegedWorker.setprivPath) !== linuxPrivilegedWorker.setprivPath ||
+            (setprivStat.mode & 0o022) !== 0) {
+            throw new Error('Privileged cloud-worker setpriv must be a canonical non-writable file');
+        }
+    }
     // Mark this sandbox invocation as active. cleanupBwrapMountPoints() will
     // defer file deletion until this (and every other concurrent) invocation
     // has been cleaned up. The matching decrement happens in
@@ -1575,10 +1675,12 @@ export async function wrapCommandWithSandboxLinux(params) {
             // If no sockets provided, network is completely blocked (--unshare-net without proxy)
         }
         // ========== FILESYSTEM RESTRICTIONS ==========
-        const fsArgs = await generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, abortSignal);
+        const fsArgs = await generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, disableMandatoryWriteProtection, abortSignal);
         bwrapArgs.push(...fsArgs);
-        // Always bind /dev
-        bwrapArgs.push('--dev', '/dev');
+        if (hostParity)
+            bwrapArgs.push('--dev-bind', '/dev', '/dev');
+        else
+            bwrapArgs.push('--dev', '/dev');
         // ========== PID NAMESPACE ISOLATION ==========
         // IMPORTANT: These must come AFTER filesystem binds for nested bwrap to work
         // By default, always unshare PID namespace and mount fresh /proc.
@@ -1586,27 +1688,33 @@ export async function wrapCommandWithSandboxLinux(params) {
         // If we don't have --proc, it is possible to read host /proc and leak information about code running
         // outside the sandbox. But, --proc is not available when running in unprivileged docker containers
         // so we support running without it if explicitly requested.
-        bwrapArgs.push('--unshare-pid');
-        // --unshare-user in both modes: bwrap only auto-creates a userns when
-        // EUID != 0. A root parent in an unprivileged container (Docker's
-        // default: EUID=0 without CAP_SYS_ADMIN) would otherwise try a direct
-        // clone and EPERM; a root parent WITH capabilities would leave the
-        // command holding them, CAP_SYS_ADMIN included, which unmounts any deny
-        // in this namespace — hence capabilityArgs in both modes as well.
-        // apply-seccomp creates its own nested userns to obtain CAP_SYS_ADMIN
-        // for its PID+mount unshare (see below); for a root caller that needs
-        // the one capability capabilityArgs keeps.
-        bwrapArgs.push('--unshare-user', ...capabilityArgs(applySeccompPrefix !== undefined));
-        if (!enableWeakerNestedSandbox) {
-            // Mount fresh /proc if PID namespace is isolated (secure mode).
-            bwrapArgs.push('--proc', '/proc');
+        if (linuxPrivilegedWorker) {
+            bwrapArgs.push('--unshare-pid', '--proc', '/proc');
+            // The trusted cloud supervisor needs only these capabilities to
+            // enter the exact worker uid/gid. setpriv then clears all sets and
+            // locks securebits before running the workload.
+            bwrapArgs.push('--cap-drop', 'ALL', '--cap-add', 'CAP_SETUID',
+                '--cap-add', 'CAP_SETGID', '--cap-add', 'CAP_SETPCAP');
+        }
+        else if (hostParity) {
+            // Preserve host process visibility without passing ambient
+            // capabilities into the workload. Retain upstream's seccomp-helper
+            // exception only when that helper is actually used.
+            bwrapArgs.push(...capabilityArgs(applySeccompPrefix !== undefined));
         }
         else {
-            // --bind /proc /proc: apply-seccomp's nested-userns path writes
-            // /proc/self/setgroups and uid_map. Without --proc above, the
-            // --ro-bind / / leaves /proc read-only and those writes EROFS; a
-            // fresh proc mount is what an unprivileged container refuses.
-            bwrapArgs.push('--bind', '/proc', '/proc');
+            bwrapArgs.push('--unshare-pid');
+            // Retain upstream's root-caller protection in BOTH normal and
+            // weaker nesting modes, including the seccomp-helper exception.
+            bwrapArgs.push('--unshare-user', ...capabilityArgs(applySeccompPrefix !== undefined));
+            if (!enableWeakerNestedSandbox) {
+                bwrapArgs.push('--proc', '/proc');
+            }
+            else {
+                // apply-seccomp's nested namespace needs writable uid_map and
+                // setgroups entries when a fresh proc mount is unavailable.
+                bwrapArgs.push('--bind', '/proc', '/proc');
+            }
         }
         // apply-seccomp obtains CAP_SYS_ADMIN for its nested PID+mount unshare
         // by creating a nested user namespace. This requires the host to permit
@@ -1630,11 +1738,12 @@ export async function wrapCommandWithSandboxLinux(params) {
             bwrapArgs.push(sandboxCommand);
         }
         else if (applySeccompPrefix) {
-            const applySeccompCmd = applySeccompPrefix + quote([shell, '-c', command]);
+            const workload = privilegedWorkerCommand(command, shell, linuxPrivilegedWorker);
+            const applySeccompCmd = applySeccompPrefix + quote([shell, '-c', workload]);
             bwrapArgs.push(applySeccompCmd);
         }
         else {
-            bwrapArgs.push(command);
+            bwrapArgs.push(privilegedWorkerCommand(command, shell, linuxPrivilegedWorker));
         }
         const wrappedCommand = quote([bwrapPath ?? 'bwrap', ...bwrapArgs]);
         const restrictions = [];
@@ -1646,6 +1755,8 @@ export async function wrapCommandWithSandboxLinux(params) {
             restrictions.push('env');
         if (applySeccompPrefix)
             restrictions.push('seccomp(unix-block)');
+        if (linuxPrivilegedWorker)
+            restrictions.push('privileged-worker-drop');
         logForDebugging(`[Sandbox Linux] Wrapped command with bwrap (${restrictions.join(', ')} restrictions)`);
         return wrappedCommand;
     }
diff --git a/dist/sandbox/macos-sandbox-utils.d.ts b/dist/sandbox/macos-sandbox-utils.d.ts
index 93b1106305aa7ffb37f04867cd343f94ff411350..ac95e3be1c8bf8f4b749a72b439414742bf9e601 100644
--- a/dist/sandbox/macos-sandbox-utils.d.ts
+++ b/dist/sandbox/macos-sandbox-utils.d.ts
@@ -42,6 +42,7 @@ export interface MacOSSandboxParams {
     ignoreViolations?: IgnoreViolationsConfig | undefined;
     allowPty?: boolean;
     allowGitConfig?: boolean;
+    disableMandatoryWriteProtection?: boolean;
     /**
      * Directories to emit as `safe.directory` via `GIT_CONFIG_*` env
      * vars — see {@link buildPosixGitSafeDirEnv}.
@@ -49,13 +50,14 @@ export interface MacOSSandboxParams {
     gitSafeDirectories?: readonly string[];
     enableWeakerNetworkIsolation?: boolean;
     allowAppleEvents?: boolean;
+    hostParity?: boolean;
     binShell?: string;
 }
 /**
  * Get mandatory deny patterns as glob patterns (no filesystem scanning).
  * macOS sandbox profile supports regex/glob matching directly via globToRegex().
  */
-export declare function macGetMandatoryDenyPatterns(allowGitConfig?: boolean): string[];
+export declare function macGetMandatoryDenyPatterns(allowGitConfig?: boolean, disableMandatoryWriteProtection?: boolean): string[];
 export interface SandboxViolationEvent {
     line: string;
     command?: string;
diff --git a/dist/sandbox/macos-sandbox-utils.js b/dist/sandbox/macos-sandbox-utils.js
index 70de07c6837687fdec53f8db60225f4df5ea8776..17303c904d34cbd92dbb27249f4c7579755d5b4a 100644
--- a/dist/sandbox/macos-sandbox-utils.js
+++ b/dist/sandbox/macos-sandbox-utils.js
@@ -10,7 +10,9 @@ import { shouldIgnoreViolation } from './sandbox-violation-store.js';
  * Get mandatory deny patterns as glob patterns (no filesystem scanning).
  * macOS sandbox profile supports regex/glob matching directly via globToRegex().
  */
-export function macGetMandatoryDenyPatterns(allowGitConfig = false) {
+export function macGetMandatoryDenyPatterns(allowGitConfig = false, disableMandatoryWriteProtection = false) {
+    if (disableMandatoryWriteProtection)
+        return [];
     const cwd = process.cwd();
     const denyPaths = [];
     // Dangerous files - static paths in CWD + glob patterns for subtree
@@ -152,8 +154,11 @@ function lateReadDenyFilters(resolved) {
     const literalAllowDirs = resolved.allows.filter(a => !a.glob).map(a => a.path);
     for (const deny of resolved.denies) {
         if (!deny.glob) {
-            if (literalAllowDirs.some(a => deny.path.startsWith(a + '/'))) {
-                filters.push(denyPathFilter(deny.path));
+            if (literalAllowDirs.some(a => isStrictlyUnder(deny, a))) {
+                const carveOuts = resolved.allows
+                    .filter(a => isStrictlyUnder(a, deny.path))
+                    .map(a => pathFilter(a.path));
+                filters.push(carveFilter(denyPathFilter(deny.path), carveOuts));
             }
             continue;
         }
@@ -542,25 +547,8 @@ function generateReadRules(resolved, logTag) {
     rules.push(...renderRule('allow', ['file-write-unlink', 'file-write-create'], writeAllowFilters, logTag));
     return rules;
 }
-/**
- * Generate filesystem write rules for sandbox profile
- */
-function generateWriteRules(config, logTag, allowGitConfig = false) {
-    if (!config) {
-        return [`(allow file-write*)`];
-    }
+function generateWriteDenyRules(denyPaths, logTag) {
     const rules = [];
-    // Generate allow rules
-    const allowFilters = new Set();
-    for (const pathPattern of config.allowOnly || []) {
-        allowFilters.add(pathFilter(normalizePathForSandbox(pathPattern)));
-    }
-    rules.push(...renderRule('allow', ['file-write*'], allowFilters, logTag));
-    // Combine user-specified and mandatory deny patterns (no ripgrep needed on macOS)
-    const denyPaths = [
-        ...(config.denyWithinAllow || []),
-        ...macGetMandatoryDenyPatterns(allowGitConfig),
-    ];
     const { groups, rest: ungrouped } = groupLiteralDenyPaths(denyPaths.map(normalizePathForSandbox));
     const groupFilters = groups.map(literalDenyGroupFilters);
     const denyFilters = new Set();
@@ -588,11 +576,57 @@ function generateWriteRules(config, logTag, allowGitConfig = false) {
     rules.push(...renderRule('deny', ['file-write-unlink', 'file-write-create'], moveFilters, logTag));
     return rules;
 }
+/**
+ * Generate filesystem write rules for sandbox profile
+ */
+function generateWriteRules(config, logTag, allowGitConfig = false, disableMandatoryWriteProtection = false) {
+    if (!config) {
+        return [`(allow file-write*)`];
+    }
+    const rules = [];
+    // Generate allow rules
+    const allowFilters = new Set();
+    for (const pathPattern of config.allowOnly || []) {
+        allowFilters.add(pathFilter(normalizePathForSandbox(pathPattern)));
+    }
+    rules.push(...renderRule('allow', ['file-write*'], allowFilters, logTag));
+    // Combine user-specified and mandatory deny patterns (no ripgrep needed on macOS)
+    const denyPaths = [
+        ...(config.denyWithinAllow || []),
+        ...macGetMandatoryDenyPatterns(allowGitConfig, disableMandatoryWriteProtection),
+    ];
+    rules.push(...generateWriteDenyRules(denyPaths, logTag));
+    // Re-open explicit writable islands inside a broader denied root, then
+    // replay any narrower deny nested inside an exception. Seatbelt is
+    // last-match winning, including rename/unlink/create operations.
+    const exceptions = (config.allowWithinDeny || []).map(pathPattern => normalizePathForSandbox(pathPattern));
+    rules.push(...renderRule('allow', ['file-write*', 'file-write-unlink', 'file-write-create'], new Set(exceptions.map(pathFilter)), logTag));
+    const nestedDenies = exceptions.length === 0
+        ? []
+        : denyPaths.filter(pathPattern => {
+            if (containsGlobChars(pathPattern))
+                return true;
+            const denied = normalizePathForSandbox(pathPattern);
+            return exceptions.some(exception => {
+                const relative = path.relative(exception, denied);
+                return relative === '' ||
+                    (relative !== '..' && !relative.startsWith(`..${path.sep}`) && !path.isAbsolute(relative));
+            });
+        });
+    rules.push(...generateWriteDenyRules(nestedDenies, logTag));
+    return rules;
+}
 /**
  * Generate complete sandbox profile
  */
-function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksProxyPort, needsNetworkRestriction, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, allowPty, allowGitConfig = false, enableWeakerNetworkIsolation = false, allowAppleEvents = false, logTag, }) {
-    const profile = [
+function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksProxyPort, needsNetworkRestriction, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, allowPty, allowGitConfig = false, disableMandatoryWriteProtection = false, enableWeakerNetworkIsolation = false, allowAppleEvents = false, hostParity = false, logTag, }) {
+    const profile = hostParity ? [
+        '(version 1)',
+        '(allow default)',
+        '',
+        `; LogTag: ${logTag}`,
+        '',
+    ] : [
         '(version 1)',
         `(deny default (with message "${logTag}"))`,
         '',
@@ -763,6 +797,20 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
         ')',
         '',
     ];
+    if (hostParity && !allowAppleEvents) {
+        // A host-parity profile starts from `(allow default)`, so omitting the
+        // ordinary opt-in allows does not subtract ambient Launch Services or
+        // Apple Events authority. Emit the denials in this profile instead of
+        // nesting a second sandbox-exec: macOS can reject applying that nested
+        // profile after the outer SRT profile is active.
+        profile.push('; Host-parity Apple Events subtraction');
+        profile.push('(deny appleevent-send)');
+        profile.push('(deny lsopen)');
+        profile.push('(deny mach-lookup (global-name "com.apple.coreservices.appleevents"))');
+        profile.push('(deny mach-lookup (global-name "com.apple.CoreServices.coreservicesd"))');
+        profile.push('(deny mach-lookup (global-name "com.apple.coreservices.quarantine-resolver"))');
+        profile.push('');
+    }
     // Network rules
     profile.push('; Network');
     if (!needsNetworkRestriction) {
@@ -833,6 +881,21 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
             profile.push(`(allow network-outbound (remote ip "localhost:${socksProxyPort}"))`);
         }
     }
+    if (hostParity && readConfig?.denyOnly) {
+        // Explicitly read-denied Unix socket paths are authority subtractions
+        // too. `allowAllUnixSockets` above is required for ordinary host parity;
+        // these last-match-wins rules remove only the exact protected endpoints.
+        const deniedSockets = [...new Set(readConfig.denyOnly
+                .filter(socketPath => !containsGlobChars(socketPath))
+                .map(normalizePathForSandbox))];
+        if (deniedSockets.length > 0) {
+            profile.push('; Host-parity Unix socket subtractions');
+            for (const socketPath of deniedSockets) {
+                profile.push(`(deny network-bind (local unix-socket (literal ${escapePath(socketPath)})))`);
+                profile.push(`(deny network-outbound (remote unix-socket (literal ${escapePath(socketPath)})))`);
+            }
+        }
+    }
     profile.push('');
     // Read rules
     // Pass write-allowed paths so that move-blocking deny rules in the read section
@@ -845,7 +908,7 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
     profile.push('');
     // Write rules
     profile.push('; File write');
-    profile.push(...generateWriteRules(writeConfig, logTag, allowGitConfig));
+    profile.push(...generateWriteRules(writeConfig, logTag, allowGitConfig, disableMandatoryWriteProtection));
     // Read-denied paths inside write roots: the read section's unlink/create
     // re-allow for write roots (and the write section's file-write* allows)
     // must not leave them renameable. Last in the profile so nothing above
@@ -884,7 +947,7 @@ function escapePath(pathStr) {
  * Wrap command with macOS sandbox
  */
 export function wrapCommandWithSandboxMacOS(params) {
-    const { command, commandId, needsNetworkRestriction, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, javaAgentJarPath, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, readConfig: readConfigIn, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, allowPty, allowGitConfig = false, gitSafeDirectories, enableWeakerNetworkIsolation = false, allowAppleEvents = false, binShell, } = params;
+    const { command, commandId, needsNetworkRestriction, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, javaAgentJarPath, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, readConfig: readConfigIn, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, allowPty, allowGitConfig = false, disableMandatoryWriteProtection = false, gitSafeDirectories, enableWeakerNetworkIsolation = false, allowAppleEvents = false, hostParity = false, binShell, } = params;
     // SBPL cannot redirect a read to different bytes, so whole-file masking
     // degrades to read-deny on macOS: the sandboxed process gets EPERM
     // instead of the sentinel. The DYLD interposer (a later step) lifts
@@ -935,8 +998,10 @@ export function wrapCommandWithSandboxMacOS(params) {
         allowMachLookup,
         allowPty,
         allowGitConfig,
+        disableMandatoryWriteProtection,
         enableWeakerNetworkIsolation,
         allowAppleEvents,
+        hostParity,
         logTag,
     });
     // Generate proxy environment variables using shared utility
diff --git a/dist/sandbox/sandbox-config.d.ts b/dist/sandbox/sandbox-config.d.ts
index 42b8e0c2678fc2219c1305b1dd4319c3b94415e9..6e591ac65919e709b67f5861341d1539277c6707 100644
--- a/dist/sandbox/sandbox-config.d.ts
+++ b/dist/sandbox/sandbox-config.d.ts
@@ -721,21 +721,27 @@ export declare const FilesystemConfigSchema: z.ZodObject<{
     allowRead: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     allowWrite: z.ZodArray<z.ZodString, "many">;
     denyWrite: z.ZodArray<z.ZodString, "many">;
+    allowWriteWithinDeny: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     allowGitConfig: z.ZodOptional<z.ZodBoolean>;
+    disableMandatoryWriteProtection: z.ZodOptional<z.ZodBoolean>;
 }, "strip", z.ZodTypeAny, {
     denyRead: string[];
     allowWrite: string[];
     denyWrite: string[];
     disabled?: boolean | undefined;
     allowRead?: string[] | undefined;
+    allowWriteWithinDeny?: string[] | undefined;
     allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
 }, {
     denyRead: string[];
     allowWrite: string[];
     denyWrite: string[];
     disabled?: boolean | undefined;
     allowRead?: string[] | undefined;
+    allowWriteWithinDeny?: string[] | undefined;
     allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
 }>;
 /**
  * Configuration schema for ignoring specific sandbox violations
@@ -970,22 +976,29 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         allowRead: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         allowWrite: z.ZodArray<z.ZodString, "many">;
         denyWrite: z.ZodArray<z.ZodString, "many">;
+        allowWriteWithinDeny: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         allowGitConfig: z.ZodOptional<z.ZodBoolean>;
+        disableMandatoryWriteProtection: z.ZodOptional<z.ZodBoolean>;
     }, "strip", z.ZodTypeAny, {
         denyRead: string[];
         allowWrite: string[];
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+        disableMandatoryWriteProtection?: boolean | undefined;
     }, {
         denyRead: string[];
         allowWrite: string[];
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+        disableMandatoryWriteProtection?: boolean | undefined;
     }>;
+    hostParity: z.ZodOptional<z.ZodBoolean>;
     credentials: z.ZodOptional<z.ZodObject<{
         files: z.ZodOptional<z.ZodArray<z.ZodObject<{
             path: z.ZodString;
@@ -1207,6 +1220,19 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     }>>;
     ignoreViolations: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodArray<z.ZodString, "many">>>;
     enableWeakerNestedSandbox: z.ZodOptional<z.ZodBoolean>;
+    linuxPrivilegedWorker: z.ZodOptional<z.ZodObject<{
+        uid: z.ZodNumber;
+        gid: z.ZodNumber;
+        setprivPath: z.ZodEffects<z.ZodString, string, string>;
+    }, "strict", z.ZodTypeAny, {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    }, {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    }>>;
     enableWeakerNetworkIsolation: z.ZodOptional<z.ZodBoolean>;
     allowAppleEvents: z.ZodOptional<z.ZodBoolean>;
     ripgrep: z.ZodOptional<z.ZodObject<{
@@ -1690,6 +1716,14 @@ export type GitConfig = z.infer<typeof GitConfigSchema>;
 export type SeccompConfig = z.infer<typeof SeccompConfigSchema>;
 export type SrtWinConfig = z.infer<typeof SrtWinConfigSchema>;
 export type WindowsConfig = z.infer<typeof WindowsConfigSchema>;
-export type SandboxRuntimeConfig = z.infer<typeof SandboxRuntimeConfigSchema>;
+export type SandboxRuntimeConfig = Omit<z.infer<typeof SandboxRuntimeConfigSchema>, "filesystem"> & {
+    filesystem: FilesystemConfig;
+    hostParity?: boolean;
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    };
+};
 export {};
 //# sourceMappingURL=sandbox-config.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/sandbox-config.js b/dist/sandbox/sandbox-config.js
index f5f3f045aa5b65f9efe34492e29bcc122a92bc1f..c08107ffb88803db3bad5539d5b9913b5736529b 100644
--- a/dist/sandbox/sandbox-config.js
+++ b/dist/sandbox/sandbox-config.js
@@ -776,10 +776,18 @@ export const FilesystemConfigSchema = z.object({
     denyWrite: z
         .array(filesystemPathSchema)
         .describe('Paths denied for writing (takes precedence over allowWrite)'),
+    allowWriteWithinDeny: z
+        .array(filesystemPathSchema)
+        .optional()
+        .describe('Paths to re-allow for writing within a broader denyWrite region. Narrower denyWrite entries inside an exception remain denied.'),
     allowGitConfig: z
         .boolean()
         .optional()
         .describe('Allow writes to .git/config files (default: false). Enables git remote URL updates while keeping .git/hooks protected.'),
+    disableMandatoryWriteProtection: z
+        .boolean()
+        .optional()
+        .describe('Disable the built-in opinionated project/config write deny list while retaining explicit allowWrite and denyWrite enforcement. Intended for embedders that supply their own complete denyWrite policy; default: false.'),
 });
 /**
  * Configuration schema for ignoring specific sandbox violations
@@ -902,6 +910,14 @@ export const SeccompConfigSchema = z.object({
         'applyPath resolves inside the bwrap namespace and that the target ' +
         'binary implements the apply-seccomp interface when ARGV0 matches.'),
 });
+/** Trusted cloud-worker identity supplied only by a root-owned supervisor. */
+const LinuxPrivilegedWorkerConfigSchema = z
+    .object({
+    uid: z.number().int().min(1).max(2147483647),
+    gid: z.number().int().min(1).max(2147483647),
+    setprivPath: binaryPathSchema,
+})
+    .strict();
 /**
  * Main configuration schema for Sandbox Runtime validation
  */
@@ -909,6 +925,10 @@ export const SandboxRuntimeConfigSchema = z
     .object({
     network: NetworkConfigSchema.describe('Network restrictions configuration'),
     filesystem: FilesystemConfigSchema.describe('Filesystem restrictions configuration'),
+    hostParity: z
+        .boolean()
+        .optional()
+        .describe('Preserve ordinary host process, IPC, device, and network access while enforcing explicit filesystem policy.'),
     credentials: CredentialsConfigSchema.optional().describe('Credential handling configuration. Only the explicitly declared files ' +
         'and environment variables are restricted.'),
     ignoreViolations: IgnoreViolationsConfigSchema.optional().describe('Optional configuration for ignoring specific violations'),
@@ -916,6 +936,7 @@ export const SandboxRuntimeConfigSchema = z
         .boolean()
         .optional()
         .describe('Enable weaker nested sandbox mode (for Docker environments)'),
+    linuxPrivilegedWorker: LinuxPrivilegedWorkerConfigSchema.optional().describe('Linux only: trusted root-supervisor mode. The workload enters the supplied non-root uid/gid with locked securebits, empty capability sets, and no_new_privs.'),
     enableWeakerNetworkIsolation: z
         .boolean()
         .optional()
@@ -966,6 +987,13 @@ export const SandboxRuntimeConfigSchema = z
         '`safe.directory` without adding them to `filesystem.allowWrite`.'),
 })
     .superRefine((cfg, ctx) => {
+    if (cfg.linuxPrivilegedWorker && cfg.enableWeakerNestedSandbox) {
+        ctx.addIssue({
+            code: z.ZodIssueCode.custom,
+            path: ['linuxPrivilegedWorker'],
+            message: 'linuxPrivilegedWorker and enableWeakerNestedSandbox are mutually exclusive',
+        });
+    }
     const creds = cfg.credentials;
     if (!creds)
         return;
diff --git a/dist/sandbox/sandbox-manager.js b/dist/sandbox/sandbox-manager.js
index 753c7545c8080796feaa23d2c91ff6cb9f794de8..371416f76216e8c318ecb4303e6bd1997c7f9faf 100644
--- a/dist/sandbox/sandbox-manager.js
+++ b/dist/sandbox/sandbox-manager.js
@@ -677,6 +677,15 @@ async function initialize(runtimeConfig, sandboxAskCallback, enableLogMonitor =
     // Initialize network infrastructure
     initializationPromise = (async () => {
         try {
+            // Host-parity mode intentionally preserves the host's direct
+            // network behavior. Do not open proxy listeners or create a Linux
+            // network bridge that no wrapped command will use.
+            if (getHostParity()) {
+                const context = {};
+                managerContext = context;
+                logForDebugging('Host-parity network infrastructure bypassed');
+                return context;
+            }
             // On Windows the WFP loopback permit covers a fixed port
             // range, so the proxies must bind inside it. Other platforms
             // bake the actual ephemeral port into the sandbox profile, so
@@ -986,6 +995,7 @@ function getFsWriteConfig() {
     return {
         allowOnly,
         denyWithinAllow: denyPaths,
+        allowWithinDeny: config.filesystem.allowWriteWithinDeny ?? [],
     };
 }
 /**
@@ -1115,12 +1125,18 @@ function getIgnoreViolations() {
 function getEnableWeakerNestedSandbox() {
     return config?.enableWeakerNestedSandbox;
 }
+function getLinuxPrivilegedWorker() {
+    return config?.linuxPrivilegedWorker;
+}
 function getEnableWeakerNetworkIsolation() {
     return config?.enableWeakerNetworkIsolation;
 }
 function getAllowAppleEvents() {
     return config?.allowAppleEvents;
 }
+function getHostParity() {
+    return config?.hostParity ?? false;
+}
 function getRipgrepConfig() {
     return config?.ripgrep ?? { command: 'rg' };
 }
@@ -1130,6 +1146,9 @@ function getMandatoryDenySearchDepth() {
 function getAllowGitConfig() {
     return config?.filesystem?.allowGitConfig ?? false;
 }
+function getDisableMandatoryWriteProtection() {
+    return config?.filesystem?.disableMandatoryWriteProtection ?? false;
+}
 /**
  * Union of session-level and per-call `git.safeDirectories`. Marks
  * paths as `safe.directory` (dubious-ownership bypass) WITHOUT
@@ -1226,6 +1245,9 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
             denyWithinAllow: stripWriteGlobs(customConfig?.filesystem?.denyWrite ??
                 config?.filesystem.denyWrite ??
                 []),
+            allowWithinDeny: stripWriteGlobs(customConfig?.filesystem?.allowWriteWithinDeny ??
+                config?.filesystem.allowWriteWithinDeny ??
+                []),
         };
         // Credential deny paths are unioned with the caller's denyRead — never
         // replacing it — so explicit filesystem restrictions always survive.
@@ -1271,8 +1293,9 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
     // 1. customConfig has network.allowedDomains defined (even if empty array = block all)
     // 2. OR config has network.allowedDomains defined (even if empty array = block all)
     // An empty allowedDomains array means "no domains allowed" = block all network access
-    const hasNetworkConfig = customConfig?.network?.allowedDomains !== undefined ||
-        config?.network?.allowedDomains !== undefined;
+    const hasNetworkConfig = !getHostParity() &&
+        (customConfig?.network?.allowedDomains !== undefined ||
+            config?.network?.allowedDomains !== undefined);
     // Network RESTRICTION is needed whenever network config is specified
     // This includes empty allowedDomains which means "block all network"
     const needsNetworkRestriction = hasNetworkConfig;
@@ -1313,9 +1336,11 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
                 ignoreViolations: getIgnoreViolations(),
                 allowPty,
                 allowGitConfig: getAllowGitConfig(),
+                disableMandatoryWriteProtection: getDisableMandatoryWriteProtection(),
                 gitSafeDirectories,
                 enableWeakerNetworkIsolation: getEnableWeakerNetworkIsolation(),
                 allowAppleEvents: getAllowAppleEvents(),
+                hostParity: getHostParity(),
                 binShell,
             });
         case 'linux':
@@ -1346,11 +1371,14 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
                 maskedFileBinds: credentialRestrictions.maskedFileBinds,
                 maskedFileStoreDir: credentialRestrictions.maskedFileStoreDir,
                 enableWeakerNestedSandbox: getEnableWeakerNestedSandbox(),
+                linuxPrivilegedWorker: getLinuxPrivilegedWorker(),
                 allowAllUnixSockets: getAllowAllUnixSockets(),
+                hostParity: getHostParity(),
                 binShell,
                 ripgrepConfig: getRipgrepConfig(),
                 mandatoryDenySearchDepth: getMandatoryDenySearchDepth(),
                 allowGitConfig: getAllowGitConfig(),
+                disableMandatoryWriteProtection: getDisableMandatoryWriteProtection(),
                 gitSafeDirectories,
                 seccompConfig: getSeccompConfig(),
                 bwrapPath: config?.bwrapPath,
@@ -1395,8 +1423,9 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
 async function wrapWithSandboxArgv(command, binShell, customConfig, abortSignal, cwd, options) {
     const platform = getPlatform();
     if (platform === 'windows') {
-        const hasNetworkConfig = customConfig?.network?.allowedDomains !== undefined ||
-            config?.network?.allowedDomains !== undefined;
+        const hasNetworkConfig = !getHostParity() &&
+            (customConfig?.network?.allowedDomains !== undefined ||
+                config?.network?.allowedDomains !== undefined);
         if (hasNetworkConfig) {
             await waitForNetworkInitialization();
         }
diff --git a/dist/sandbox/sandbox-schemas.d.ts b/dist/sandbox/sandbox-schemas.d.ts
index d753a3a3a500be8c485f511ec5a94d5921094b91..743e2cb71b1ce5d15aa70553abd18ddab23dd01c 100644
--- a/dist/sandbox/sandbox-schemas.d.ts
+++ b/dist/sandbox/sandbox-schemas.d.ts
@@ -34,6 +34,7 @@ export interface FsReadRestrictionConfig {
 export interface FsWriteRestrictionConfig {
     allowOnly: string[];
     denyWithinAllow: string[];
+    allowWithinDeny?: string[];
 }
 /**
  * Credential restriction config (internal structure built from the
