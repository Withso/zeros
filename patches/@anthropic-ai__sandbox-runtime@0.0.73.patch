diff --git a/dist/sandbox/body-substitution.d.ts b/dist/sandbox/body-substitution.d.ts
index 0cce60dd791c76286bddf24ddb6435ca372707a8..f24453a4a5a933b39f4249757099ccce8dba8fcc 100644
--- a/dist/sandbox/body-substitution.d.ts
+++ b/dist/sandbox/body-substitution.d.ts
@@ -28,7 +28,7 @@ export interface SentinelBufferPair {
  * no credential is injectable at the host — the caller must keep the
  * existing bare pipe, byte-identical.
  */
-export type GetBodySubstitutions = (destHost: string) => SentinelBufferPair[] | undefined;
+export type GetBodySubstitutions = (destHost: string, destPort: number) => SentinelBufferPair[] | undefined;
 /** True when substitution cannot change the body length. */
 export declare function allLengthMatched(pairs: readonly SentinelBufferPair[]): boolean;
 /**
@@ -42,7 +42,7 @@ export declare function allLengthMatched(pairs: readonly SentinelBufferPair[]):
  * fake), Content-Length is deleted from `fwdHeaders` so the outbound
  * request re-frames as chunked; otherwise Content-Length stays verbatim.
  */
-export declare function prepareBodySubstitution(getBodySubstitutions: GetBodySubstitutions | undefined, req: IncomingMessage, fwdHeaders: IncomingHttpHeaders, destHost: string): Transform | undefined;
+export declare function prepareBodySubstitution(getBodySubstitutions: GetBodySubstitutions | undefined, req: IncomingMessage, fwdHeaders: IncomingHttpHeaders, destHost: string, destPort: number): Transform | undefined;
 /**
  * Transform that replaces every occurrence of each pair's sentinel bytes
  * with its real-value bytes.
diff --git a/dist/sandbox/body-substitution.js b/dist/sandbox/body-substitution.js
index 900379823b8933b02e401730a6a50f6ddeb7aa15..f3c32a02b61ed52830fa386ff1ccf06c41157754 100644
--- a/dist/sandbox/body-substitution.js
+++ b/dist/sandbox/body-substitution.js
@@ -16,7 +16,7 @@
  */
 import { Transform } from 'node:stream';
 import { logForDebugging } from '../utils/debug.js';
-import { BODYLESS_METHODS } from './request-filter.js';
+import { BODYLESS_METHODS, requestDeclaresBody } from './request-filter.js';
 /** True when substitution cannot change the body length. */
 export function allLengthMatched(pairs) {
     return pairs.every(p => p.sentinel.length === p.realValue.length);
@@ -32,18 +32,17 @@ export function allLengthMatched(pairs) {
  * fake), Content-Length is deleted from `fwdHeaders` so the outbound
  * request re-frames as chunked; otherwise Content-Length stays verbatim.
  */
-export function prepareBodySubstitution(getBodySubstitutions, req, fwdHeaders, destHost) {
+export function prepareBodySubstitution(getBodySubstitutions, req, fwdHeaders, destHost, destPort) {
     if (getBodySubstitutions === undefined)
         return undefined;
     // A bodyless-method request can still carry a declared body (GET-with-body
     // APIs are legal HTTP and forwarded today) — skip only when there is
     // provably nothing to scan.
-    const declaresBody = req.headers['content-length'] !== undefined ||
-        req.headers['transfer-encoding'] !== undefined;
+    const declaresBody = requestDeclaresBody(req);
     if (BODYLESS_METHODS.has(req.method ?? 'GET') && !declaresBody) {
         return undefined;
     }
-    const pairs = getBodySubstitutions(destHost);
+    const pairs = getBodySubstitutions(destHost, destPort);
     if (!pairs?.length)
         return undefined;
     if (req.headers['content-encoding'] !== undefined) {
diff --git a/dist/sandbox/credential-mask-env.d.ts b/dist/sandbox/credential-mask-env.d.ts
index e30c0b44d5d37b9b7c9caac6f5662ad4dc0ca0de..dabc70e451afb123fb19986ede2e8d560cab4984 100644
--- a/dist/sandbox/credential-mask-env.d.ts
+++ b/dist/sandbox/credential-mask-env.d.ts
@@ -66,5 +66,5 @@ export interface MaskedEnvBuildResult {
  * `mode: "deny"` entries are ignored here; the caller handles them
  * directly (they need no registry or host environment access).
  */
-export declare function buildMaskedEnvVars(envVars: readonly CredentialEnvVarConfig[], allowedDomains: readonly string[], registry: SentinelRegistry, env?: Record<string, string | undefined>): MaskedEnvBuildResult;
+export declare function buildMaskedEnvVars(envVars: readonly CredentialEnvVarConfig[], allowedDomains: readonly string[], registry: SentinelRegistry, env?: Record<string, string | undefined>, allowPlaintextInject?: boolean): MaskedEnvBuildResult;
 //# sourceMappingURL=credential-mask-env.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/credential-mask-env.js b/dist/sandbox/credential-mask-env.js
index 139403549f5e47ca18c137f7f6aa002055c8b560..f9bb5cfb9911dd87b9e9d139929a7a5cd1d87364 100644
--- a/dist/sandbox/credential-mask-env.js
+++ b/dist/sandbox/credential-mask-env.js
@@ -62,7 +62,7 @@ const ENV_EXTRACT_KEY_PREFIX = 'env:';
  * `mode: "deny"` entries are ignored here; the caller handles them
  * directly (they need no registry or host environment access).
  */
-export function buildMaskedEnvVars(envVars, allowedDomains, registry, env = process.env) {
+export function buildMaskedEnvVars(envVars, allowedDomains, registry, env = process.env, allowPlaintextInject = false) {
     const setEnvVars = {};
     const degradeToUnsetNames = [];
     for (const v of envVars) {
@@ -78,6 +78,7 @@ export function buildMaskedEnvVars(envVars, allowedDomains, registry, env = proc
         // the sandbox can reach — narrow it explicitly when the credential
         // should only go to a subset.
         const injectHosts = v.injectHosts ?? allowedDomains;
+        const plaintext = v.allowPlaintextInject ?? allowPlaintextInject;
         if (v.decode === 'jwt') {
             if (!verifyJwt(real)) {
                 // Nothing was masked — the operator declared the value a JWT but
@@ -97,7 +98,7 @@ export function buildMaskedEnvVars(envVars, allowedDomains, registry, env = proc
                 // rebuilt fake token is itself registered as a sentinel for the
                 // whole real token, so both the bearer path (token sent
                 // verbatim) and the extracted-claim path substitute on egress.
-                const masked = maskJwtClaims(real, v.maskClaims, (claim, value) => registry.register(`${ENV_EXTRACT_KEY_PREFIX}${v.name}#${claim}`, value, injectHosts));
+                const masked = maskJwtClaims(real, v.maskClaims, (claim, value) => registry.register(`${ENV_EXTRACT_KEY_PREFIX}${v.name}#${claim}`, value, injectHosts, plaintext));
                 if (masked === null) {
                     // A verified JWT none of whose named claims are present as
                     // strings: nothing was masked — same fail-open posture as a
@@ -118,22 +119,22 @@ export function buildMaskedEnvVars(envVars, allowedDomains, registry, env = proc
                         `${JSON.stringify(skipped)} absent or non-string in the ` +
                         `JWT — skipped.`);
                 }
-                setEnvVars[v.name] = registry.registerWithSentinel(`${ENV_EXTRACT_KEY_PREFIX}${v.name}`, masked.fakeToken, real, injectHosts);
+                setEnvVars[v.name] = registry.registerWithSentinel(`${ENV_EXTRACT_KEY_PREFIX}${v.name}`, masked.fakeToken, real, injectHosts, plaintext);
                 continue;
             }
             // JWT-shaped fake so token-parsing tools inside the sandbox keep
             // working; the proxy swaps the whole fake token on egress. Keyed
             // env:<NAME> — caller-minted keys stay disjoint from the file:
             // namespace and from plain masked env vars.
-            setEnvVars[v.name] = registry.registerWithSentinel(`${ENV_EXTRACT_KEY_PREFIX}${v.name}`, mintFakeJwt(randomUUID()), real, injectHosts);
+            setEnvVars[v.name] = registry.registerWithSentinel(`${ENV_EXTRACT_KEY_PREFIX}${v.name}`, mintFakeJwt(randomUUID()), real, injectHosts, plaintext);
             continue;
         }
         if (v.extract === undefined) {
             // Whole-value: one sentinel for the entire value.
-            setEnvVars[v.name] = registry.register(v.name, real, injectHosts);
+            setEnvVars[v.name] = registry.register(v.name, real, injectHosts, plaintext);
             continue;
         }
-        const extracted = extractAndSubstitute(real, v.extract, (cap, i) => registry.register(`${ENV_EXTRACT_KEY_PREFIX}${v.name}#${i}`, cap, injectHosts));
+        const extracted = extractAndSubstitute(real, v.extract, (cap, i) => registry.register(`${ENV_EXTRACT_KEY_PREFIX}${v.name}#${i}`, cap, injectHosts, plaintext));
         if (extracted === null) {
             const onNoMatch = v.onExtractNoMatch ?? 'warn';
             if (onNoMatch === 'error') {
diff --git a/dist/sandbox/credential-mask-files.d.ts b/dist/sandbox/credential-mask-files.d.ts
index 9030c5b4ae5689127aa9e574f5225b05346233f5..a2143f7aa366de0866e0be9245d22d67d62da4d8 100644
--- a/dist/sandbox/credential-mask-files.d.ts
+++ b/dist/sandbox/credential-mask-files.d.ts
@@ -115,6 +115,6 @@ export interface MaskedFileBuildResult {
  * The directory check is the authoritative one (the schema only catches a
  * trailing slash); whole-file masking has no meaning for a directory.
  */
-export declare function buildMaskedFileBinds(files: readonly CredentialFileConfig[], allowedDomains: readonly string[], registry: SentinelRegistry, store: MaskedFileStore): MaskedFileBuildResult;
+export declare function buildMaskedFileBinds(files: readonly CredentialFileConfig[], allowedDomains: readonly string[], registry: SentinelRegistry, store: MaskedFileStore, allowPlaintextInject?: boolean): MaskedFileBuildResult;
 export declare const MASKED_FILE_STORE_PREFIX = "srt-credmask-";
 //# sourceMappingURL=credential-mask-files.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/credential-mask-files.js b/dist/sandbox/credential-mask-files.js
index 017935a9d917c4219f371768a38982d4fc438729..8b392899257d4e0490928af309c160923892ccfb 100644
--- a/dist/sandbox/credential-mask-files.js
+++ b/dist/sandbox/credential-mask-files.js
@@ -142,7 +142,7 @@ export class MaskedFileStore {
  * The directory check is the authoritative one (the schema only catches a
  * trailing slash); whole-file masking has no meaning for a directory.
  */
-export function buildMaskedFileBinds(files, allowedDomains, registry, store) {
+export function buildMaskedFileBinds(files, allowedDomains, registry, store, allowPlaintextInject = false) {
     const binds = [];
     const degradeToDenyPaths = [];
     for (const f of files) {
@@ -176,11 +176,12 @@ export function buildMaskedFileBinds(files, allowedDomains, registry, store) {
             continue;
         }
         const injectHosts = f.injectHosts ?? allowedDomains;
+        const plaintext = f.allowPlaintextInject ?? allowPlaintextInject;
         const key = FILE_KEY_PREFIX + realPath;
         let fakeContent;
         if (f.extract === undefined && f.decode === undefined) {
             // Whole-file: one sentinel for the entire content.
-            fakeContent = registry.register(key, content, injectHosts);
+            fakeContent = registry.register(key, content, injectHosts, plaintext);
         }
         else {
             // An explicit extract pattern wins; decode without one falls back to
@@ -199,7 +200,7 @@ export function buildMaskedFileBinds(files, allowedDomains, registry, store) {
                     // rebuilt fake token is itself registered as a sentinel for
                     // the whole real token, so both the bearer path (token sent
                     // verbatim) and the extracted-claim path substitute on egress.
-                    const masked = maskJwtClaims(cap, f.maskClaims, (claim, real) => registry.register(`${key}#jwt${i}.${claim}`, real, injectHosts));
+                    const masked = maskJwtClaims(cap, f.maskClaims, (claim, real) => registry.register(`${key}#jwt${i}.${claim}`, real, injectHosts, plaintext));
                     if (masked === null) {
                         // A verified JWT none of whose named claims are present as
                         // strings: nothing to mask in THIS token — leave it as-is;
@@ -216,15 +217,15 @@ export function buildMaskedFileBinds(files, allowedDomains, registry, store) {
                             `verified JWT — skipped.`);
                     }
                     maskedCount++;
-                    return registry.registerWithSentinel(name, masked.fakeToken, cap, injectHosts);
+                    return registry.registerWithSentinel(name, masked.fakeToken, cap, injectHosts, plaintext);
                 }
                 maskedCount++;
                 // For decode the fake must keep the token's shape: a JWT-shaped
                 // sentinel keeps client-side parsers (segment count, payload
                 // decode, exp checks) working inside the sandbox.
                 return f.decode === 'jwt'
-                    ? registry.registerWithSentinel(name, mintFakeJwt(randomUUID()), cap, injectHosts)
-                    : registry.register(name, cap, injectHosts);
+                    ? registry.registerWithSentinel(name, mintFakeJwt(randomUUID()), cap, injectHosts, plaintext)
+                    : registry.register(name, cap, injectHosts, plaintext);
             }, { maskDuplicates: f.maskDuplicates ?? false });
             if (extracted === null || maskedCount === 0) {
                 // Nothing was masked — either the pattern matched nothing or, with
diff --git a/dist/sandbox/credential-sentinel.d.ts b/dist/sandbox/credential-sentinel.d.ts
index 3dbbbb9e563b4cab5223648262672f37b75f428f..b2557d99755bad60ba7e06d67bf3cde547bd8457 100644
--- a/dist/sandbox/credential-sentinel.d.ts
+++ b/dist/sandbox/credential-sentinel.d.ts
@@ -11,7 +11,7 @@ import type { IncomingHttpHeaders } from 'node:http';
 import type { SentinelBufferPair } from './body-substitution.js';
 export declare const SENTINEL_PREFIX = "fake_value_";
 /** Predicate matching a destination host against one injectHosts pattern. */
-export type HostMatcher = (host: string, pattern: string) => boolean;
+export type HostMatcher = (host: string, port: number, pattern: string) => boolean;
 /**
  * Sentinel↔real-value map for one sandbox session, keyed by credential name.
  *
@@ -42,7 +42,7 @@ export declare class SentinelRegistry {
      * sentinel un-length-matched; body substitution then re-frames as chunked
      * rather than trusting a stale Content-Length.)
      */
-    register(name: string, realValue: string, injectHosts: readonly string[]): string;
+    register(name: string, realValue: string, injectHosts: readonly string[], allowPlaintextInject?: boolean): string;
     /**
      * Like {@link register}, but with a caller-minted sentinel instead of the
      * default `fake_value_<uuid4>` — used when the fake must keep the real
@@ -65,7 +65,7 @@ export declare class SentinelRegistry {
      * generally not length-matched, and body substitution at hosts where they
      * inject falls back to chunked framing.
      */
-    registerWithSentinel(name: string, sentinel: string, realValue: string, injectHosts: readonly string[]): string;
+    registerWithSentinel(name: string, sentinel: string, realValue: string, injectHosts: readonly string[], allowPlaintextInject?: boolean): string;
     /** Real value for `sentinel`, or undefined if not registered. */
     lookupReal(sentinel: string): string | undefined;
     /**
@@ -74,7 +74,7 @@ export declare class SentinelRegistry {
      * exempted from TLS termination (so substitution can never run there) but
      * a masked credential is configured for injection at it.
      */
-    namesInjectableAt(destHost: string, matches: HostMatcher): string[];
+    namesInjectableAt(destHost: string, destPort: number, matches: HostMatcher, plaintext?: boolean): string[];
     /**
      * Sentinel→real byte pairs for every credential whose `injectHosts`
      * cover `destHost` — the substitution set the body transform scans for.
@@ -82,7 +82,7 @@ export declare class SentinelRegistry {
      * built per call so a re-registered credential's updated real value is
      * always current.
      */
-    sentinelsForHost(destHost: string, matches: HostMatcher): SentinelBufferPair[];
+    sentinelsForHost(destHost: string, destPort: number, matches: HostMatcher, plaintext?: boolean): SentinelBufferPair[];
     /** Iterate registered `[sentinel, realValue]` pairs. */
     entries(): IterableIterator<[string, string]>;
     /** Number of registered sentinels. */
@@ -102,7 +102,7 @@ export declare class SentinelRegistry {
      * The caller remains responsible for transport gating (TLS-terminated path
      * unless `allowPlaintextInject`).
      */
-    substituteInHeaders(headers: IncomingHttpHeaders, destHost: string, matches: HostMatcher): void;
+    substituteInHeaders(headers: IncomingHttpHeaders, destHost: string, destPort: number, matches: HostMatcher, plaintext?: boolean): void;
     private substituteInString;
 }
 //# sourceMappingURL=credential-sentinel.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/credential-sentinel.js b/dist/sandbox/credential-sentinel.js
index 03166a4a2facb7e15b6ac40f08373ad200438acd..6a79ce7da81ac59bb4124633ad5ca6051cc95f66 100644
--- a/dist/sandbox/credential-sentinel.js
+++ b/dist/sandbox/credential-sentinel.js
@@ -67,8 +67,8 @@ export class SentinelRegistry {
      * sentinel un-length-matched; body substitution then re-frames as chunked
      * rather than trusting a stale Content-Length.)
      */
-    register(name, realValue, injectHosts) {
-        return this.registerWithSentinel(name, mintSentinel(realValue), realValue, injectHosts);
+    register(name, realValue, injectHosts, allowPlaintextInject = false) {
+        return this.registerWithSentinel(name, mintSentinel(realValue), realValue, injectHosts, allowPlaintextInject);
     }
     /**
      * Like {@link register}, but with a caller-minted sentinel instead of the
@@ -92,17 +92,18 @@ export class SentinelRegistry {
      * generally not length-matched, and body substitution at hosts where they
      * inject falls back to chunked framing.
      */
-    registerWithSentinel(name, sentinel, realValue, injectHosts) {
+    registerWithSentinel(name, sentinel, realValue, injectHosts, allowPlaintextInject = false) {
         const existing = this.byName.get(name);
         if (existing !== undefined) {
             existing.realValue = realValue;
             existing.injectHosts = injectHosts;
+            existing.allowPlaintextInject = allowPlaintextInject;
             return existing.sentinel;
         }
         if (!sentinel.startsWith(SENTINEL_PREFIX)) {
             this.allSentinelsPrefixed = false;
         }
-        const entry = { name, sentinel, realValue, injectHosts };
+        const entry = { name, sentinel, realValue, injectHosts, allowPlaintextInject };
         this.byName.set(name, entry);
         this.bySentinel.set(sentinel, entry);
         return sentinel;
@@ -117,10 +118,11 @@ export class SentinelRegistry {
      * exempted from TLS termination (so substitution can never run there) but
      * a masked credential is configured for injection at it.
      */
-    namesInjectableAt(destHost, matches) {
+    namesInjectableAt(destHost, destPort, matches, plaintext = false) {
         const names = [];
         for (const e of this.byName.values()) {
-            if (e.injectHosts.some(p => matches(destHost, p)))
+            if ((!plaintext || e.allowPlaintextInject) &&
+                e.injectHosts.some(p => matches(destHost, destPort, p)))
                 names.push(e.name);
         }
         return names;
@@ -132,10 +134,11 @@ export class SentinelRegistry {
      * built per call so a re-registered credential's updated real value is
      * always current.
      */
-    sentinelsForHost(destHost, matches) {
+    sentinelsForHost(destHost, destPort, matches, plaintext = false) {
         const pairs = [];
         for (const e of this.bySentinel.values()) {
-            if (!e.injectHosts.some(p => matches(destHost, p)))
+            if ((plaintext && !e.allowPlaintextInject) ||
+                !e.injectHosts.some(p => matches(destHost, destPort, p)))
                 continue;
             pairs.push({
                 sentinel: Buffer.from(e.sentinel),
@@ -172,7 +175,7 @@ export class SentinelRegistry {
      * The caller remains responsible for transport gating (TLS-terminated path
      * unless `allowPlaintextInject`).
      */
-    substituteInHeaders(headers, destHost, matches) {
+    substituteInHeaders(headers, destHost, destPort, matches, plaintext = false) {
         if (this.bySentinel.size === 0)
             return;
         for (const [name, value] of Object.entries(headers)) {
@@ -180,15 +183,15 @@ export class SentinelRegistry {
                 continue;
             if (Array.isArray(value)) {
                 for (let i = 0; i < value.length; i++) {
-                    value[i] = this.substituteInString(value[i], destHost, matches);
+                    value[i] = this.substituteInString(value[i], destHost, destPort, matches, plaintext);
                 }
             }
             else {
-                headers[name] = this.substituteInString(value, destHost, matches);
+                headers[name] = this.substituteInString(value, destHost, destPort, matches, plaintext);
             }
         }
     }
-    substituteInString(s, destHost, matches) {
+    substituteInString(s, destHost, destPort, matches, plaintext) {
         // Fast path: while every sentinel carries the fixed prefix, a header
         // value that doesn't contain it cannot contain any sentinel. Disabled
         // once a caller-minted sentinel (arbitrary shape) is registered.
@@ -198,7 +201,8 @@ export class SentinelRegistry {
         for (const e of this.bySentinel.values()) {
             if (!out.includes(e.sentinel))
                 continue;
-            if (!e.injectHosts.some(p => matches(destHost, p)))
+            if ((plaintext && !e.allowPlaintextInject) ||
+                !e.injectHosts.some(p => matches(destHost, destPort, p)))
                 continue;
             out = out.split(e.sentinel).join(e.realValue);
         }
diff --git a/dist/sandbox/domain-pattern.d.ts b/dist/sandbox/domain-pattern.d.ts
index c612fe5282dfae53c2ede00ff58639334f86c089..cc2ac6d0b15adc0d1095217a75ea0c102f1cb2ca 100644
--- a/dist/sandbox/domain-pattern.d.ts
+++ b/dist/sandbox/domain-pattern.d.ts
@@ -70,4 +70,6 @@ export declare function matchesDomainPatternWithPort(hostname: string, port: num
  * host-scoped, and a port-scoped allow still makes the host reachable.
  */
 export declare function isInjectHostCoveredByAllowedDomains(injectHost: string, allowedDomains: readonly string[]): boolean;
+/** True when every host+port selected by `pattern` is covered by `covers`. */
+export declare function isDomainPortPatternCoveredBy(pattern: string, covers: readonly string[]): boolean;
 //# sourceMappingURL=domain-pattern.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/domain-pattern.js b/dist/sandbox/domain-pattern.js
index 05e1d9e632ea070be8f0904828080ec2d913f2d1..5082e9d9899ff385c47ff4a4c97c132ba8fbcb30 100644
--- a/dist/sandbox/domain-pattern.js
+++ b/dist/sandbox/domain-pattern.js
@@ -135,4 +135,16 @@ export function isInjectHostCoveredByAllowedDomains(injectHost, allowedDomains)
         return injectBase === allowedBase || injectBase.endsWith('.' + allowedBase);
     });
 }
+/** True when every host+port selected by `pattern` is covered by `covers`. */
+export function isDomainPortPatternCoveredBy(pattern, covers) {
+    const candidate = splitDomainPatternPort(pattern);
+    return covers.some(rawCover => {
+        const cover = splitDomainPatternPort(rawCover);
+        if (cover.port !== undefined &&
+            (candidate.port === undefined || cover.port !== candidate.port)) {
+            return false;
+        }
+        return isInjectHostCoveredByAllowedDomains(candidate.hostPattern, [cover.hostPattern]);
+    });
+}
 //# sourceMappingURL=domain-pattern.js.map
\ No newline at end of file
diff --git a/dist/sandbox/http-proxy.js b/dist/sandbox/http-proxy.js
index 4b954d49549511f9712b3b011731b8f99c35d1e7..125d5b75bd4353ac75aca40efd9ece4c1b88fc49 100644
--- a/dist/sandbox/http-proxy.js
+++ b/dist/sandbox/http-proxy.js
@@ -6,9 +6,9 @@ import { URL } from 'node:url';
 import { logForDebugging } from '../utils/debug.js';
 import { encodedCommandFromProxyUser } from './sandbox-utils.js';
 import { CRL_PATH } from './mitm-ca.js';
-import { decideAndRespond, } from './request-filter.js';
+import { decideAndRespond } from './request-filter.js';
 import { peekForClientHello, terminateAndForward, } from './tls-terminate-proxy.js';
-import { prepareBodySubstitution, } from './body-substitution.js';
+import { prepareBodySubstitution } from './body-substitution.js';
 import { canonicalizeHost, connectViaParentProxy, dialDirect, openConnectTunnel, proxyAuthHeader, selectParentProxyUrl, shouldBypassParentProxy, stripBrackets, stripHopByHop, } from './parent-proxy.js';
 export function createHttpProxyServer(options) {
     const server = createServer();
@@ -24,8 +24,7 @@ export function createHttpProxyServer(options) {
         // The peer may reset while the 400 flushes; without a listener that
         // write failure is itself an uncaughtException.
         socket.on('error', () => { });
-        if (err.code !== 'ECONNRESET' &&
-            socket.writable) {
+        if (err.code !== 'ECONNRESET' && socket.writable) {
             // Destroy only after the flush: destroying immediately races the
             // write and can reset the connection before the 400 reaches the
             // client, while end() alone can leave a half-open socket from a
@@ -43,7 +42,7 @@ export function createHttpProxyServer(options) {
         }
         socket.destroy();
     });
-    const checkAuth = (got) => {
+    const checkAuth = got => {
         if (!options.proxyAuthToken)
             return { ok: true };
         const m = /^basic\s+([a-z0-9+/=]+)\s*$/i.exec(got ?? '');
@@ -63,7 +62,9 @@ export function createHttpProxyServer(options) {
     server.on('connect', async (req, socket, head) => {
         // Attach error handler immediately to prevent unhandled errors
         socket.on('error', err => {
-            logForDebugging(`Client socket error: ${err.message}`, { level: 'error' });
+            logForDebugging(`Client socket error: ${err.message}`, {
+                level: 'error',
+            });
             // A failed write (e.g. EPIPE to a dead peer) must also release
             // the descriptor: logging alone leaves the fd open for the life
             // of the process, and an open dead-peer fd is what the runtime's
@@ -138,7 +139,7 @@ export function createHttpProxyServer(options) {
         let decisionCaptureBytes = 0;
         let capturing = false;
         const decisionData = [];
-        const onDecisionData = (chunk) => {
+        const onDecisionData = chunk => {
             if (!capturing)
                 return;
             decisionCaptureBytes += chunk.length;
@@ -184,7 +185,7 @@ export function createHttpProxyServer(options) {
         // Decision-phase status writes go through this guard: a verdict for
         // a dead client is dropped, never written. The filter's work is not
         // wasted — host-side allow/deny caches serve the client's retry.
-        const endWithStatus = (payload) => {
+        const endWithStatus = payload => {
             if (clientGone ||
                 socket.destroyed ||
                 socket.readableEnded ||
@@ -281,7 +282,7 @@ export function createHttpProxyServer(options) {
                 }
                 if (peeked.isTLS) {
                     disarmDecisionWindowEof();
-                    terminateAndForward(options.mitmCA, options.filterRequest, options.mutateHeaders, options.getBodySubstitutions, socket, peeked.head, {
+                    await terminateAndForward(options.mitmCA, options.filterRequest, options.mutateHeaders, options.getBodySubstitutions, socket, peeked.head, {
                         hostname,
                         port,
                         upstreamCA: options.tlsTerminateUpstreamCA,
@@ -476,10 +477,10 @@ export function createHttpProxyServer(options) {
             const authority = (isIP(hostname) === 6 ? `[${hostname}]` : hostname) +
                 (url.port ? `:${url.port}` : '');
             const fwdHeaders = { ...stripHopByHop(req.headers), host: authority };
-            options.mutateHeadersPlaintext?.(fwdHeaders, hostname);
+            options.mutateHeadersPlaintext?.(fwdHeaders, hostname, port);
             // Body-substitution counterpart of mutateHeadersPlaintext (opt-in via
             // the same config gate). May delete content-length from fwdHeaders.
-            const bodyTransform = prepareBodySubstitution(options.getBodySubstitutionsPlaintext, req, fwdHeaders, hostname);
+            const bodyTransform = prepareBodySubstitution(options.getBodySubstitutionsPlaintext, req, fwdHeaders, hostname, port);
             // Decide upstream route: MITM unix socket > parent HTTP proxy > direct.
             const mitmSocketPath = options.getMitmSocketPath?.(hostname);
             const parentUrl = !mitmSocketPath &&
@@ -645,7 +646,9 @@ export function createHttpProxyServer(options) {
             }
         }
         catch (err) {
-            logForDebugging(`Error handling HTTP request: ${err}`, { level: 'error' });
+            logForDebugging(`Error handling HTTP request: ${err}`, {
+                level: 'error',
+            });
             if (!res.headersSent) {
                 res.writeHead(500, { 'Content-Type': 'text/plain' });
                 res.end('Internal Server Error');
diff --git a/dist/sandbox/linux-sandbox-utils.d.ts b/dist/sandbox/linux-sandbox-utils.d.ts
index d3a3efdf46d039b24317cda40acc8ba3e5a424bb..05c86563f6956f4bf2fc1b56d7899764414d5ced 100644
--- a/dist/sandbox/linux-sandbox-utils.d.ts
+++ b/dist/sandbox/linux-sandbox-utils.d.ts
@@ -43,6 +43,13 @@ export interface LinuxSandboxParams {
      */
     maskedFileStoreDir?: string;
     enableWeakerNestedSandbox?: boolean;
+    /** Root-supervisor-only cloud worker mode. Namespace and mount setup runs
+     * before the command irreversibly enters this non-root identity. */
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    };
     allowAllUnixSockets?: boolean;
     binShell?: string;
     ripgrepConfig?: {
@@ -53,6 +60,14 @@ export interface LinuxSandboxParams {
     mandatoryDenySearchDepth?: number;
     /** Allow writes to .git/config files (default: false) */
     allowGitConfig?: boolean;
+    /** Disable built-in project/config write denies; explicit denyWrite remains enforced. */
+    disableMandatoryWriteProtection?: boolean;
+    /** Trusted Linux bind projections applied after deny masks. */
+    bindMounts?: readonly {
+        source: string;
+        destination: string;
+        readOnly: boolean;
+    }[];
     /**
      * Directories to emit as `safe.directory` via `GIT_CONFIG_*` env
      * vars (see {@link buildPosixGitSafeDirEnv}). Under `--unshare-user`
diff --git a/dist/sandbox/linux-sandbox-utils.js b/dist/sandbox/linux-sandbox-utils.js
index 4c521f62da435e7de02d1a69ceda617ea51cca08..050a92b33097bbccd417f8d3875ea562765cbdaf 100644
--- a/dist/sandbox/linux-sandbox-utils.js
+++ b/dist/sandbox/linux-sandbox-utils.js
@@ -168,7 +168,9 @@ function findFirstNonExistentComponent(targetPath) {
  * Uses a SINGLE ripgrep call with multiple glob patterns for efficiency.
  * With --max-depth limiting, this is fast enough to run on each command without memoization.
  */
-async function linuxGetMandatoryDenyPaths(ripgrepConfig = { command: 'rg' }, maxDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, abortSignal) {
+async function linuxGetMandatoryDenyPaths(ripgrepConfig = { command: 'rg' }, maxDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, abortSignal) {
+    if (disableMandatoryWriteProtection)
+        return [];
     const cwd = process.cwd();
     // Use provided signal or create a fallback controller
     const fallbackController = new AbortController();
@@ -577,29 +579,125 @@ function resolveApplySeccompPrefix(applyPath, argv0) {
     const binary = getApplySeccompBinaryPath(applyPath);
     return binary ? `${quote([binary])} ` : undefined;
 }
+/**
+ * Resolve Zeros-owned, per-launch proxy ports. Keeping these dynamic avoids
+ * occupying common developer ports (1080/3128) inside the isolated network
+ * namespace. The values are set only by the trusted outer supervisor and are
+ * validated again here because this module also has non-Zeros consumers.
+ */
+function zerosInternalProxyPort(name, fallback) {
+    const raw = process.env[name];
+    if (raw === undefined)
+        return fallback;
+    if (!/^[0-9]{1,5}$/.test(raw))
+        throw new Error(`invalid ${name}`);
+    const port = Number.parseInt(raw, 10);
+    if (!Number.isInteger(port) || port < 1024 || port > 65535)
+        throw new Error(`invalid ${name}`);
+    return port;
+}
+function zerosInternalProxyPorts() {
+    const http = zerosInternalProxyPort('ZEROS_ZSR_INTERNAL_HTTP_PORT', 3128);
+    const socks = zerosInternalProxyPort('ZEROS_ZSR_INTERNAL_SOCKS_PORT', 1080);
+    if (http === socks)
+        throw new Error('Zeros internal proxy ports must be distinct');
+    return { http, socks };
+}
 /**
  * Build the command that runs inside the sandbox.
- * Sets up HTTP proxy on port 3128 and SOCKS proxy on port 1080
  */
-function buildSandboxCommand(httpSocketPath, socksSocketPath, userCommand, applySeccompPrefix, shell, socatPath) {
+function zerosNetworkBridgeBootstrap(infrastructurePrefix = '') {
+    if (process.env.ZEROS_ZSR_NETWORK_BRIDGE !== '1')
+        return [];
+    const runtime = process.env.ZEROS_ZSR_NETWORK_BRIDGE_RUNTIME;
+    const script = process.env.ZEROS_ZSR_NETWORK_BRIDGE_SCRIPT;
+    const descriptor = process.env.ZEROS_ZSR_NETWORK_BRIDGE_DESCRIPTOR;
+    const socketPath = process.env.ZEROS_ZSR_NETWORK_BRIDGE_SOCKET;
+    for (const [label, value] of [
+        ['runtime', runtime],
+        ['script', script],
+        ['descriptor', descriptor],
+        ['socket', socketPath],
+    ]) {
+        if (!value || !path.isAbsolute(value) || value.includes('\0')) {
+            throw new Error(`invalid Zeros network bridge ${label}`);
+        }
+    }
+    for (const [label, value] of [
+        ['runtime', runtime],
+        ['script', script],
+        ['descriptor', descriptor],
+    ]) {
+        const stat = fs.lstatSync(value);
+        if (!stat.isFile() || stat.isSymbolicLink() || fs.realpathSync(value) !== value) {
+            throw new Error(`Zeros network bridge ${label} is not a canonical file`);
+        }
+    }
+    const socketParent = path.dirname(socketPath);
+    const parentStat = fs.lstatSync(socketParent);
+    if (!parentStat.isDirectory() ||
+        parentStat.isSymbolicLink() ||
+        fs.realpathSync(socketParent) !== socketParent) {
+        throw new Error('Zeros network bridge socket parent is not canonical');
+    }
+    const launch = `${infrastructurePrefix}${quote([runtime, script, '--config', descriptor])}`;
+    const socket = quote([socketPath]);
+    const output = process.env.SRT_DEBUG ? '' : ' >/dev/null 2>&1';
+    return [
+        `${launch}${output} &`,
+        'zsr_network_bridge_pid=$!',
+        'zsr_network_bridge_ready=0',
+        `zsr_network_bridge_attempt=0; while [ "$zsr_network_bridge_attempt" -lt 500 ]; do if [ -S ${socket} ]; then zsr_network_bridge_ready=1; break; fi; if ! kill -0 "$zsr_network_bridge_pid" 2>/dev/null; then break; fi; zsr_network_bridge_attempt=$((zsr_network_bridge_attempt + 1)); sleep 0.01; done`,
+        'if [ "$zsr_network_bridge_ready" -ne 1 ]; then exit 125; fi',
+    ];
+}
+function privilegedSetprivPrefix(worker, enterWorkerIdentity) {
+    if (!worker)
+        return '';
+    const args = [
+        worker.setprivPath,
+        '--securebits=+noroot,+noroot_locked,+no_setuid_fixup,+no_setuid_fixup_locked',
+        '--bounding-set=-all',
+        '--inh-caps=-all',
+        '--ambient-caps=-all',
+        '--no-new-privs',
+    ];
+    if (enterWorkerIdentity) {
+        args.push(`--reuid=${worker.uid}`, `--regid=${worker.gid}`, '--clear-groups');
+    }
+    return `${quote(args)} `;
+}
+function privilegedWorkerCommand(userCommand, shellPath, worker) {
+    if (!worker)
+        return userCommand;
+    return `${privilegedSetprivPrefix(worker, true)}${quote([shellPath, '-c', userCommand])}`;
+}
+function buildSandboxCommand(httpSocketPath, socksSocketPath, userCommand, applySeccompPrefix, shell, socatPath, privilegedWorker) {
     // Default to bash for backward compatibility
     const shellPath = shell || 'bash';
     // Host filesystem is bind-mounted into the sandbox, so an explicit
     // socatPath resolves to the same binary inside bwrap.
-    const socat = quote([socatPath ?? 'socat']);
+    const infrastructurePrefix = privilegedSetprivPrefix(privilegedWorker, false);
+    const socat = `${infrastructurePrefix}${quote([socatPath ?? 'socat'])}`;
+    const internalProxyPorts = zerosInternalProxyPorts();
     const socatCommands = [
-        `${socat} TCP-LISTEN:3128,fork,reuseaddr UNIX-CONNECT:${httpSocketPath} >/dev/null 2>&1 &`,
-        `${socat} TCP-LISTEN:1080,fork,reuseaddr UNIX-CONNECT:${socksSocketPath} >/dev/null 2>&1 &`,
-        'trap "kill %1 %2 2>/dev/null; exit" EXIT',
+        `${socat} TCP-LISTEN:${internalProxyPorts.http},fork,reuseaddr UNIX-CONNECT:${httpSocketPath} >/dev/null 2>&1 &`,
+        'zsr_http_bridge_pid=$!',
+        `${socat} TCP-LISTEN:${internalProxyPorts.socks},fork,reuseaddr UNIX-CONNECT:${socksSocketPath} >/dev/null 2>&1 &`,
+        'zsr_socks_bridge_pid=$!',
+        'zsr_network_bridge_pid=',
+        ...zerosNetworkBridgeBootstrap(infrastructurePrefix),
+        `trap 'kill "$zsr_http_bridge_pid" "$zsr_socks_bridge_pid" \${zsr_network_bridge_pid:+"$zsr_network_bridge_pid"} 2>/dev/null; wait "$zsr_http_bridge_pid" "$zsr_socks_bridge_pid" \${zsr_network_bridge_pid:+"$zsr_network_bridge_pid"} 2>/dev/null' EXIT`,
     ];
     // apply-seccomp runs after socat so socat can still create Unix sockets.
+    const workload = privilegedWorkerCommand(userCommand, shellPath, privilegedWorker);
     if (applySeccompPrefix) {
-        const applySeccompCmd = applySeccompPrefix + quote([shellPath, '-c', userCommand]);
+        const applySeccompCmd = applySeccompPrefix + quote([shellPath, '-c', workload]);
         const innerScript = [...socatCommands, applySeccompCmd].join('\n');
         return `${shellPath} -c ${quote([innerScript])}`;
     }
     else {
-        const innerScript = [...socatCommands, `eval ${quote([userCommand])}`].join('\n');
+        const innerScript = [...socatCommands, `eval ${quote([workload])}`].join('\n');
         return `${shellPath} -c ${quote([innerScript])}`;
     }
 }
@@ -636,26 +734,19 @@ function resolveSymlinkDenyDest(normalizedPath) {
 function pushReadDenyDirMounts(args, normalizedPath, allowedWritePaths, readAllowPaths) {
     const denySep = normalizedPath === '/' ? '/' : normalizedPath + '/';
     args.push('--tmpfs', normalizedPath);
-    // tmpfs wiped any earlier write binds under this path — restore them.
-    for (const writePath of allowedWritePaths) {
-        if (writePath.startsWith(denySep) || writePath === normalizedPath) {
-            args.push('--bind', writePath, writePath);
-            logForDebugging(`[Sandbox Linux] Re-bound write path wiped by denyRead tmpfs: ${writePath}`);
-        }
-    }
     // Re-allow specific paths within the denied directory (allowRead overrides denyRead).
     // After mounting tmpfs over the denied dir, bind back the allowed subdirectories
-    // so they are readable again.
+    // so they are readable again. These must land before writable descendants:
+    // a later ro-bind of an allowRead ancestor would otherwise silently turn a
+    // previously restored allowWrite child read-only.
     for (const allowPath of readAllowPaths) {
         if (allowPath.startsWith(denySep) || allowPath === normalizedPath) {
             if (!fs.existsSync(allowPath)) {
                 logForDebugging(`[Sandbox Linux] Skipping non-existent read allow path: ${allowPath}`);
                 continue;
             }
-            // Skip only if a write path was re-bound just above AND covers
-            // allowPath. A write path that's an ancestor of the deny dir isn't
-            // re-bound (it wasn't wiped), so allowPath under it still needs
-            // its own ro-bind here.
+            // Skip when one writable mount covers the complete allow path; the
+            // writable bind emitted below already restores read access too.
             if (allowedWritePaths.some(w => (w.startsWith(denySep) || w === normalizedPath) &&
                 (allowPath === w || allowPath.startsWith(w + '/')))) {
                 continue;
@@ -665,11 +756,19 @@ function pushReadDenyDirMounts(args, normalizedPath, allowedWritePaths, readAllo
             logForDebugging(`[Sandbox Linux] Re-allowed read access within denied region: ${allowPath}`);
         }
     }
+    // tmpfs (and any allowRead ancestor binds above) wiped earlier write binds
+    // under this path — restore them last so nested writable state stays rw.
+    for (const writePath of allowedWritePaths) {
+        if (writePath.startsWith(denySep) || writePath === normalizedPath) {
+            args.push('--bind', writePath, writePath);
+            logForDebugging(`[Sandbox Linux] Re-bound write path wiped by denyRead tmpfs: ${writePath}`);
+        }
+    }
 }
 /**
  * Generate filesystem bind mount arguments for bwrap
  */
-async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, abortSignal) {
+async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, bindMounts, abortSignal) {
     const args = [];
     // fs already imported
     // Collect normalized allowed write paths. Populated in the writeConfig
@@ -859,7 +958,7 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
         // Deny writes within allowed paths (user-specified + mandatory denies)
         const denyPaths = [
             ...(writeConfig.denyWithinAllow || []),
-            ...(await linuxGetMandatoryDenyPaths(ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, abortSignal)),
+            ...(await linuxGetMandatoryDenyPaths(ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, disableMandatoryWriteProtection, abortSignal)),
         ];
         // Duplicate deny entries must be collapsed: a duplicate
         // --ro-bind /dev/null <dest> hits a char device on the second pass and
@@ -1249,6 +1348,31 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
         if (rawDest !== dest)
             emittedDenyWriteDests.push(rawDest);
     }
+    // Re-bind explicit writable islands after a broader denyWrite ro-bind.
+    // Only paths already present in allowOnly qualify, so this changes
+    // precedence rather than adding authority. Re-apply narrower deny binds
+    // that fall inside an exception immediately afterward.
+    const lateWritePaths = (writeConfig?.allowWithinDeny || [])
+        .map(candidate => normalizePathForSandbox(candidate).replace(/\/+$/, '') || '/')
+        .filter(candidate => allowedWritePaths.includes(candidate) && fs.existsSync(candidate));
+    for (const lateWritePath of lateWritePaths) {
+        args.push('--bind', lateWritePath, lateWritePath);
+        for (const deniedDest of emittedDenyWriteDests) {
+            if (deniedDest === lateWritePath || deniedDest.startsWith(lateWritePath + '/')) {
+                args.push('--ro-bind', deniedDest, deniedDest);
+            }
+        }
+        for (const [maskedFile, source] of maskedFiles) {
+            if (maskedFile === lateWritePath || maskedFile.startsWith(lateWritePath + '/')) {
+                if (resolveSymlinkDenyDest(maskedFile) === maskedFile)
+                    args.push('--ro-bind', source, maskedFile);
+            }
+        }
+        for (const tmpfsDir of tmpfsDirs) {
+            if (tmpfsDir === lateWritePath || tmpfsDir.startsWith(lateWritePath + '/'))
+                pushReadDenyDirMounts(args, tmpfsDir, allowedWritePaths, readAllowPaths);
+        }
+    }
     // The inverse stacking problem: a denyWrite ro-bind whose dest strictly
     // contains a read-denied dir re-exposes that dir's real contents (the bind
     // landed after the tmpfs). Re-apply the tmpfs on top, with the same write
@@ -1285,6 +1409,41 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
     if (maskedFileStoreDir !== undefined) {
         args.push('--ro-bind', maskedFileStoreDir, maskedFileStoreDir);
     }
+    // Trusted projections intentionally land last so an embedder can replace a
+    // denied control path with a private view (for example, a session-private
+    // Git directory at the checkout's ordinary .git path). SRT validates the
+    // mount shape defensively; authorization of each source/destination pair is
+    // the embedder's responsibility and must happen before this API is called.
+    for (const mount of bindMounts ?? []) {
+        if (!mount ||
+            typeof mount.source !== 'string' ||
+            typeof mount.destination !== 'string' ||
+            typeof mount.readOnly !== 'boolean' ||
+            !path.isAbsolute(mount.source) ||
+            !path.isAbsolute(mount.destination) ||
+            mount.source.includes('\0') ||
+            mount.destination.includes('\0')) {
+            throw new Error('Filesystem bind mounts require absolute NUL-free source and destination paths');
+        }
+        const source = path.normalize(mount.source);
+        const destination = path.normalize(mount.destination);
+        if (source === destination) {
+            throw new Error('Filesystem bind mount source and destination must differ');
+        }
+        const sourceStat = fs.lstatSync(source);
+        const destinationStat = fs.lstatSync(destination);
+        if (sourceStat.isSymbolicLink() || destinationStat.isSymbolicLink() ||
+            fs.realpathSync(source) !== source ||
+            fs.realpathSync(destination) !== destination) {
+            throw new Error('Filesystem bind mounts must use canonical physical paths');
+        }
+        const sameType = (sourceStat.isDirectory() && destinationStat.isDirectory()) ||
+            (sourceStat.isFile() && destinationStat.isFile());
+        if (!sameType) {
+            throw new Error('Filesystem bind mount source and destination must have matching regular file or directory types');
+        }
+        args.push(mount.readOnly ? '--ro-bind' : '--bind', source, destination);
+    }
     return args;
 }
 /**
@@ -1336,7 +1495,7 @@ async function generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds,
  * Dependencies are checked by checkLinuxDependencies() before enabling the sandbox.
  */
 export async function wrapCommandWithSandboxLinux(params) {
-    const { command, commandId, needsNetworkRestriction, httpSocketPath, socksSocketPath, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, readConfig, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, maskedFileStoreDir, enableWeakerNestedSandbox, allowAllUnixSockets, binShell, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, gitSafeDirectories, seccompConfig, bwrapPath, socatPath, observeSocketPath, abortSignal, } = params;
+    const { command, commandId, needsNetworkRestriction, httpSocketPath, socksSocketPath, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, readConfig, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, maskedFileStoreDir, enableWeakerNestedSandbox, linuxPrivilegedWorker, allowAllUnixSockets, binShell, ripgrepConfig = { command: 'rg' }, mandatoryDenySearchDepth = DEFAULT_MANDATORY_DENY_SEARCH_DEPTH, allowGitConfig = false, disableMandatoryWriteProtection = false, bindMounts, gitSafeDirectories, seccompConfig, bwrapPath, socatPath, observeSocketPath, abortSignal, } = params;
     // Determine if we have restrictions to apply
     // Read: denyOnly pattern - empty array means no restrictions
     // Write: allowOnly pattern - undefined means no restrictions, any config means restrictions
@@ -1351,9 +1510,29 @@ export async function wrapCommandWithSandboxLinux(params) {
         !hasReadRestrictions &&
         !hasWriteRestrictions &&
         !hasEnvRestrictions &&
-        !hasGitConfig) {
+        !hasGitConfig &&
+        !linuxPrivilegedWorker) {
         return command;
     }
+    if (linuxPrivilegedWorker) {
+        if (enableWeakerNestedSandbox) {
+            throw new Error('Privileged cloud-worker mode cannot use weaker nested sandboxing');
+        }
+        if (typeof process.geteuid !== 'function' || process.geteuid() !== 0) {
+            throw new Error('Privileged cloud-worker mode requires a root supervisor');
+        }
+        if (!Number.isInteger(linuxPrivilegedWorker.uid) || linuxPrivilegedWorker.uid <= 0 ||
+            !Number.isInteger(linuxPrivilegedWorker.gid) || linuxPrivilegedWorker.gid <= 0 ||
+            !path.isAbsolute(linuxPrivilegedWorker.setprivPath)) {
+            throw new Error('Invalid privileged cloud-worker identity');
+        }
+        const setprivStat = fs.lstatSync(linuxPrivilegedWorker.setprivPath);
+        if (!setprivStat.isFile() || setprivStat.isSymbolicLink() ||
+            fs.realpathSync(linuxPrivilegedWorker.setprivPath) !== linuxPrivilegedWorker.setprivPath ||
+            (setprivStat.mode & 0o022) !== 0) {
+            throw new Error('Privileged cloud-worker setpriv must be a canonical non-writable file');
+        }
+    }
     // Mark this sandbox invocation as active. cleanupBwrapMountPoints() will
     // defer file deletion until this (and every other concurrent) invocation
     // has been cleaned up. The matching decrement happens in
@@ -1376,6 +1555,9 @@ export async function wrapCommandWithSandboxLinux(params) {
             else {
                 logForDebugging('[Sandbox Linux] Applying seccomp filter for Unix socket blocking');
             }
+            if (linuxPrivilegedWorker && !applySeccompPrefix) {
+                throw new Error('Privileged cloud-worker mode requires seccomp when Unix sockets are blocked');
+            }
         }
         else {
             logForDebugging('[Sandbox Linux] Skipping seccomp filter - allowAllUnixSockets is enabled');
@@ -1454,10 +1636,11 @@ export async function wrapCommandWithSandboxLinux(params) {
                     bwrapArgs.push('--bind', socksSocketPath, socksSocketPath);
                 }
                 // Add proxy environment variables
-                // HTTP_PROXY points to the socat listener inside the sandbox (port 3128)
+                const internalProxyPorts = zerosInternalProxyPorts();
+                // HTTP_PROXY points to the per-launch socat listener inside the sandbox.
                 // which forwards to the Unix socket that bridges to the host's proxy server
-                const proxyEnv = generateProxyEnvVars(3128, // Internal HTTP listener port
-                1080, // Internal SOCKS listener port
+                const proxyEnv = generateProxyEnvVars(internalProxyPorts.http, // Internal HTTP listener port
+                internalProxyPorts.socks, // Internal SOCKS listener port
                 caCertPath, proxyAuthToken, writeConfig === undefined, encodeSandboxedCommand(commandId ?? command));
                 bwrapArgs.push(...proxyEnv.flatMap((env) => {
                     const firstEq = env.indexOf('=');
@@ -1477,8 +1660,18 @@ export async function wrapCommandWithSandboxLinux(params) {
             // If no sockets provided, network is completely blocked (--unshare-net without proxy)
         }
         // ========== FILESYSTEM RESTRICTIONS ==========
-        const fsArgs = await generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, abortSignal);
+        const fsArgs = await generateFilesystemArgs(readConfig, writeConfig, maskedFileBinds, maskedFileStoreDir, ripgrepConfig, mandatoryDenySearchDepth, allowGitConfig, disableMandatoryWriteProtection, bindMounts, abortSignal);
         bwrapArgs.push(...fsArgs);
+        // A broad denyRead (notably Zeros' AF_UNIX root isolation) can mount a
+        // tmpfs over the proxy sockets after the early network setup binds were
+        // emitted. Re-bind the two manager-owned endpoints last so remote
+        // network parity survives without reopening their parent /tmp tree.
+        if (needsNetworkRestriction && httpSocketPath && socksSocketPath) {
+            bwrapArgs.push('--bind', httpSocketPath, httpSocketPath);
+            if (socksSocketPath !== httpSocketPath) {
+                bwrapArgs.push('--bind', socksSocketPath, socksSocketPath);
+            }
+        }
         // Always bind /dev
         bwrapArgs.push('--dev', '/dev');
         // ========== PID NAMESPACE ISOLATION ==========
@@ -1489,7 +1682,22 @@ export async function wrapCommandWithSandboxLinux(params) {
         // outside the sandbox. But, --proc is not available when running in unprivileged docker containers
         // so we support running without it if explicitly requested.
         bwrapArgs.push('--unshare-pid');
-        if (!enableWeakerNestedSandbox) {
+        if (linuxPrivilegedWorker) {
+            // The trusted root supervisor performs only namespace/mount setup.
+            // The outer shell keeps SETUID/SETGID/SETPCAP just long enough for
+            // setpriv to lock securebits, empty every capability set and enter
+            // the dedicated worker identity. If apply-seccomp is active it
+            // additionally needs SYS_ADMIN to create its inner PID/mount
+            // namespace; the actual workload never receives that capability.
+            bwrapArgs.push('--unshare-ipc', '--unshare-uts', '--unshare-cgroup',
+                '--cap-drop', 'ALL', '--cap-add', 'CAP_SETUID',
+                '--cap-add', 'CAP_SETGID', '--cap-add', 'CAP_SETPCAP');
+            if (applySeccompPrefix) {
+                bwrapArgs.push('--cap-add', 'CAP_SYS_ADMIN');
+            }
+            bwrapArgs.push('--proc', '/proc');
+        }
+        else if (!enableWeakerNestedSandbox) {
             // Mount fresh /proc if PID namespace is isolated (secure mode).
             // --unshare-user + --cap-drop ALL: bwrap only auto-creates a userns
             // when EUID != 0, so a root parent would otherwise leave the sandboxed
@@ -1529,15 +1737,16 @@ export async function wrapCommandWithSandboxLinux(params) {
         // so socat starts before seccomp is applied. Otherwise invoke apply-seccomp
         // directly if we have a binary.
         if (needsNetworkRestriction && httpSocketPath && socksSocketPath) {
-            const sandboxCommand = buildSandboxCommand(httpSocketPath, socksSocketPath, command, applySeccompPrefix, shell, socatPath);
+            const sandboxCommand = buildSandboxCommand(httpSocketPath, socksSocketPath, command, applySeccompPrefix, shell, socatPath, linuxPrivilegedWorker);
             bwrapArgs.push(sandboxCommand);
         }
         else if (applySeccompPrefix) {
-            const applySeccompCmd = applySeccompPrefix + quote([shell, '-c', command]);
+            const workload = privilegedWorkerCommand(command, shell, linuxPrivilegedWorker);
+            const applySeccompCmd = applySeccompPrefix + quote([shell, '-c', workload]);
             bwrapArgs.push(applySeccompCmd);
         }
         else {
-            bwrapArgs.push(command);
+            bwrapArgs.push(privilegedWorkerCommand(command, shell, linuxPrivilegedWorker));
         }
         const wrappedCommand = quote([bwrapPath ?? 'bwrap', ...bwrapArgs]);
         const restrictions = [];
@@ -1549,6 +1758,8 @@ export async function wrapCommandWithSandboxLinux(params) {
             restrictions.push('env');
         if (applySeccompPrefix)
             restrictions.push('seccomp(unix-block)');
+        if (linuxPrivilegedWorker)
+            restrictions.push('privileged-worker-drop');
         logForDebugging(`[Sandbox Linux] Wrapped command with bwrap (${restrictions.join(', ')} restrictions)`);
         return wrappedCommand;
     }
diff --git a/dist/sandbox/macos-sandbox-utils.d.ts b/dist/sandbox/macos-sandbox-utils.d.ts
index 819c56a3f6bfb89fbacc008e6b0fcf6bf40901ed..abd3a0b53b22346569c0ca16cf047840dd1a31ee 100644
--- a/dist/sandbox/macos-sandbox-utils.d.ts
+++ b/dist/sandbox/macos-sandbox-utils.d.ts
@@ -21,6 +21,12 @@ export interface MacOSSandboxParams {
     allowUnixSockets?: string[];
     allowAllUnixSockets?: boolean;
     allowLocalBinding?: boolean;
+    /** Exact loopback TCP destination ports granted to the sandbox. */
+    allowedLocalPorts?: number[];
+    /** Exact local TCP bind/inbound ports granted to the sandbox. */
+    allowedBindPorts?: number[];
+    /** Exact loopback TCP ports denied after the broad local-binding allow. */
+    deniedLocalPorts?: number[];
     allowMachLookup?: string[];
     readConfig: FsReadRestrictionConfig | undefined;
     writeConfig: FsWriteRestrictionConfig | undefined;
@@ -40,6 +46,7 @@ export interface MacOSSandboxParams {
     ignoreViolations?: IgnoreViolationsConfig | undefined;
     allowPty?: boolean;
     allowGitConfig?: boolean;
+    disableMandatoryWriteProtection?: boolean;
     /**
      * Directories to emit as `safe.directory` via `GIT_CONFIG_*` env
      * vars — see {@link buildPosixGitSafeDirEnv}.
@@ -53,7 +60,7 @@ export interface MacOSSandboxParams {
  * Get mandatory deny patterns as glob patterns (no filesystem scanning).
  * macOS sandbox profile supports regex/glob matching directly via globToRegex().
  */
-export declare function macGetMandatoryDenyPatterns(allowGitConfig?: boolean): string[];
+export declare function macGetMandatoryDenyPatterns(allowGitConfig?: boolean, disableMandatoryWriteProtection?: boolean): string[];
 export interface SandboxViolationEvent {
     line: string;
     command?: string;
diff --git a/dist/sandbox/macos-sandbox-utils.js b/dist/sandbox/macos-sandbox-utils.js
index c3b6bd388235dccb965697cc07035d08aa9666eb..5259afe31bc2431256a0cb27cc22d4a349b63b60 100644
--- a/dist/sandbox/macos-sandbox-utils.js
+++ b/dist/sandbox/macos-sandbox-utils.js
@@ -9,7 +9,9 @@ import { shouldIgnoreViolation } from './sandbox-violation-store.js';
  * Get mandatory deny patterns as glob patterns (no filesystem scanning).
  * macOS sandbox profile supports regex/glob matching directly via globToRegex().
  */
-export function macGetMandatoryDenyPatterns(allowGitConfig = false) {
+export function macGetMandatoryDenyPatterns(allowGitConfig = false, disableMandatoryWriteProtection = false) {
+    if (disableMandatoryWriteProtection)
+        return [];
     const cwd = process.cwd();
     const denyPaths = [];
     // Dangerous files - static paths in CWD + glob patterns for subtree
@@ -418,7 +420,7 @@ function generateReadRules(resolved, logTag) {
 /**
  * Generate filesystem write rules for sandbox profile
  */
-function generateWriteRules(config, logTag, allowGitConfig = false) {
+function generateWriteRules(config, logTag, allowGitConfig = false, disableMandatoryWriteProtection = false) {
     if (!config) {
         return [`(allow file-write*)`];
     }
@@ -432,7 +434,7 @@ function generateWriteRules(config, logTag, allowGitConfig = false) {
     // Combine user-specified and mandatory deny patterns (no ripgrep needed on macOS)
     const denyPaths = [
         ...(config.denyWithinAllow || []),
-        ...macGetMandatoryDenyPatterns(allowGitConfig),
+        ...macGetMandatoryDenyPatterns(allowGitConfig, disableMandatoryWriteProtection),
     ];
     const denyFilters = new Set();
     for (const pathPattern of denyPaths) {
@@ -441,12 +443,36 @@ function generateWriteRules(config, logTag, allowGitConfig = false) {
     rules.push(...renderRule('deny', ['file-write*'], denyFilters, logTag));
     // Block file movement to prevent bypass via mv/rename
     rules.push(...generateMoveBlockingRules(denyPaths, logTag));
+    // Re-open only explicit writable islands inside a broader denied root.
+    // This is required when an embedder keeps private per-session HOME/temp/Git
+    // below its otherwise-inaccessible engine state. Seatbelt is last-match
+    // winning, and specific unlink/create denies can beat file-write*, so emit
+    // all three operation classes deliberately.
+    const exceptions = (config.allowWithinDeny || []).map(pathPattern => normalizePathForSandbox(pathPattern));
+    const exceptionFilters = new Set(exceptions.map(pathFilter));
+    rules.push(...renderRule('allow', ['file-write*', 'file-write-unlink', 'file-write-create'], exceptionFilters, logTag));
+    // An exception never overrides a more-specific deny nested inside it.
+    // Glob denies are conservatively replayed because their matched descendants
+    // cannot be classified lexically here.
+    const nestedDenies = denyPaths.filter(pathPattern => {
+        if (containsGlobChars(pathPattern))
+            return true;
+        const denied = normalizePathForSandbox(pathPattern);
+        return exceptions.some(exception => {
+            const relative = path.relative(exception, denied);
+            return relative === '' ||
+                (relative !== '..' && !relative.startsWith(`..${path.sep}`) && !path.isAbsolute(relative));
+        });
+    });
+    const nestedFilters = new Set(nestedDenies.map(pathPattern => denyPathFilter(normalizePathForSandbox(pathPattern))));
+    rules.push(...renderRule('deny', ['file-write*'], nestedFilters, logTag));
+    rules.push(...generateMoveBlockingRules(nestedDenies, logTag));
     return rules;
 }
 /**
  * Generate complete sandbox profile
  */
-function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksProxyPort, needsNetworkRestriction, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, allowPty, allowGitConfig = false, enableWeakerNetworkIsolation = false, allowAppleEvents = false, logTag, }) {
+function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksProxyPort, needsNetworkRestriction, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowedLocalPorts, allowedBindPorts, deniedLocalPorts, allowMachLookup, allowPty, allowGitConfig = false, disableMandatoryWriteProtection = false, enableWeakerNetworkIsolation = false, allowAppleEvents = false, logTag, }) {
     const profile = [
         '(version 1)',
         `(deny default (with message "${logTag}"))`,
@@ -478,7 +504,9 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
         '  (global-name "com.apple.system.opendirectoryd.libinfo")',
         '  (global-name "com.apple.system.opendirectoryd.membership")',
         '  (global-name "com.apple.bsd.dirhelper")',
-        '  (global-name "com.apple.securityd.xpc")',
+        ...(process.env.ZEROS_ZSR_DENY_SECURITY_SERVER === '1'
+            ? []
+            : ['  (global-name "com.apple.securityd.xpc")']),
         '  (global-name "com.apple.coreservices.launchservicesd")',
         ')',
         '',
@@ -599,8 +627,12 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
         '; Distributed notifications',
         '(allow distributed-notification-post)',
         '',
-        '; Specific mach-lookup permissions for security operations',
-        '(allow mach-lookup (global-name "com.apple.SecurityServer"))',
+        ...(process.env.ZEROS_ZSR_DENY_SECURITY_SERVER === '1'
+            ? ['; SecurityServer intentionally unavailable to this embedding']
+            : [
+                '; Specific mach-lookup permissions for security operations',
+                '(allow mach-lookup (global-name "com.apple.SecurityServer"))',
+            ]),
         '',
         '; File I/O on device files',
         '(allow file-ioctl (literal "/dev/null"))',
@@ -647,7 +679,18 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
         if (allowLocalBinding) {
             profile.push('(allow network-bind (local ip "*:*"))');
             profile.push('(allow network-inbound (local ip "*:*"))');
-            profile.push('(allow network-outbound (remote ip "localhost:*"))');
+        }
+        // Immutable exact bind/inbound capabilities for embedders that map
+        // dynamic development servers into a pre-admitted per-session pool.
+        for (const port of allowedBindPorts ?? []) {
+            profile.push(`(allow network-bind (local ip "*:${port}"))`);
+            profile.push(`(allow network-inbound (local ip "*:${port}"))`);
+        }
+        // Local service access is an exact capability, independent from the
+        // broad ability to bind a development server. Without this list the
+        // profile grants no direct loopback egress beyond its own proxies.
+        for (const port of allowedLocalPorts ?? []) {
+            profile.push(`(allow network-outbound (remote ip "localhost:${port}"))`);
         }
         // Unix domain sockets for local IPC (SSH agent, Docker, Gradle, etc.)
         // Three separate operations must be allowed:
@@ -687,6 +730,13 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
             profile.push(`(allow network-inbound (local ip "localhost:${socksProxyPort}"))`);
             profile.push(`(allow network-outbound (remote ip "localhost:${socksProxyPort}"))`);
         }
+        // Local binding is required for dev servers, but its broad localhost
+        // outbound allowance must not reopen an embedding application's own
+        // control ports. Callers express those ports through exact loopback
+        // deniedDomains entries; deny rules come last and win over the allow.
+        for (const port of deniedLocalPorts ?? []) {
+            profile.push(`(deny network-outbound (remote ip "localhost:${port}"))`);
+        }
     }
     profile.push('');
     // Read rules
@@ -700,7 +750,7 @@ function generateSandboxProfile({ readConfig, writeConfig, httpProxyPort, socksP
     profile.push('');
     // Write rules
     profile.push('; File write');
-    profile.push(...generateWriteRules(writeConfig, logTag, allowGitConfig));
+    profile.push(...generateWriteRules(writeConfig, logTag, allowGitConfig, disableMandatoryWriteProtection));
     // Read-denied paths inside write roots: the read section's unlink/create
     // re-allow for write roots (and the write section's file-write* allows)
     // must not leave them renameable. Last in the profile so nothing above
@@ -739,7 +789,7 @@ function escapePath(pathStr) {
  * Wrap command with macOS sandbox
  */
 export function wrapCommandWithSandboxMacOS(params) {
-    const { command, commandId, needsNetworkRestriction, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowMachLookup, readConfig: readConfigIn, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, allowPty, allowGitConfig = false, gitSafeDirectories, enableWeakerNetworkIsolation = false, allowAppleEvents = false, binShell, } = params;
+    const { command, commandId, needsNetworkRestriction, httpProxyPort, socksProxyPort, proxyAuthToken, caCertPath, allowUnixSockets, allowAllUnixSockets, allowLocalBinding, allowedLocalPorts, allowedBindPorts, deniedLocalPorts, allowMachLookup, readConfig: readConfigIn, writeConfig, unsetEnvVars, setEnvVars, maskedFileBinds, allowPty, allowGitConfig = false, disableMandatoryWriteProtection = false, gitSafeDirectories, enableWeakerNetworkIsolation = false, allowAppleEvents = false, binShell, } = params;
     // SBPL cannot redirect a read to different bytes, so whole-file masking
     // degrades to read-deny on macOS: the sandboxed process gets EPERM
     // instead of the sentinel. The DYLD interposer (a later step) lifts
@@ -787,9 +837,13 @@ export function wrapCommandWithSandboxMacOS(params) {
         allowUnixSockets,
         allowAllUnixSockets,
         allowLocalBinding,
+        allowedLocalPorts,
+        allowedBindPorts,
+        deniedLocalPorts,
         allowMachLookup,
         allowPty,
         allowGitConfig,
+        disableMandatoryWriteProtection,
         enableWeakerNetworkIsolation,
         allowAppleEvents,
         logTag,
@@ -805,7 +859,8 @@ export function wrapCommandWithSandboxMacOS(params) {
     // Seatbelt filter. The flag is appended after any inherited
     // JAVA_TOOL_OPTIONS unless that var is on the credential-deny list, in
     // which case the inherited value is dropped so the deny holds.
-    if (allowLocalBinding && needsNetworkRestriction) {
+    if (((allowedLocalPorts?.length ?? 0) > 0 ||
+        (allowedBindPorts?.length ?? 0) > 0) && needsNetworkRestriction) {
         const flag = '-Djava.net.preferIPv4Stack=true';
         const denied = (unsetEnvVars ?? []).includes('JAVA_TOOL_OPTIONS');
         const inherited = denied ? '' : (process.env.JAVA_TOOL_OPTIONS ?? '');
diff --git a/dist/sandbox/mitm-ca.js b/dist/sandbox/mitm-ca.js
index 1be790f7c84d2063e463482072970f078fee9301..790086553a822645eafa87b63c2e9b6df00b26fd 100644
--- a/dist/sandbox/mitm-ca.js
+++ b/dist/sandbox/mitm-ca.js
@@ -13,7 +13,7 @@ import { mkdtempSync, readFileSync, writeFileSync } from 'node:fs';
 import { rm } from 'node:fs/promises';
 import { tmpdir } from 'node:os';
 import { join, dirname } from 'node:path';
-import { rootCertificates } from 'node:tls';
+import * as tls from 'node:tls';
 import { logForDebugging } from '../utils/debug.js';
 const { asn1, pki, md, random, util } = forge;
 // node-forge exports pki.getTBSCertificate at runtime; @types/node-forge omits it.
@@ -138,7 +138,19 @@ export async function disposeMitmCA(ca) {
  * semantics as NODE_EXTRA_CA_CERTS.
  */
 function writeTrustBundle(dir, caCertPem, extraCaCertPaths) {
-    const parts = [caCertPem.trim(), ...rootCertificates];
+    // Node >=22 can expose its effective default set, including OS roots when
+    // NODE_USE_SYSTEM_CA/--use-system-ca is active. Fall back to the bundled
+    // Mozilla roots on older supported Node releases.
+    let defaultRoots = tls.rootCertificates;
+    try {
+        if (typeof tls.getCACertificates === 'function') {
+            defaultRoots = tls.getCACertificates('default');
+        }
+    }
+    catch {
+        // Keep the stable bundled roots if the host trust-store lookup fails.
+    }
+    const parts = [caCertPem.trim(), ...defaultRoots];
     // Honour extra roots the parent process trusts (e.g. a corporate CA).
     const extra = process.env.NODE_EXTRA_CA_CERTS;
     if (extra) {
diff --git a/dist/sandbox/request-filter.d.ts b/dist/sandbox/request-filter.d.ts
index 58e31b688d9c9007e32b0f9cc54b678ceb51ee61..3534ca83babf2609aa424ae65b6665f6b16bf569 100644
--- a/dist/sandbox/request-filter.d.ts
+++ b/dist/sandbox/request-filter.d.ts
@@ -18,6 +18,24 @@ export type RequestDecision = {
      */
     reason?: string;
 };
+export interface FilterRequestContext {
+    /** True when the parsed HTTP message can carry bytes after its headers. */
+    readonly hasBody: boolean;
+    /** The callback-side tee branch. This remains non-null for GET/HEAD bodies,
+     * which the Fetch Request constructor cannot represent. */
+    readonly body: ReadableStream<Uint8Array> | null;
+    readonly httpVersion: string;
+    /** HTTP/2 extended-CONNECT protocol (for example `websocket`). Omitted
+     * for ordinary HTTP requests. DATA after this handshake is opaque and is
+     * intentionally not exposed as an HTTP request body. */
+    readonly protocol?: string;
+}
+export interface FilterRequestOptions {
+    /** Treat DATA as an opaque post-handshake protocol stream. */
+    readonly opaqueBody?: boolean;
+    /** Validated HTTP/2 `:protocol` value for extended CONNECT. */
+    readonly protocol?: string;
+}
 /**
  * Called once per HTTP request that the proxy parses.
  *
@@ -25,10 +43,12 @@ export type RequestDecision = {
  *   `request.body` stream (one branch of a tee — reading it does not consume
  *   the bytes that get forwarded upstream). `request.signal` aborts when the
  *   client disconnects.
+ * - `context.body` exposes the same callback-side branch and remains non-null
+ *   for GET/HEAD content, which Fetch cannot attach to `request.body`.
  * - **Throwing or rejecting denies the request.** This is the failure
  *   contract for a security boundary: a buggy policy fails closed.
  */
-export type FilterRequestCallback = (request: Request) => Promise<RequestDecision>;
+export type FilterRequestCallback = (request: Request, context: FilterRequestContext) => Promise<RequestDecision>;
 /**
  * Mutate the headers that will be sent upstream, in place.
  *
@@ -39,8 +59,17 @@ export type FilterRequestCallback = (request: Request) => Promise<RequestDecisio
  * spelling the allowlist evaluated) — never the client-supplied Host
  * header, which is spoofable.
  */
-export type MutateForwardedHeaders = (headers: IncomingHttpHeaders, destHost: string) => void;
+export type MutateForwardedHeaders = (headers: IncomingHttpHeaders, destHost: string, destPort: number) => void;
 export declare const BODYLESS_METHODS: Set<string>;
+/**
+ * Whether the parsed request can carry bytes after its headers. HTTP/2 uses
+ * END_STREAM rather than HTTP/1 framing headers for this decision.
+ */
+export declare function requestDeclaresBody(req: IncomingMessage & {
+    readonly stream?: {
+        readonly endAfterHeaders?: boolean;
+    };
+}): boolean;
 /**
  * User-facing reason when a filterRequest denial doesn't supply one. This
  * text reaches the sandboxed client (403 body) and the model
@@ -60,7 +89,7 @@ export declare const DEFAULT_DENY_REASON = "denied by sandbox policy";
  * branch, we cancel it after the decision so the tee does not buffer the
  * entire upload.
  */
-export declare function decideAndRespond(filterRequest: FilterRequestCallback, req: IncomingMessage, res: ServerResponse, url: string, signal: AbortSignal, onDeny?: (method: string, url: string, reason: string) => void): Promise<Readable | null>;
+export declare function decideAndRespond(filterRequest: FilterRequestCallback, req: IncomingMessage, res: ServerResponse, url: string, signal: AbortSignal, onDeny?: (method: string, url: string, reason: string) => void, options?: FilterRequestOptions): Promise<Readable | null>;
 /**
  * Write the proxy's standard policy-denial response: 403 with the reason
  * in the body, so the sandboxed client can tell a policy block from a
diff --git a/dist/sandbox/request-filter.js b/dist/sandbox/request-filter.js
index bd397f0ed189add83847d1ecd5e769833a1f3e4a..332bd563a9375abd746bef05b25bf0f71e951347 100644
--- a/dist/sandbox/request-filter.js
+++ b/dist/sandbox/request-filter.js
@@ -10,6 +10,19 @@
 import { PassThrough, Readable } from 'node:stream';
 import { logForDebugging } from '../utils/debug.js';
 export const BODYLESS_METHODS = new Set(['GET', 'HEAD', 'OPTIONS']);
+const FETCH_FORBIDDEN_METHODS = new Set(['CONNECT', 'TRACE', 'TRACK']);
+/**
+ * Whether the parsed request can carry bytes after its headers.
+ *
+ * HTTP/1 declares this with framing headers. HTTP/2 deliberately has no
+ * transfer-encoding and does not require content-length, so the authoritative
+ * signal is the END_STREAM flag exposed as `stream.endAfterHeaders`.
+ */
+export function requestDeclaresBody(req) {
+    if (Boolean(req.headers['content-length'] || req.headers['transfer-encoding']))
+        return true;
+    return req.httpVersionMajor === 2 && req.stream?.endAfterHeaders === false;
+}
 /**
  * User-facing reason when a filterRequest denial doesn't supply one. This
  * text reaches the sandboxed client (403 body) and the model
@@ -42,9 +55,10 @@ function destroyAfterResponse(req, res) {
  * branch, we cancel it after the decision so the tee does not buffer the
  * entire upload.
  */
-export async function decideAndRespond(filterRequest, req, res, url, signal, onDeny) {
+export async function decideAndRespond(filterRequest, req, res, url, signal, onDeny, options = {}) {
     const method = req.method ?? 'GET';
     let forCallback;
+    let callbackBranch;
     let forUpstream = req;
     // Gate on a declared body, not just the method: a GET/HEAD/OPTIONS with
     // Content-Length or Transfer-Encoding is legal HTTP and its body must go
@@ -54,9 +68,13 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
     // request upstream: a request-smuggling primitive.
     // Truthiness, not presence: an empty Content-Length/Transfer-Encoding
     // value declares nothing and must not trigger teeing or re-framing.
-    const declaresBody = Boolean(req.headers['content-length'] || req.headers['transfer-encoding']);
-    const bodylessMethod = BODYLESS_METHODS.has(method);
-    if (!bodylessMethod || declaresBody) {
+    // Extended CONNECT DATA is a protocol byte stream, not an HTTP message
+    // body. Policy evaluates the authenticated handshake headers only and the
+    // caller relays subsequent bytes opaquely (the same contract as an HTTP/1
+    // Upgrade). Never tee or buffer that stream here.
+    const declaresBody = options.opaqueBody ? false : requestDeclaresBody(req);
+    const fetchForbidsBody = method === 'GET' || method === 'HEAD';
+    if (!options.opaqueBody && (!BODYLESS_METHODS.has(method) || declaresBody)) {
         // Never hand toWeb a stream that can error: when its source errors,
         // the toWeb/tee/fromWeb bridge leaks the error as internal promise
         // rejections (and, under Bun, uncaught exceptions) that no userland
@@ -72,7 +90,13 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
         req.pipe(shim);
         const web = Readable.toWeb(shim);
         const [a, b] = web.tee();
-        forCallback = a;
+        // Keep an engine-owned Node wrapper around the callback branch. Once
+        // the callback returns we can destroy this wrapper even if buggy policy
+        // code locked its Web reader, preventing the unused tee branch from
+        // buffering the rest of an arbitrarily large upload in host memory.
+        callbackBranch = Readable.fromWeb(a);
+        callbackBranch.on('error', () => { });
+        forCallback = Readable.toWeb(callbackBranch);
         forUpstream = Readable.fromWeb(b);
         const upstreamBranch = forUpstream;
         // The caller only wires its own 'error' handler after this function
@@ -81,6 +105,7 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
         upstreamBranch.on('error', () => { });
         req.on('error', err => {
             shim.end();
+            callbackBranch?.destroy(err);
             upstreamBranch.destroy(err);
         });
     }
@@ -89,22 +114,34 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
         // Fetch-spec Requests reject a body on GET/HEAD (Node throws; Bun is
         // lenient) — a GET with a declared body is still teed above so the
         // upstream leg stays framed, but the callback sees a bodyless Request.
-        const callbackBody = forCallback && !bodylessMethod
+        const callbackBody = forCallback && !fetchForbidsBody
             ? { body: forCallback, duplex: 'half' }
             : {};
+        // Fetch forbids CONNECT/TRACE/TRACK even though an HTTP forward proxy
+        // must be able to policy-check them. Build with a harmless temporary
+        // method and shadow the configurable prototype accessor so callbacks
+        // still observe the exact wire method.
+        const fetchMethod = FETCH_FORBIDDEN_METHODS.has(method) ? 'POST' : method;
         webReq = new Request(url, {
-            method,
+            method: fetchMethod,
             headers: incomingHeaders(req),
             signal,
             ...callbackBody,
         });
+        if (fetchMethod !== method) {
+            Object.defineProperty(webReq, 'method', {
+                configurable: true,
+                enumerable: true,
+                value: method,
+            });
+        }
     }
     catch (err) {
         // Malformed URL/headers from the client — deny rather than crash.
         const reason = `malformed request: ${err.message}`;
         onDeny?.(method, url, reason);
         deny(res, { action: 'deny', reason });
-        forCallback?.cancel().catch(() => { });
+        callbackBranch?.destroy();
         forUpstream.destroy();
         // The shim breaks the old fromWeb→tee→toWeb cancel cascade that used
         // to destroy req; without this a denied client keeps uploading into a
@@ -115,7 +152,15 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
     }
     let decision;
     try {
-        decision = await filterRequest(webReq);
+        decision = await filterRequest(webReq, {
+            hasBody: declaresBody,
+            // Fetch prohibits GET/HEAD Request bodies. Preserve a real
+            // standards-based Request while exposing the tee branch here so a
+            // policy can still inspect those legal HTTP/2 DATA bytes.
+            body: forCallback ?? null,
+            httpVersion: req.httpVersion,
+            ...(options.protocol ? { protocol: options.protocol } : {}),
+        });
     }
     catch (err) {
         decision = {
@@ -123,14 +168,10 @@ export async function decideAndRespond(filterRequest, req, res, url, signal, onD
             reason: `sandbox policy check failed: ${err.message}`,
         };
     }
-    // If the callback didn't read its branch, cancel it so tee() stops
-    // buffering bytes nobody will consume. If it did, the tee already buffered
-    // whatever was read; the upstream branch sees the same bytes.
-    if (forCallback && !webReq.bodyUsed) {
-        // cancel() rejects with the stream's stored error if it already
-        // failed; that rejection must not escape.
-        forCallback.cancel().catch(() => { });
-    }
+    // Policy inspection is scoped to the callback invocation. Tear its branch
+    // down unconditionally after the decision; the independently teed upstream
+    // branch retains every byte and a retained/locked reader cannot pin memory.
+    callbackBranch?.destroy();
     if (decision.action === 'allow') {
         logForDebugging(`[request-filter] allow ${method} ${url}`);
         return forUpstream;
@@ -166,7 +207,12 @@ export function respondDenied(res, reason) {
 function incomingHeaders(req) {
     const h = new Headers();
     for (const [k, v] of Object.entries(req.headers)) {
-        if (v === undefined)
+        // Http2ServerRequest.headers includes :method/:path/:authority/:scheme.
+        // Web Headers rejects colon-prefixed names, and the policy URL/method
+        // already come from the verified CONNECT target plus public request
+        // properties. Never let pseudo-headers turn every h2 request into a
+        // synthetic "malformed request" denial.
+        if (k.startsWith(':') || v === undefined)
             continue;
         if (Array.isArray(v)) {
             for (const vv of v)
diff --git a/dist/sandbox/sandbox-config.d.ts b/dist/sandbox/sandbox-config.d.ts
index a5d9fe988f1f5c1cc1e48aef859c7276be37bb12..b8cb1de08ab3ce9d12756b464727b776c847fc32 100644
--- a/dist/sandbox/sandbox-config.d.ts
+++ b/dist/sandbox/sandbox-config.d.ts
@@ -160,6 +160,7 @@ export declare const CredentialFileConfigSchema: z.ZodObject<{
     maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     maskDuplicates: z.ZodOptional<z.ZodBoolean>;
     injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+    allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
 }, "strip", z.ZodTypeAny, {
     mode: "deny" | "mask";
     path: string;
@@ -169,6 +170,7 @@ export declare const CredentialFileConfigSchema: z.ZodObject<{
     maskClaims?: string[] | undefined;
     maskDuplicates?: boolean | undefined;
     injectHosts?: string[] | undefined;
+    allowPlaintextInject?: boolean | undefined;
 }, {
     mode: "deny" | "mask";
     path: string;
@@ -178,6 +180,7 @@ export declare const CredentialFileConfigSchema: z.ZodObject<{
     maskClaims?: string[] | undefined;
     maskDuplicates?: boolean | undefined;
     injectHosts?: string[] | undefined;
+    allowPlaintextInject?: boolean | undefined;
 }>;
 /**
  * Schema for a single credential environment variable entry.
@@ -271,6 +274,7 @@ export declare const CredentialEnvVarConfigSchema: z.ZodObject<{
      */
     maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+    allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
 }, "strip", z.ZodTypeAny, {
     mode: "deny" | "mask";
     name: string;
@@ -279,6 +283,7 @@ export declare const CredentialEnvVarConfigSchema: z.ZodObject<{
     decode?: "jwt" | undefined;
     maskClaims?: string[] | undefined;
     injectHosts?: string[] | undefined;
+    allowPlaintextInject?: boolean | undefined;
 }, {
     mode: "deny" | "mask";
     name: string;
@@ -287,6 +292,7 @@ export declare const CredentialEnvVarConfigSchema: z.ZodObject<{
     decode?: "jwt" | undefined;
     maskClaims?: string[] | undefined;
     injectHosts?: string[] | undefined;
+    allowPlaintextInject?: boolean | undefined;
 }>;
 /**
  * Explicit grouping of masked env vars into an AWS credential pair for
@@ -419,6 +425,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         maskDuplicates: z.ZodOptional<z.ZodBoolean>;
         injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+        allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
     }, "strip", z.ZodTypeAny, {
         mode: "deny" | "mask";
         path: string;
@@ -428,6 +435,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         maskClaims?: string[] | undefined;
         maskDuplicates?: boolean | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }, {
         mode: "deny" | "mask";
         path: string;
@@ -437,6 +445,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         maskClaims?: string[] | undefined;
         maskDuplicates?: boolean | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }>, "many">>;
     envVars: z.ZodOptional<z.ZodArray<z.ZodObject<{
         name: z.ZodString;
@@ -483,6 +492,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
          */
         maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+        allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
     }, "strip", z.ZodTypeAny, {
         mode: "deny" | "mask";
         name: string;
@@ -491,6 +501,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         decode?: "jwt" | undefined;
         maskClaims?: string[] | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }, {
         mode: "deny" | "mask";
         name: string;
@@ -499,6 +510,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         decode?: "jwt" | undefined;
         maskClaims?: string[] | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }>, "many">>;
     allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
     awsPairs: z.ZodOptional<z.ZodArray<z.ZodObject<{
@@ -537,6 +549,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         maskClaims?: string[] | undefined;
         maskDuplicates?: boolean | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }[] | undefined;
     envVars?: {
         mode: "deny" | "mask";
@@ -546,6 +559,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         decode?: "jwt" | undefined;
         maskClaims?: string[] | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }[] | undefined;
     allowPlaintextInject?: boolean | undefined;
     awsPairs?: {
@@ -568,6 +582,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         maskClaims?: string[] | undefined;
         maskDuplicates?: boolean | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }[] | undefined;
     envVars?: {
         mode: "deny" | "mask";
@@ -577,6 +592,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
         decode?: "jwt" | undefined;
         maskClaims?: string[] | undefined;
         injectHosts?: string[] | undefined;
+        allowPlaintextInject?: boolean | undefined;
     }[] | undefined;
     allowPlaintextInject?: boolean | undefined;
     awsPairs?: {
@@ -594,6 +610,7 @@ export declare const CredentialsConfigSchema: z.ZodObject<{
  * Network configuration schema for validation
  */
 export declare const NetworkConfigSchema: z.ZodObject<{
+    disabled: z.ZodOptional<z.ZodBoolean>;
     allowedDomains: z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">;
     deniedDomains: z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">;
     deniedDomainReasons: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodString>>;
@@ -601,6 +618,8 @@ export declare const NetworkConfigSchema: z.ZodObject<{
     allowUnixSockets: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     allowAllUnixSockets: z.ZodOptional<z.ZodBoolean>;
     allowLocalBinding: z.ZodOptional<z.ZodBoolean>;
+    allowedLocalPorts: z.ZodOptional<z.ZodArray<z.ZodNumber, "many">>;
+    allowedBindPorts: z.ZodOptional<z.ZodArray<z.ZodNumber, "many">>;
     allowMachLookup: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
     httpProxyPort: z.ZodOptional<z.ZodNumber>;
     socksProxyPort: z.ZodOptional<z.ZodNumber>;
@@ -619,26 +638,31 @@ export declare const NetworkConfigSchema: z.ZodObject<{
         caCertPath: z.ZodOptional<z.ZodString>;
         caKeyPath: z.ZodOptional<z.ZodString>;
         excludeDomains: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+        includeDomains: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
         extraCaCertPaths: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     }, "strip", z.ZodTypeAny, {
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     }, {
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     }>, {
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     }, {
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     }>>;
     parentProxy: z.ZodOptional<z.ZodObject<{
@@ -655,6 +679,7 @@ export declare const NetworkConfigSchema: z.ZodObject<{
         noProxy?: string | undefined;
     }>>;
 }, "strip", z.ZodTypeAny, {
+    disabled?: boolean | undefined;
     allowedDomains: string[];
     deniedDomains: string[];
     deniedDomainReasons?: Record<string, string> | undefined;
@@ -662,6 +687,8 @@ export declare const NetworkConfigSchema: z.ZodObject<{
     allowUnixSockets?: string[] | undefined;
     allowAllUnixSockets?: boolean | undefined;
     allowLocalBinding?: boolean | undefined;
+    allowedLocalPorts?: number[] | undefined;
+    allowedBindPorts?: number[] | undefined;
     allowMachLookup?: string[] | undefined;
     httpProxyPort?: number | undefined;
     socksProxyPort?: number | undefined;
@@ -674,6 +701,7 @@ export declare const NetworkConfigSchema: z.ZodObject<{
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     } | undefined;
     parentProxy?: {
@@ -682,6 +710,7 @@ export declare const NetworkConfigSchema: z.ZodObject<{
         noProxy?: string | undefined;
     } | undefined;
 }, {
+    disabled?: boolean | undefined;
     allowedDomains: string[];
     deniedDomains: string[];
     deniedDomainReasons?: Record<string, string> | undefined;
@@ -689,6 +718,8 @@ export declare const NetworkConfigSchema: z.ZodObject<{
     allowUnixSockets?: string[] | undefined;
     allowAllUnixSockets?: boolean | undefined;
     allowLocalBinding?: boolean | undefined;
+    allowedLocalPorts?: number[] | undefined;
+    allowedBindPorts?: number[] | undefined;
     allowMachLookup?: string[] | undefined;
     httpProxyPort?: number | undefined;
     socksProxyPort?: number | undefined;
@@ -701,6 +732,7 @@ export declare const NetworkConfigSchema: z.ZodObject<{
         caCertPath?: string | undefined;
         caKeyPath?: string | undefined;
         excludeDomains?: string[] | undefined;
+        includeDomains?: string[] | undefined;
         extraCaCertPaths?: string[] | undefined;
     } | undefined;
     parentProxy?: {
@@ -718,21 +750,50 @@ export declare const FilesystemConfigSchema: z.ZodObject<{
     allowRead: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     allowWrite: z.ZodArray<z.ZodString, "many">;
     denyWrite: z.ZodArray<z.ZodString, "many">;
+    allowWriteWithinDeny: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
     allowGitConfig: z.ZodOptional<z.ZodBoolean>;
+    disableMandatoryWriteProtection: z.ZodOptional<z.ZodBoolean>;
+    bindMounts: z.ZodOptional<z.ZodArray<z.ZodObject<{
+        source: z.ZodString;
+        destination: z.ZodString;
+        readOnly: z.ZodBoolean;
+    }, "strip", z.ZodTypeAny, {
+        source: string;
+        destination: string;
+        readOnly: boolean;
+    }, {
+        source: string;
+        destination: string;
+        readOnly: boolean;
+    }>, "many">>;
 }, "strip", z.ZodTypeAny, {
     denyRead: string[];
     allowWrite: string[];
     denyWrite: string[];
     disabled?: boolean | undefined;
     allowRead?: string[] | undefined;
+    allowWriteWithinDeny?: string[] | undefined;
     allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+    bindMounts?: {
+        source: string;
+        destination: string;
+        readOnly: boolean;
+    }[] | undefined;
 }, {
     denyRead: string[];
     allowWrite: string[];
     denyWrite: string[];
     disabled?: boolean | undefined;
     allowRead?: string[] | undefined;
+    allowWriteWithinDeny?: string[] | undefined;
     allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+    bindMounts?: {
+        source: string;
+        destination: string;
+        readOnly: boolean;
+    }[] | undefined;
 }>;
 /**
  * Configuration schema for ignoring specific sandbox violations
@@ -843,13 +904,16 @@ export declare const SeccompConfigSchema: z.ZodObject<{
  */
 export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     network: z.ZodObject<{
-        allowedDomains: z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">;
+        disabled: z.ZodOptional<z.ZodBoolean>;
+    allowedDomains: z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">;
         deniedDomains: z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">;
         deniedDomainReasons: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodString>>;
         strictAllowlist: z.ZodOptional<z.ZodBoolean>;
         allowUnixSockets: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         allowAllUnixSockets: z.ZodOptional<z.ZodBoolean>;
         allowLocalBinding: z.ZodOptional<z.ZodBoolean>;
+        allowedLocalPorts: z.ZodOptional<z.ZodArray<z.ZodNumber, "many">>;
+        allowedBindPorts: z.ZodOptional<z.ZodArray<z.ZodNumber, "many">>;
         allowMachLookup: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
         httpProxyPort: z.ZodOptional<z.ZodNumber>;
         socksProxyPort: z.ZodOptional<z.ZodNumber>;
@@ -868,26 +932,31 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath: z.ZodOptional<z.ZodString>;
             caKeyPath: z.ZodOptional<z.ZodString>;
             excludeDomains: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+            includeDomains: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
             extraCaCertPaths: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         }, "strip", z.ZodTypeAny, {
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         }, {
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         }>, {
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         }, {
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         }>>;
         parentProxy: z.ZodOptional<z.ZodObject<{
@@ -904,13 +973,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             noProxy?: string | undefined;
         }>>;
     }, "strip", z.ZodTypeAny, {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -923,6 +995,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -931,13 +1004,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             noProxy?: string | undefined;
         } | undefined;
     }, {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -950,6 +1026,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -964,21 +1041,50 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         allowRead: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         allowWrite: z.ZodArray<z.ZodString, "many">;
         denyWrite: z.ZodArray<z.ZodString, "many">;
+        allowWriteWithinDeny: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
         allowGitConfig: z.ZodOptional<z.ZodBoolean>;
+    disableMandatoryWriteProtection: z.ZodOptional<z.ZodBoolean>;
+        bindMounts: z.ZodOptional<z.ZodArray<z.ZodObject<{
+            source: z.ZodString;
+            destination: z.ZodString;
+            readOnly: z.ZodBoolean;
+        }, "strip", z.ZodTypeAny, {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }, {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }>, "many">>;
     }, "strip", z.ZodTypeAny, {
         denyRead: string[];
         allowWrite: string[];
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     }, {
         denyRead: string[];
         allowWrite: string[];
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     }>;
     credentials: z.ZodOptional<z.ZodObject<{
         files: z.ZodOptional<z.ZodArray<z.ZodObject<{
@@ -1028,6 +1134,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
             maskDuplicates: z.ZodOptional<z.ZodBoolean>;
             injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+            allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
         }, "strip", z.ZodTypeAny, {
             mode: "deny" | "mask";
             path: string;
@@ -1037,6 +1144,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }, {
             mode: "deny" | "mask";
             path: string;
@@ -1046,6 +1154,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }>, "many">>;
         envVars: z.ZodOptional<z.ZodArray<z.ZodObject<{
             name: z.ZodString;
@@ -1092,6 +1201,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
              */
             maskClaims: z.ZodOptional<z.ZodArray<z.ZodString, "many">>;
             injectHosts: z.ZodOptional<z.ZodArray<z.ZodEffects<z.ZodString, string, string>, "many">>;
+            allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
         }, "strip", z.ZodTypeAny, {
             mode: "deny" | "mask";
             name: string;
@@ -1100,6 +1210,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }, {
             mode: "deny" | "mask";
             name: string;
@@ -1108,6 +1219,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }>, "many">>;
         allowPlaintextInject: z.ZodOptional<z.ZodBoolean>;
         awsPairs: z.ZodOptional<z.ZodArray<z.ZodObject<{
@@ -1146,6 +1258,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1155,6 +1268,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1177,6 +1291,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1186,6 +1301,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1201,6 +1317,19 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     }>>;
     ignoreViolations: z.ZodOptional<z.ZodRecord<z.ZodString, z.ZodArray<z.ZodString, "many">>>;
     enableWeakerNestedSandbox: z.ZodOptional<z.ZodBoolean>;
+    linuxPrivilegedWorker: z.ZodOptional<z.ZodObject<{
+        uid: z.ZodNumber;
+        gid: z.ZodNumber;
+        setprivPath: z.ZodEffects<z.ZodString, string, string>;
+    }, "strict", z.ZodTypeAny, {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    }, {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    }>>;
     enableWeakerNetworkIsolation: z.ZodOptional<z.ZodBoolean>;
     allowAppleEvents: z.ZodOptional<z.ZodBoolean>;
     ripgrep: z.ZodOptional<z.ZodObject<{
@@ -1268,13 +1397,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     }>>;
 }, "strip", z.ZodTypeAny, {
     network: {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -1287,6 +1419,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -1301,7 +1434,14 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     };
     windows?: {
         sandboxUser?: string | undefined;
@@ -1322,6 +1462,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1331,6 +1472,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1346,6 +1488,11 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
     ignoreViolations?: Record<string, string[]> | undefined;
     enableWeakerNestedSandbox?: boolean | undefined;
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    } | undefined;
     enableWeakerNetworkIsolation?: boolean | undefined;
     allowAppleEvents?: boolean | undefined;
     ripgrep?: {
@@ -1366,13 +1513,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
 }, {
     network: {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -1385,6 +1535,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -1399,7 +1550,14 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     };
     windows?: {
         sandboxUser?: string | undefined;
@@ -1420,6 +1578,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1429,6 +1588,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1444,6 +1604,11 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
     ignoreViolations?: Record<string, string[]> | undefined;
     enableWeakerNestedSandbox?: boolean | undefined;
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    } | undefined;
     enableWeakerNetworkIsolation?: boolean | undefined;
     allowAppleEvents?: boolean | undefined;
     ripgrep?: {
@@ -1464,13 +1629,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
 }>, {
     network: {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -1483,6 +1651,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -1497,7 +1666,14 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     };
     windows?: {
         sandboxUser?: string | undefined;
@@ -1518,6 +1694,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1527,6 +1704,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1542,6 +1720,11 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
     ignoreViolations?: Record<string, string[]> | undefined;
     enableWeakerNestedSandbox?: boolean | undefined;
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    } | undefined;
     enableWeakerNetworkIsolation?: boolean | undefined;
     allowAppleEvents?: boolean | undefined;
     ripgrep?: {
@@ -1562,13 +1745,16 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
 }, {
     network: {
-        allowedDomains: string[];
+        disabled?: boolean | undefined;
+    allowedDomains: string[];
         deniedDomains: string[];
         deniedDomainReasons?: Record<string, string> | undefined;
         strictAllowlist?: boolean | undefined;
         allowUnixSockets?: string[] | undefined;
         allowAllUnixSockets?: boolean | undefined;
         allowLocalBinding?: boolean | undefined;
+        allowedLocalPorts?: number[] | undefined;
+        allowedBindPorts?: number[] | undefined;
         allowMachLookup?: string[] | undefined;
         httpProxyPort?: number | undefined;
         socksProxyPort?: number | undefined;
@@ -1581,6 +1767,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             caCertPath?: string | undefined;
             caKeyPath?: string | undefined;
             excludeDomains?: string[] | undefined;
+            includeDomains?: string[] | undefined;
             extraCaCertPaths?: string[] | undefined;
         } | undefined;
         parentProxy?: {
@@ -1595,7 +1782,14 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
         denyWrite: string[];
         disabled?: boolean | undefined;
         allowRead?: string[] | undefined;
+        allowWriteWithinDeny?: string[] | undefined;
         allowGitConfig?: boolean | undefined;
+    disableMandatoryWriteProtection?: boolean | undefined;
+        bindMounts?: {
+            source: string;
+            destination: string;
+            readOnly: boolean;
+        }[] | undefined;
     };
     windows?: {
         sandboxUser?: string | undefined;
@@ -1616,6 +1810,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             maskClaims?: string[] | undefined;
             maskDuplicates?: boolean | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         envVars?: {
             mode: "deny" | "mask";
@@ -1625,6 +1820,7 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
             decode?: "jwt" | undefined;
             maskClaims?: string[] | undefined;
             injectHosts?: string[] | undefined;
+            allowPlaintextInject?: boolean | undefined;
         }[] | undefined;
         allowPlaintextInject?: boolean | undefined;
         awsPairs?: {
@@ -1640,6 +1836,11 @@ export declare const SandboxRuntimeConfigSchema: z.ZodEffects<z.ZodObject<{
     } | undefined;
     ignoreViolations?: Record<string, string[]> | undefined;
     enableWeakerNestedSandbox?: boolean | undefined;
+    linuxPrivilegedWorker?: {
+        uid: number;
+        gid: number;
+        setprivPath: string;
+    } | undefined;
     enableWeakerNetworkIsolation?: boolean | undefined;
     allowAppleEvents?: boolean | undefined;
     ripgrep?: {
diff --git a/dist/sandbox/sandbox-config.js b/dist/sandbox/sandbox-config.js
index 74c24ad4680083f6be1f6ffd12e4119ddbeee489..7f4e47b9bfdec0183236cf1d863f3a83e49e5115 100644
--- a/dist/sandbox/sandbox-config.js
+++ b/dist/sandbox/sandbox-config.js
@@ -5,7 +5,7 @@
 import { isIP } from 'node:net';
 import { isAbsolute } from 'node:path';
 import { z } from 'zod';
-import { isInjectHostCoveredByAllowedDomains, splitDomainPatternPort, stripDomainPatternPort, } from './domain-pattern.js';
+import { isDomainPortPatternCoveredBy, isInjectHostCoveredByAllowedDomains, splitDomainPatternPort, stripDomainPatternPort, } from './domain-pattern.js';
 /**
  * Host-only pattern check (e.g., "example.com", "*.npmjs.org"). Rejects
  * protocols, paths, ports, and overly broad wildcards.
@@ -45,7 +45,7 @@ function isValidDomainPattern(val) {
     // Regular domains must have at least one dot and only valid characters
     return val.includes('.') && !val.startsWith('.') && !val.endsWith('.');
 }
-const DOMAIN_PATTERN_MESSAGE = 'Invalid domain pattern. Must be a valid domain (e.g., "example.com"), a wildcard (e.g., "*.example.com"), or a bracketed IPv6 literal (e.g., "[::1]", "[2001:db8::1]:443"). Overly broad patterns like "*.com" or "*" are not allowed for security reasons.';
+const DOMAIN_PATTERN_MESSAGE = 'Invalid domain pattern. Must be a valid domain (e.g., "example.com"), a wildcard (e.g., "*.example.com"), a bare "*", or a bracketed IPv6 literal (e.g., "[::1]", "[2001:db8::1]:443").';
 /**
  * Schema for domain patterns (e.g., "example.com", "*.npmjs.org")
  * Validates that domain patterns are safe and don't include overly broad wildcards
@@ -73,7 +73,8 @@ function hasValidIpv6Bracketing(val) {
 const domainPortPatternSchema = z
     .string()
     .refine(val => hasValidIpv6Bracketing(val) &&
-    isValidDomainPattern(splitDomainPatternPort(val).hostPattern), {
+    (splitDomainPatternPort(val).hostPattern === '*' ||
+        isValidDomainPattern(splitDomainPatternPort(val).hostPattern)), {
     message: DOMAIN_PATTERN_MESSAGE +
         ' An optional ":port" suffix (1-65535) restricts the entry to that port.',
 });
@@ -358,6 +359,11 @@ export const CredentialFileConfigSchema = z.object({
         'If unset, defaults to network.allowedDomains — the credential is ' +
         'injected at every reachable host. Only meaningful when mode is ' +
         '"mask"; accepted but ignored for "deny".'),
+    allowPlaintextInject: z
+        .boolean()
+        .optional()
+        .describe('Allow this credential to be substituted on plain HTTP. ' +
+        'Defaults to the block-level credentials.allowPlaintextInject value.'),
 });
 /**
  * Schema for a single credential environment variable entry.
@@ -487,6 +493,11 @@ export const CredentialEnvVarConfigSchema = z.object({
         'If unset, defaults to network.allowedDomains — the credential is ' +
         'injected at every reachable host. Only meaningful when mode is ' +
         '"mask"; accepted but ignored for "deny".'),
+    allowPlaintextInject: z
+        .boolean()
+        .optional()
+        .describe('Allow this credential to be substituted on plain HTTP. ' +
+        'Defaults to the block-level credentials.allowPlaintextInject value.'),
 });
 /**
  * Explicit grouping of masked env vars into an AWS credential pair for
@@ -613,6 +624,10 @@ export const CredentialsConfigSchema = z
  * Network configuration schema for validation
  */
 export const NetworkConfigSchema = z.object({
+    disabled: z
+        .boolean()
+        .optional()
+        .describe('Disable SRT network policy while retaining filesystem, process, and credential policy. Intended for embedders that enforce control-plane isolation separately and require normal localhost/service parity; default: false.'),
     allowedDomains: z
         .array(domainPortPatternSchema)
         .describe('List of allowed domains (e.g., ["github.com", "*.npmjs.org", "api.example.com:443"]). ' +
@@ -643,6 +658,16 @@ export const NetworkConfigSchema = z.object({
         .boolean()
         .optional()
         .describe('Whether to allow binding to local ports (default: false)'),
+    allowedLocalPorts: z
+        .array(z.number().int().min(1).max(65535))
+        .max(256)
+        .optional()
+        .describe('Exact loopback destination ports allowed by the proxy and macOS profile. When present, all other loopback destinations are denied.'),
+    allowedBindPorts: z
+        .array(z.number().int().min(1).max(65535))
+        .max(256)
+        .optional()
+        .describe('macOS only: exact local TCP ports allowed for bind and inbound operations. Unlike allowLocalBinding, this does not grant arbitrary ports.'),
     allowMachLookup: z
         .array(z.string().refine(val => {
         const prefix = val.endsWith('*') ? val.slice(0, -1) : val;
@@ -712,6 +737,14 @@ export const NetworkConfigSchema = z.object({
             'and clients that pin the upstream certificate and would reject ' +
             'the MITM CA. Hosts still need to be reachable via ' +
             'allowedDomains; this list only changes how they are tunnelled.'),
+        includeDomains: z
+            .array(domainPortPatternSchema)
+            .min(1)
+            .optional()
+            .describe('When set, terminate TLS only for these exact domain/port ' +
+            'patterns; every other allowed CONNECT remains an opaque tunnel. ' +
+            'This keeps credential injection narrow without changing ordinary ' +
+            'network compatibility.'),
         extraCaCertPaths: z
             .array(z.string().min(1))
             .optional()
@@ -761,10 +794,27 @@ export const FilesystemConfigSchema = z.object({
     denyWrite: z
         .array(filesystemPathSchema)
         .describe('Paths denied for writing (takes precedence over allowWrite)'),
+    allowWriteWithinDeny: z
+        .array(filesystemPathSchema)
+        .optional()
+        .describe('Paths to re-allow for writing within a broader denyWrite region. Narrower denyWrite entries inside an exception remain denied.'),
     allowGitConfig: z
         .boolean()
         .optional()
         .describe('Allow writes to .git/config files (default: false). Enables git remote URL updates while keeping .git/hooks protected.'),
+    disableMandatoryWriteProtection: z
+        .boolean()
+        .optional()
+        .describe('Disable the built-in opinionated project/config write deny list while retaining explicit allowWrite and denyWrite enforcement. Intended for embedders that supply their own complete denyWrite policy; default: false.'),
+    bindMounts: z
+        .array(z.object({
+        source: filesystemPathSchema,
+        destination: filesystemPathSchema,
+        readOnly: z.boolean(),
+    }))
+        .max(32)
+        .optional()
+        .describe('Linux-only trusted bind-mount projections applied after deny masks. Sources and destinations must be existing canonical absolute paths of the same type. Embedders must validate this capability before passing it to SRT.'),
 });
 /**
  * Configuration schema for ignoring specific sandbox violations
@@ -887,6 +937,19 @@ export const SeccompConfigSchema = z.object({
         'applyPath resolves inside the bwrap namespace and that the target ' +
         'binary implements the apply-seccomp interface when ARGV0 matches.'),
 });
+/**
+ * Trusted cloud-worker identity used only when a root-owned supervisor is the
+ * caller. Bubblewrap retains privilege long enough to construct namespaces and
+ * mounts, then setpriv irreversibly enters this non-root identity before any
+ * untrusted command bytes execute.
+ */
+const LinuxPrivilegedWorkerConfigSchema = z
+    .object({
+    uid: z.number().int().min(1).max(2147483647),
+    gid: z.number().int().min(1).max(2147483647),
+    setprivPath: binaryPathSchema,
+})
+    .strict();
 /**
  * Main configuration schema for Sandbox Runtime validation
  */
@@ -901,6 +964,10 @@ export const SandboxRuntimeConfigSchema = z
         .boolean()
         .optional()
         .describe('Enable weaker nested sandbox mode (for Docker environments)'),
+    linuxPrivilegedWorker: LinuxPrivilegedWorkerConfigSchema.optional().describe('Linux only: trusted root-supervisor mode. Mount namespaces are created ' +
+        'without a nested user namespace, then the workload enters the supplied ' +
+        'non-root uid/gid with locked securebits, an empty capability bounding set, ' +
+        'and no_new_privs. Never populate this from sandboxed input.'),
     enableWeakerNetworkIsolation: z
         .boolean()
         .optional()
@@ -945,6 +1012,13 @@ export const SandboxRuntimeConfigSchema = z
         '`safe.directory` without adding them to `filesystem.allowWrite`.'),
 })
     .superRefine((cfg, ctx) => {
+    if (cfg.linuxPrivilegedWorker && cfg.enableWeakerNestedSandbox) {
+        ctx.addIssue({
+            code: z.ZodIssueCode.custom,
+            path: ['linuxPrivilegedWorker'],
+            message: 'linuxPrivilegedWorker and enableWeakerNestedSandbox are mutually exclusive',
+        });
+    }
     const creds = cfg.credentials;
     if (!creds)
         return;
@@ -952,12 +1026,10 @@ export const SandboxRuntimeConfigSchema = z
     // allowedDomains — semantic (wildcard-aware) coverage, not literal
     // string membership, so `injectHosts: ['api.github.com']` is accepted
     // when `allowedDomains: ['*.github.com']`.
-    // Host-scoped view of the allowlist (":port" suffixes dropped) —
-    // injection is per-host, not per-port.
     const allowedHosts = cfg.network.allowedDomains.map(stripDomainPatternPort);
     const checkSubset = (hosts, path) => {
         for (const [i, host] of hosts.entries()) {
-            if (!isInjectHostCoveredByAllowedDomains(host, allowedHosts)) {
+            if (!isDomainPortPatternCoveredBy(host, cfg.network.allowedDomains)) {
                 ctx.addIssue({
                     code: z.ZodIssueCode.custom,
                     path: [...path, i],
@@ -981,6 +1053,23 @@ export const SandboxRuntimeConfigSchema = z
         if (entry.mode !== 'mask')
             return;
         hasMasked = true;
+        const include = cfg.network.tlsTerminate?.includeDomains;
+        const effectiveHosts = entry.injectHosts ?? cfg.network.allowedDomains;
+        if (include?.length) {
+            for (const [i, host] of effectiveHosts.entries()) {
+                if (!isDomainPortPatternCoveredBy(host, include)) {
+                    ctx.addIssue({
+                        code: z.ZodIssueCode.custom,
+                        path: entry.injectHosts
+                            ? [...path, 'injectHosts', i]
+                            : [...path],
+                        message: `Credential destination "${host}" is not covered by ` +
+                            `network.tlsTerminate.includeDomains, so HTTPS injection ` +
+                            `could never occur there.`,
+                    });
+                }
+            }
+        }
         // Credential substitution only runs on the TLS-terminated path, so a
         // host covered by tlsTerminate.excludeDomains can never receive the
         // real value — the upstream sees the placeholder. Reject the
@@ -997,7 +1086,7 @@ export const SandboxRuntimeConfigSchema = z
         if (exclude?.length) {
             if (entry.injectHosts) {
                 for (const [i, host] of entry.injectHosts.entries()) {
-                    if (isInjectHostCoveredByAllowedDomains(host, exclude)) {
+                    if (isDomainPortPatternCoveredBy(host, exclude)) {
                         ctx.addIssue({
                             code: z.ZodIssueCode.custom,
                             path: [...path, 'injectHosts', i],
diff --git a/dist/sandbox/sandbox-manager.js b/dist/sandbox/sandbox-manager.js
index 5be4a4f0928908ab4d0ad437dd33b236e53e9b95..d122101967bb94c3bbd3e59bab160046ce9c1f8d 100644
--- a/dist/sandbox/sandbox-manager.js
+++ b/dist/sandbox/sandbox-manager.js
@@ -19,8 +19,9 @@ import { checkWindowsDependencies, checkWindowsDependenciesAsync, wrapCommandWit
 import { getDefaultWritePaths, containsGlobChars, removeTrailingGlobSuffix, expandGlobPattern, decodeSandboxedCommand, encodeSandboxedCommand, } from './sandbox-utils.js';
 import { SandboxViolationStore, sanitizeViolationText, shouldIgnoreViolation, } from './sandbox-violation-store.js';
 import { canonicalizeHost, isValidHost, redactUrl, resolveParentProxy, } from './parent-proxy.js';
-import { matchesDomainPattern, matchesDomainPatternWithPort, stripDomainPatternPort, } from './domain-pattern.js';
+import { matchesDomainPattern, matchesDomainPatternWithPort, } from './domain-pattern.js';
 import { EOL } from 'node:os';
+import { BlockList, isIP } from 'node:net';
 import { dirname } from 'node:path';
 // ============================================================================
 // Private Module State
@@ -181,6 +182,21 @@ function redactUrlForViolation(url) {
         return q === -1 ? url : `${url.slice(0, q)}?…`;
     }
 }
+const zerosLoopbackAddresses = new BlockList();
+zerosLoopbackAddresses.addSubnet('127.0.0.0', 8, 'ipv4');
+zerosLoopbackAddresses.addAddress('::1', 'ipv6');
+zerosLoopbackAddresses.addSubnet('::ffff:127.0.0.0', 104, 'ipv6');
+function isZerosLoopbackHost(host) {
+    if (host.toLowerCase() === 'localhost')
+        return true;
+    const unbracketed = host.startsWith('[') && host.endsWith(']') ? host.slice(1, -1) : host;
+    const family = isIP(unbracketed);
+    return family === 4
+        ? zerosLoopbackAddresses.check(unbracketed, 'ipv4')
+        : family === 6
+            ? zerosLoopbackAddresses.check(unbracketed, 'ipv6')
+            : false;
+}
 async function filterNetworkRequest(port, host, sandboxAskCallback, encodedCommand) {
     const denied = (reason) => {
         recordProxyViolation(`deny network-outbound ${host}:${port} (${reason})`, encodedCommand);
@@ -217,6 +233,12 @@ async function filterNetworkRequest(port, host, sandboxAskCallback, encodedComma
                 'host is on the deny list');
         }
     }
+    const allowedLocalPorts = config.network.allowedLocalPorts;
+    if (allowedLocalPorts !== undefined &&
+        isZerosLoopbackHost(canonicalHost) &&
+        !allowedLocalPorts.includes(port)) {
+        return denied('loopback port is not leased to this sandbox');
+    }
     // Check allowed domains
     for (const allowedDomain of config.network.allowedDomains) {
         if (matchesDomainPatternWithPort(canonicalHost, port, allowedDomain)) {
@@ -258,11 +280,11 @@ async function filterNetworkRequest(port, host, sandboxAskCallback, encodedComma
  * returned closure does not log header values; the registry holds the only
  * copy of the real value.
  */
-function buildCredentialInjector() {
+function buildCredentialInjector(plaintext = false) {
     if (!config?.credentials)
         return undefined;
-    return (headers, destHost) => {
-        sentinelRegistry.substituteInHeaders(headers, destHost, matchesDomainPattern);
+    return (headers, destHost, destPort) => {
+        sentinelRegistry.substituteInHeaders(headers, destHost, destPort, matchesDomainPatternWithPort, plaintext);
     };
 }
 /**
@@ -270,10 +292,10 @@ function buildCredentialInjector() {
  * sentinel→real pairs for streaming substitution in request bodies, with
  * the same per-credential injectHosts gating applied inside the registry.
  */
-function buildBodyCredentialInjector() {
+function buildBodyCredentialInjector(plaintext = false) {
     if (!config?.credentials)
         return undefined;
-    return destHost => sentinelRegistry.sentinelsForHost(destHost, matchesDomainPattern);
+    return (destHost, destPort) => sentinelRegistry.sentinelsForHost(destHost, destPort, matchesDomainPatternWithPort, plaintext);
 }
 /**
  * Build the per-request SigV4 hook for the TLS-terminating proxy.
@@ -329,11 +351,16 @@ function getMitmSocketPath(host) {
  * canonicalization (`127.1`, a trailing-dot FQDN) would dodge the exclusion
  * and get terminated anyway.
  */
-function shouldTerminateTLSForHost(host) {
+function shouldTerminateTLSForHost(host, port) {
+    const includeDomains = config?.network.tlsTerminate?.includeDomains;
+    const canonicalHost = canonicalizeHost(host) ?? host;
+    if (includeDomains?.length &&
+        !includeDomains.some(pattern => matchesDomainPatternWithPort(canonicalHost, port, pattern))) {
+        return false;
+    }
     const excludeDomains = config?.network.tlsTerminate?.excludeDomains;
     if (!excludeDomains?.length)
         return true;
-    const canonicalHost = canonicalizeHost(host) ?? host;
     for (const pattern of excludeDomains) {
         if (!matchesDomainPattern(canonicalHost, pattern))
             continue;
@@ -344,7 +371,7 @@ function shouldTerminateTLSForHost(host) {
         // rejects the fully-contradictory spellings; this flags the partial
         // ones (e.g. default injectHosts = allowedDomains) at the moment they
         // actually bite.
-        const masked = sentinelRegistry.namesInjectableAt(canonicalHost, matchesDomainPattern);
+        const masked = sentinelRegistry.namesInjectableAt(canonicalHost, port, matchesDomainPatternWithPort);
         if (masked.length > 0) {
             logForDebugging(`tlsTerminate.excludeDomains: masked credential(s) ${masked.join(', ')} ` +
                 `are configured for injection at ${host}, but its connections are ` +
@@ -357,10 +384,22 @@ function shouldTerminateTLSForHost(host) {
 async function startMuxProxyServer(sandboxAskCallback, portRange) {
     const injectCredentials = buildCredentialInjector();
     const injectBodyCredentials = buildBodyCredentialInjector();
+    const injectPlaintextCredentials = buildCredentialInjector(true);
+    const injectPlaintextBodyCredentials = buildBodyCredentialInjector(true);
     httpProxyServer = createHttpProxyServer({
         filter: (port, host, _socket, encodedCommand) => filterNetworkRequest(port, host, sandboxAskCallback, encodedCommand),
         getMitmSocketPath,
         mitmCA,
+        // Node reads NODE_EXTRA_CA_CERTS only at process startup. A library
+        // consumer can configure tlsTerminate.extraCaCertPaths after startup,
+        // so the proxy's own outbound TLS leg must receive the generated trust
+        // bundle explicitly or internal/corporate HTTPS endpoints fail while
+        // the sandboxed child trusts them. The bundle contains certificates
+        // only (never private keys) and is SRT-owned.
+        tlsTerminateUpstreamCA: mitmCA &&
+            config?.network.tlsTerminate?.extraCaCertPaths?.length
+            ? fs.readFileSync(mitmCA.trustBundlePath)
+            : undefined,
         shouldTerminateTLS: shouldTerminateTLSForHost,
         filterRequest: config?.network.filterRequest,
         onFilterRequestDenied: ({ method, url, reason, encodedCommand }) => {
@@ -370,13 +409,9 @@ async function startMuxProxyServer(sandboxAskCallback, portRange) {
         // only when explicitly opted in. Without the opt-in, a sentinel sent
         // over plain HTTP reaches the upstream unchanged (fails closed).
         mutateHeaders: injectCredentials,
-        mutateHeadersPlaintext: config?.credentials?.allowPlaintextInject
-            ? injectCredentials
-            : undefined,
+        mutateHeadersPlaintext: injectPlaintextCredentials,
         getBodySubstitutions: injectBodyCredentials,
-        getBodySubstitutionsPlaintext: config?.credentials?.allowPlaintextInject
-            ? injectBodyCredentials
-            : undefined,
+        getBodySubstitutionsPlaintext: injectPlaintextBodyCredentials,
         // SigV4 re-signing is TLS-terminated-path only, like credential
         // injection: the real signature must not travel over plaintext.
         planSigv4: buildSigv4Planner(),
@@ -836,9 +871,7 @@ function getCredentialRestrictions(credentials, allowedDomains) {
     // Default injectHosts (= allowedDomains) is host-scoped: drop any
     // `:port` suffixes, otherwise the sentinel would carry an entry no bare
     // destination host can ever match and the credential would never inject.
-    const defaultInjectHosts = [
-        ...new Set((allowedDomains ?? []).map(stripDomainPatternPort)),
-    ];
+    const defaultInjectHosts = [...new Set(allowedDomains ?? [])];
     const unsetEnvVars = [];
     for (const v of credentials.envVars ?? []) {
         if (v.mode === 'deny')
@@ -849,7 +882,7 @@ function getCredentialRestrictions(credentials, allowedDomains) {
     // the sandbox. degradeToUnsetNames carries variables whose extract
     // pattern matched nothing with onExtractNoMatch: "deny" — merged into
     // unsetEnvVars below so the value is withheld rather than exposed.
-    const { setEnvVars, degradeToUnsetNames } = buildMaskedEnvVars(credentials.envVars ?? [], defaultInjectHosts, sentinelRegistry);
+    const { setEnvVars, degradeToUnsetNames } = buildMaskedEnvVars(credentials.envVars ?? [], defaultInjectHosts, sentinelRegistry, process.env, credentials.allowPlaintextInject ?? false);
     unsetEnvVars.push(...degradeToUnsetNames);
     // Link masked AWS credentials into pairs so the proxy can re-sign
     // SigV4 requests (the signature is derived from the secret; header
@@ -862,7 +895,7 @@ function getCredentialRestrictions(credentials, allowedDomains) {
     // nothing with onExtractNoMatch: "deny" — merged into denyReadPaths
     // below so both the read-deny config and the platform builders see them.
     const files = credentials.files ?? [];
-    const { binds: maskedFileBinds, degradeToDenyPaths } = buildMaskedFileBinds(files, defaultInjectHosts, sentinelRegistry, maskedFileStore);
+    const { binds: maskedFileBinds, degradeToDenyPaths } = buildMaskedFileBinds(files, defaultInjectHosts, sentinelRegistry, maskedFileStore, credentials.allowPlaintextInject ?? false);
     return {
         denyReadPaths: [...new Set([...denyReadPaths, ...degradeToDenyPaths])],
         unsetEnvVars: [...new Set(unsetEnvVars)],
@@ -962,6 +995,7 @@ function getFsWriteConfig() {
     return {
         allowOnly,
         denyWithinAllow: denyPaths,
+        allowWithinDeny: config.filesystem.allowWriteWithinDeny ?? [],
     };
 }
 /**
@@ -1082,6 +1116,24 @@ function getAllowAllUnixSockets() {
 function getAllowLocalBinding() {
     return config?.network?.allowLocalBinding;
 }
+function getAllowedLocalPorts() {
+    return config?.network?.allowedLocalPorts;
+}
+function getAllowedBindPorts() {
+    return config?.network?.allowedBindPorts;
+}
+function getDeniedLocalPorts() {
+    const ports = new Set();
+    for (const entry of config?.network?.deniedDomains ?? []) {
+        const match = /^(?:localhost|127\.0\.0\.1|\[::1\]):([0-9]{1,5})$/i.exec(entry);
+        if (!match)
+            continue;
+        const port = Number(match[1]);
+        if (Number.isInteger(port) && port >= 1 && port <= 65535)
+            ports.add(port);
+    }
+    return [...ports].sort((a, b) => a - b);
+}
 function getAllowMachLookup() {
     return config?.network?.allowMachLookup;
 }
@@ -1091,6 +1143,9 @@ function getIgnoreViolations() {
 function getEnableWeakerNestedSandbox() {
     return config?.enableWeakerNestedSandbox;
 }
+function getLinuxPrivilegedWorker() {
+    return config?.linuxPrivilegedWorker;
+}
 function getEnableWeakerNetworkIsolation() {
     return config?.enableWeakerNetworkIsolation;
 }
@@ -1106,6 +1161,12 @@ function getMandatoryDenySearchDepth() {
 function getAllowGitConfig() {
     return config?.filesystem?.allowGitConfig ?? false;
 }
+function getDisableMandatoryWriteProtection() {
+    return config?.filesystem?.disableMandatoryWriteProtection ?? false;
+}
+function getFilesystemBindMounts(customConfig) {
+    return customConfig?.filesystem?.bindMounts ?? config?.filesystem?.bindMounts;
+}
 /**
  * Union of session-level and per-call `git.safeDirectories`. Marks
  * paths as `safe.directory` (dubious-ownership bypass) WITHOUT
@@ -1202,6 +1263,9 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
             denyWithinAllow: stripWriteGlobs(customConfig?.filesystem?.denyWrite ??
                 config?.filesystem.denyWrite ??
                 []),
+            allowWithinDeny: stripWriteGlobs(customConfig?.filesystem?.allowWriteWithinDeny ??
+                config?.filesystem.allowWriteWithinDeny ??
+                []),
         };
         // Credential deny paths are unioned with the caller's denyRead — never
         // replacing it — so explicit filesystem restrictions always survive.
@@ -1243,8 +1307,12 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
     // 1. customConfig has network.allowedDomains defined (even if empty array = block all)
     // 2. OR config has network.allowedDomains defined (even if empty array = block all)
     // An empty allowedDomains array means "no domains allowed" = block all network access
-    const hasNetworkConfig = customConfig?.network?.allowedDomains !== undefined ||
-        config?.network?.allowedDomains !== undefined;
+    const networkDisabled = customConfig?.network !== undefined
+        ? (customConfig.network.disabled ?? false)
+        : (config?.network?.disabled ?? false);
+    const hasNetworkConfig = !networkDisabled &&
+        (customConfig?.network?.allowedDomains !== undefined ||
+            config?.network?.allowedDomains !== undefined);
     // Network RESTRICTION is needed whenever network config is specified
     // This includes empty allowedDomains which means "block all network"
     const needsNetworkRestriction = hasNetworkConfig;
@@ -1280,10 +1348,14 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
                 allowUnixSockets: getAllowUnixSockets(),
                 allowAllUnixSockets: getAllowAllUnixSockets(),
                 allowLocalBinding: getAllowLocalBinding(),
+                allowedLocalPorts: getAllowedLocalPorts(),
+                allowedBindPorts: getAllowedBindPorts(),
+                deniedLocalPorts: getDeniedLocalPorts(),
                 allowMachLookup: getAllowMachLookup(),
                 ignoreViolations: getIgnoreViolations(),
                 allowPty,
                 allowGitConfig: getAllowGitConfig(),
+                disableMandatoryWriteProtection: getDisableMandatoryWriteProtection(),
                 gitSafeDirectories,
                 enableWeakerNetworkIsolation: getEnableWeakerNetworkIsolation(),
                 allowAppleEvents: getAllowAppleEvents(),
@@ -1316,11 +1388,14 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
                 maskedFileBinds: credentialRestrictions.maskedFileBinds,
                 maskedFileStoreDir: credentialRestrictions.maskedFileStoreDir,
                 enableWeakerNestedSandbox: getEnableWeakerNestedSandbox(),
+                linuxPrivilegedWorker: getLinuxPrivilegedWorker(),
                 allowAllUnixSockets: getAllowAllUnixSockets(),
                 binShell,
                 ripgrepConfig: getRipgrepConfig(),
                 mandatoryDenySearchDepth: getMandatoryDenySearchDepth(),
                 allowGitConfig: getAllowGitConfig(),
+                disableMandatoryWriteProtection: getDisableMandatoryWriteProtection(),
+                bindMounts: getFilesystemBindMounts(customConfig),
                 gitSafeDirectories,
                 seccompConfig: getSeccompConfig(),
                 bwrapPath: config?.bwrapPath,
@@ -1365,8 +1440,12 @@ async function wrapWithSandbox(command, binShell, customConfig, abortSignal, opt
 async function wrapWithSandboxArgv(command, binShell, customConfig, abortSignal, cwd, options) {
     const platform = getPlatform();
     if (platform === 'windows') {
-        const hasNetworkConfig = customConfig?.network?.allowedDomains !== undefined ||
-            config?.network?.allowedDomains !== undefined;
+        const networkDisabled = customConfig?.network !== undefined
+            ? (customConfig.network.disabled ?? false)
+            : (config?.network?.disabled ?? false);
+        const hasNetworkConfig = !networkDisabled &&
+            (customConfig?.network?.allowedDomains !== undefined ||
+                config?.network?.allowedDomains !== undefined);
         if (hasNetworkConfig) {
             await waitForNetworkInitialization();
         }
diff --git a/dist/sandbox/sandbox-schemas.d.ts b/dist/sandbox/sandbox-schemas.d.ts
index d753a3a3a500be8c485f511ec5a94d5921094b91..48e851637c5f009bf42bccad2a44e67d19cdec54 100644
--- a/dist/sandbox/sandbox-schemas.d.ts
+++ b/dist/sandbox/sandbox-schemas.d.ts
@@ -34,6 +34,7 @@ export interface FsReadRestrictionConfig {
 export interface FsWriteRestrictionConfig {
     allowOnly: string[];
     denyWithinAllow: string[];
+    allowWithinDeny?: string[];
 }
 /**
  * Credential restriction config (internal structure built from the
diff --git a/dist/sandbox/sandbox-utils.d.ts b/dist/sandbox/sandbox-utils.d.ts
index c29547245ee516a3e19fb9e56c53f7d1309ec0dd..e86d629057a53ca33960ea417fbbf8b62ab24108 100644
--- a/dist/sandbox/sandbox-utils.d.ts
+++ b/dist/sandbox/sandbox-utils.d.ts
@@ -137,7 +137,7 @@ export declare function getDefaultWritePaths(): string[];
  * Per-tool trust-store env vars set to the TLS-termination CA cert path so
  * HTTPS clients in the sandboxed child accept proxy-minted certs.
  */
-export declare const CA_TRUST_VARS: readonly ["NODE_EXTRA_CA_CERTS", "SSL_CERT_FILE", "CURL_CA_BUNDLE", "REQUESTS_CA_BUNDLE", "PIP_CERT", "GIT_SSL_CAINFO", "AWS_CA_BUNDLE", "CARGO_HTTP_CAINFO", "DENO_CERT", "CLOUDSDK_CORE_CUSTOM_CA_CERTS_FILE", "NIX_SSL_CERT_FILE"];
+export declare const CA_TRUST_VARS: readonly ["NODE_EXTRA_CA_CERTS", "SSL_CERT_FILE", "CURL_CA_BUNDLE", "REQUESTS_CA_BUNDLE", "PIP_CERT", "GIT_SSL_CAINFO", "AWS_CA_BUNDLE", "CARGO_HTTP_CAINFO", "DENO_CERT", "CLOUDSDK_CORE_CUSTOM_CA_CERTS_FILE", "NIX_SSL_CERT_FILE", "NPM_CONFIG_CAFILE", "npm_config_cafile", "GRPC_DEFAULT_SSL_ROOTS_FILE_PATH"];
 export declare function generateProxyEnvVars(httpProxyPort?: number, socksProxyPort?: number, caCertPath?: string, proxyAuthToken?: string, skipTmpdir?: boolean, encodedCommand?: string): string[];
 /**
  * Build the `GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_<n>` /
diff --git a/dist/sandbox/sandbox-utils.js b/dist/sandbox/sandbox-utils.js
index 32831bf4b8fb14d9ea6fc5bcfe0dc621702c91f4..df5f2b526c1a497e537bf189f91363c19a1506e1 100644
--- a/dist/sandbox/sandbox-utils.js
+++ b/dist/sandbox/sandbox-utils.js
@@ -406,6 +406,11 @@ export const CA_TRUST_VARS = [
     // it's typically pre-set to the Nix system bundle in the parent env, so we
     // must override it explicitly.
     'NIX_SSL_CERT_FILE',
+    // npm/Bun package-manager cafile config and native gRPC Core do not
+    // consistently consult SSL_CERT_FILE.
+    'NPM_CONFIG_CAFILE',
+    'npm_config_cafile',
+    'GRPC_DEFAULT_SSL_ROOTS_FILE_PATH',
 ];
 export function generateProxyEnvVars(httpProxyPort, socksProxyPort, caCertPath, proxyAuthToken, skipTmpdir, encodedCommand) {
     // When the proxy requires auth, embed the credential in the URL so clients
diff --git a/dist/sandbox/tls-terminate-proxy.d.ts b/dist/sandbox/tls-terminate-proxy.d.ts
index 059e9bc560e31d8e7d1c508f94414ab97eac6ffa..d5513cdaf0f9dfab100a4a2bd10c14b678fce446 100644
--- a/dist/sandbox/tls-terminate-proxy.d.ts
+++ b/dist/sandbox/tls-terminate-proxy.d.ts
@@ -3,10 +3,12 @@
  *
  * When a MitmCA is configured, the forward proxy hands CONNECT requests here
  * instead of opening an opaque byte tunnel. We terminate the client's TLS
- * with a per-host leaf cert (see mitm-leaf.ts), parse the decrypted stream
- * as HTTP/1.1, and re-issue each request upstream over a real TLS
- * connection. The optional `filterRequest` callback runs on each parsed
- * request before it is forwarded.
+ * with a per-host leaf cert (see mitm-leaf.ts), parse the decrypted stream as
+ * HTTP/2 or HTTP/1.1, and re-issue each request upstream over a real TLS
+ * connection using the same application protocol. Classic HTTP/1.1 WebSocket
+ * upgrades are relayed after their handshake is filtered and mutated. The
+ * optional `filterRequest` callback runs on each parsed request before it is
+ * forwarded.
  */
 import type { Duplex } from 'node:stream';
 import type { MitmCA } from './mitm-ca.js';
@@ -46,12 +48,16 @@ export declare function looksLikeClientHello(buf: Buffer): boolean;
  * Any bytes consumed here are returned in `.head` so the caller can forward
  * them to whichever downstream (terminate or opaque tunnel) it picks. The
  * socket is left paused so further bytes buffer until the downstream
- * `pipe()` resumes it.
+ * `pipe()` resumes it. Ambiguous partial prefixes reject after a bounded
+ * timeout instead of holding a proxy connection indefinitely.
  */
 export declare function peekForClientHello(socket: Duplex, head: Buffer): Promise<{
     isTLS: boolean;
     head: Buffer;
 }>;
+/** Parse the ALPN offer from a complete TLS ClientHello, or return null while
+ * more TLS record bytes are required. Malformed/oversized input throws. */
+export declare function parseClientHelloAlpn(buffer: Buffer): string[] | null;
 export type TerminateTarget = {
     /**
      * Canonical (see canonicalizeHost) CONNECT target: what the allowlist
@@ -77,9 +83,8 @@ export type TerminateTarget = {
     onFilterRequestDeny?: (method: string, url: string, reason: string) => void;
 };
 /**
- * Terminate the client's TLS on `socket`, parse the decrypted HTTP/1.1
- * stream, and forward each request to `target` over a fresh upstream TLS
- * connection.
+ * Terminate the client's TLS on `socket`, parse decrypted HTTP/2 or HTTP/1.1,
+ * and forward each request to `target` over an upstream TLS connection.
  *
  * Preconditions: the caller has already validated `target` against the
  * domain allowlist; this function does not re-check it.
@@ -92,5 +97,5 @@ export type TerminateTarget = {
  * request handler close over `target` (which carries the originally-
  * requested host:port) without socket-keyed lookups.
  */
-export declare function terminateAndForward(ca: MitmCA, filterRequest: FilterRequestCallback | undefined, mutateHeaders: MutateForwardedHeaders | undefined, getBodySubstitutions: GetBodySubstitutions | undefined, socket: Duplex, head: Buffer, target: TerminateTarget, planSigv4?: PlanSigv4, maxSigv4BodyBytes?: number): void;
+export declare function terminateAndForward(ca: MitmCA, filterRequest: FilterRequestCallback | undefined, mutateHeaders: MutateForwardedHeaders | undefined, getBodySubstitutions: GetBodySubstitutions | undefined, socket: Duplex, head: Buffer, target: TerminateTarget, planSigv4?: PlanSigv4, maxSigv4BodyBytes?: number): Promise<void>;
 //# sourceMappingURL=tls-terminate-proxy.d.ts.map
\ No newline at end of file
diff --git a/dist/sandbox/tls-terminate-proxy.js b/dist/sandbox/tls-terminate-proxy.js
index 648c26617daded9d524d86b7243eccb62e8e8a2e..fe5d6aa5b853437db8869ce91c12432f3fc94591 100644
--- a/dist/sandbox/tls-terminate-proxy.js
+++ b/dist/sandbox/tls-terminate-proxy.js
@@ -3,19 +3,24 @@
  *
  * When a MitmCA is configured, the forward proxy hands CONNECT requests here
  * instead of opening an opaque byte tunnel. We terminate the client's TLS
- * with a per-host leaf cert (see mitm-leaf.ts), parse the decrypted stream
- * as HTTP/1.1, and re-issue each request upstream over a real TLS
- * connection. The optional `filterRequest` callback runs on each parsed
- * request before it is forwarded.
+ * with a per-host leaf cert (see mitm-leaf.ts), parse the decrypted stream as
+ * HTTP/2 or HTTP/1.1, and re-issue each request upstream over a real TLS
+ * connection using the same application protocol. Classic HTTP/1.1 WebSocket
+ * upgrades are relayed after their handshake is filtered and mutated. The
+ * optional `filterRequest` callback runs on each parsed request before it is
+ * forwarded.
  */
-import { createServer as createHttpsServer, request as httpsRequest, } from 'node:https';
+import { Agent as HttpsAgent, request as httpsRequest } from 'node:https';
+import { connect as connectHttp2, constants as http2Constants, createSecureServer as createHttp2SecureServer, sensitiveHeaders, } from 'node:http2';
 import { connect, isIP } from 'node:net';
-import { unlink } from 'node:fs';
+import { connect as connectTls } from 'node:tls';
+import { mkdtempSync, rmdir, unlink } from 'node:fs';
 import { tmpdir } from 'node:os';
 import { join } from 'node:path';
 import { logForDebugging } from '../utils/debug.js';
-import { decideAndRespond, respondDenied, } from './request-filter.js';
-import { prepareBodySubstitution, } from './body-substitution.js';
+import { decideAndRespond, DEFAULT_DENY_REASON, respondDenied, } from './request-filter.js';
+import { requestDeclaresBody } from './request-filter.js';
+import { prepareBodySubstitution } from './body-substitution.js';
 import { mintLeafCert, secureContextFor } from './mitm-leaf.js';
 import { stripHopByHop } from './parent-proxy.js';
 import { sha256Hex } from './aws-sigv4.js';
@@ -29,6 +34,11 @@ import { sha256Hex } from './aws-sigv4.js';
  * with larger payloads should sign UNSIGNED-PAYLOAD, which streams.
  */
 export const MAX_SIGV4_RESIGN_BODY_BYTES = 64 * 1024 * 1024;
+const MAX_CLIENT_HELLO_BYTES = 256 * 1024;
+const MAX_HANDSHAKE_RECORD_BYTES = 18 * 1024;
+const CLIENT_HELLO_TIMEOUT_MS = 10000;
+const UPSTREAM_TLS_TIMEOUT_MS = 30000;
+const HTTP2_SETTINGS_TIMEOUT_MS = 10000;
 /** Rejection cause when a body exceeds {@link MAX_SIGV4_RESIGN_BODY_BYTES}. */
 class BodyTooLargeError extends Error {
 }
@@ -47,7 +57,7 @@ class BodyTooLargeError extends Error {
  * start 16 03 0x is handed to the TLS server, which then rejects it properly.
  */
 export function looksLikeClientHello(buf) {
-    return (buf.length >= 3 && buf[0] === 0x16 && buf[1] === 0x03 && buf[2] <= 0x03);
+    return buf.length >= 3 && buf[0] === 0x16 && buf[1] === 0x03 && buf[2] <= 0x03;
 }
 /**
  * Wait for the client's first post-CONNECT bytes and report whether they look
@@ -60,27 +70,240 @@ export function looksLikeClientHello(buf) {
  * `pipe()` resumes it.
  */
 export function peekForClientHello(socket, head) {
-    if (head.length >= 3) {
-        return Promise.resolve({ isTLS: looksLikeClientHello(head), head });
-    }
-    return new Promise(resolve => {
+    const classify = buf => {
+        if (buf.length >= 1 && buf[0] !== 0x16)
+            return false;
+        if (buf.length >= 2 && buf[1] !== 0x03)
+            return false;
+        if (buf.length >= 3)
+            return looksLikeClientHello(buf);
+        return null;
+    };
+    const initial = classify(head);
+    if (initial !== null)
+        return Promise.resolve({ isTLS: initial, head });
+    return new Promise((resolve, reject) => {
         let buf = head;
-        const done = () => {
+        const timer = setTimeout(() => done(new Error('CONNECT protocol sniff timed out')), CLIENT_HELLO_TIMEOUT_MS);
+        timer.unref?.();
+        const done = (error, isTLS) => {
+            clearTimeout(timer);
             socket.removeListener('data', onData);
-            socket.removeListener('close', done);
-            resolve({ isTLS: looksLikeClientHello(buf), head: buf });
+            socket.removeListener('close', onClose);
+            if (error)
+                reject(error);
+            else
+                resolve({ isTLS: isTLS ?? false, head: buf });
         };
-        const onData = (chunk) => {
+        const onData = chunk => {
             // Pause synchronously so anything after this chunk buffers for the
             // downstream pipe() rather than being dropped in flowing mode.
             socket.pause();
             buf = buf.length ? Buffer.concat([buf, chunk]) : chunk;
-            if (buf.length >= 3)
-                return done();
+            const classification = classify(buf);
+            if (classification !== null)
+                return done(undefined, classification);
             socket.resume();
         };
+        const onClose = () => done(undefined, false);
         socket.on('data', onData);
-        socket.once('close', done);
+        socket.once('close', onClose);
+    });
+}
+/**
+ * Parse the ALPN protocol list from a complete TLS ClientHello.
+ *
+ * `null` means more record bytes are required. Malformed or oversized input
+ * throws so a credential-bearing MITM tunnel fails before any trusted leaf is
+ * served or upstream HTTP is emitted.
+ */
+export function parseClientHelloAlpn(buffer) {
+    const handshakeParts = [];
+    let handshakeBytes = 0;
+    let handshakeLength;
+    let offset = 0;
+    while (true) {
+        if (buffer.length - offset < 5)
+            return null;
+        if (buffer[offset] !== 0x16)
+            throw new Error('TLS ClientHello contains a non-handshake record');
+        const recordLength = buffer.readUInt16BE(offset + 3);
+        if (recordLength === 0 || recordLength > MAX_HANDSHAKE_RECORD_BYTES) {
+            throw new Error('TLS ClientHello record length is invalid');
+        }
+        if (buffer.length - offset - 5 < recordLength)
+            return null;
+        const payload = buffer.subarray(offset + 5, offset + 5 + recordLength);
+        handshakeParts.push(payload);
+        handshakeBytes += payload.length;
+        if (handshakeBytes > MAX_CLIENT_HELLO_BYTES + 4) {
+            throw new Error('TLS ClientHello exceeds the parsing limit');
+        }
+        const joined = Buffer.concat(handshakeParts, handshakeBytes);
+        if (joined.length >= 4 && handshakeLength === undefined) {
+            if (joined[0] !== 0x01)
+                throw new Error('first TLS handshake message is not ClientHello');
+            handshakeLength = joined.readUIntBE(1, 3);
+            if (handshakeLength > MAX_CLIENT_HELLO_BYTES) {
+                throw new Error('TLS ClientHello exceeds the parsing limit');
+            }
+        }
+        if (handshakeLength !== undefined && joined.length >= handshakeLength + 4) {
+            return parseClientHelloBodyAlpn(joined.subarray(4, handshakeLength + 4));
+        }
+        offset += 5 + recordLength;
+    }
+}
+function parseClientHelloBodyAlpn(body) {
+    let offset = 0;
+    const take = (length, label) => {
+        if (!Number.isInteger(length) ||
+            length < 0 ||
+            offset + length > body.length) {
+            throw new Error(`TLS ClientHello ${label} is truncated`);
+        }
+        const value = body.subarray(offset, offset + length);
+        offset += length;
+        return value;
+    };
+    take(2 + 32, 'prefix');
+    const sessionIdLength = take(1, 'session id length')[0];
+    take(sessionIdLength, 'session id');
+    const cipherLength = take(2, 'cipher suite length').readUInt16BE(0);
+    if (cipherLength === 0 || cipherLength % 2 !== 0)
+        throw new Error('TLS ClientHello cipher suite list is invalid');
+    take(cipherLength, 'cipher suites');
+    const compressionLength = take(1, 'compression length')[0];
+    if (compressionLength === 0)
+        throw new Error('TLS ClientHello compression list is invalid');
+    take(compressionLength, 'compression methods');
+    if (offset === body.length)
+        return [];
+    const extensionsLength = take(2, 'extensions length').readUInt16BE(0);
+    if (offset + extensionsLength !== body.length)
+        throw new Error('TLS ClientHello extensions length is invalid');
+    const extensionsEnd = offset + extensionsLength;
+    let alpn;
+    while (offset < extensionsEnd) {
+        const type = take(2, 'extension type').readUInt16BE(0);
+        const length = take(2, 'extension length').readUInt16BE(0);
+        const extension = take(length, 'extension');
+        if (type !== 16)
+            continue;
+        if (alpn !== undefined)
+            throw new Error('TLS ClientHello contains duplicate ALPN extensions');
+        if (extension.length < 2)
+            throw new Error('TLS ClientHello ALPN extension is truncated');
+        const listLength = extension.readUInt16BE(0);
+        if (listLength !== extension.length - 2)
+            throw new Error('TLS ClientHello ALPN list length is invalid');
+        alpn = [];
+        let protocolOffset = 2;
+        while (protocolOffset < extension.length) {
+            const length = extension[protocolOffset++];
+            if (length === 0 || protocolOffset + length > extension.length) {
+                throw new Error('TLS ClientHello ALPN protocol is invalid');
+            }
+            alpn.push(extension.subarray(protocolOffset, protocolOffset + length));
+            protocolOffset += length;
+        }
+    }
+    return (alpn ?? []).map(protocol => protocol.toString('latin1'));
+}
+async function collectClientHello(socket, initial) {
+    let buffered = initial;
+    for (;;) {
+        const parsed = parseClientHelloAlpn(buffered);
+        if (parsed !== null)
+            return { head: buffered, alpnProtocols: parsed };
+        const chunk = await readPausedChunk(socket, CLIENT_HELLO_TIMEOUT_MS);
+        if (chunk === null)
+            throw new Error('client closed before completing TLS ClientHello');
+        if (buffered.length + chunk.length >
+            MAX_CLIENT_HELLO_BYTES + MAX_HANDSHAKE_RECORD_BYTES) {
+            throw new Error('TLS ClientHello exceeds the parsing limit');
+        }
+        buffered = Buffer.concat([buffered, chunk]);
+    }
+}
+function readPausedChunk(socket, timeoutMs) {
+    const ready = socket.read();
+    if (ready !== null)
+        return Promise.resolve(ready);
+    return new Promise((resolve, reject) => {
+        const timer = setTimeout(() => finish(new Error('TLS ClientHello timed out')), timeoutMs);
+        timer.unref?.();
+        const finish = (error, value) => {
+            clearTimeout(timer);
+            socket.removeListener('readable', onReadable);
+            socket.removeListener('end', onEnd);
+            socket.removeListener('close', onEnd);
+            socket.removeListener('error', onError);
+            if (error)
+                reject(error);
+            else
+                resolve(value ?? null);
+        };
+        const onReadable = () => {
+            const chunk = socket.read();
+            if (chunk !== null)
+                finish(undefined, chunk);
+        };
+        const onEnd = () => finish(undefined, null);
+        const onError = error => finish(error);
+        socket.on('readable', onReadable);
+        socket.once('end', onEnd);
+        socket.once('close', onEnd);
+        socket.once('error', onError);
+    });
+}
+async function connectUpstreamTls(target, alpnProtocols, clientSocket) {
+    return await new Promise((resolve, reject) => {
+        const upstream = connectTls({
+            host: target.hostname,
+            port: target.port,
+            ...(isIP(target.hostname) ? {} : { servername: target.hostname }),
+            ...(target.upstreamCA ? { ca: target.upstreamCA } : {}),
+            ...(alpnProtocols.length > 0 ? { ALPNProtocols: alpnProtocols } : {}),
+        });
+        const timer = setTimeout(() => {
+            upstream.destroy(new Error('upstream TLS handshake timed out'));
+        }, UPSTREAM_TLS_TIMEOUT_MS);
+        timer.unref?.();
+        let settled = false;
+        const cleanup = () => {
+            clearTimeout(timer);
+            clientSocket.removeListener('close', onClientClose);
+            upstream.removeListener('secureConnect', onSecureConnect);
+            upstream.removeListener('error', onError);
+            upstream.removeListener('close', onClose);
+        };
+        const fail = error => {
+            if (settled)
+                return;
+            settled = true;
+            cleanup();
+            upstream.destroy();
+            reject(error);
+        };
+        const onClientClose = () => fail(new Error('client closed during upstream TLS handshake'));
+        const onSecureConnect = () => {
+            if (settled)
+                return;
+            settled = true;
+            cleanup();
+            // The connected socket may sit primed until the first request. A
+            // peer reset in that interval must not become an uncaught error;
+            // the HTTP agent/session will still receive its normal close/error.
+            upstream.on('error', () => { });
+            resolve(upstream);
+        };
+        const onError = error => fail(error);
+        const onClose = () => fail(new Error('upstream TLS socket closed during handshake'));
+        clientSocket.once('close', onClientClose);
+        upstream.once('secureConnect', onSecureConnect);
+        upstream.once('error', onError);
+        upstream.once('close', onClose);
     });
 }
 /**
@@ -113,9 +336,170 @@ function relayPullMode(src, dst) {
     src.once('end', () => dst.end());
 }
 /**
- * Terminate the client's TLS on `socket`, parse the decrypted HTTP/1.1
- * stream, and forward each request to `target` over a fresh upstream TLS
- * connection.
+ * One upstream HTTP/2 connection pool per terminated CONNECT tunnel.
+ *
+ * A single client-side h2 session can multiplex many concurrent requests. If
+ * the proxy opened one upstream session per request it would preserve wire
+ * correctness but destroy Cursor/Connect's latency and connection semantics.
+ * Reuse the active session, retire it after GOAWAY, and fail individual streams
+ * closed on connection errors. The pool never crosses CONNECT targets.
+ */
+function createHttp2SessionPool(target, initialSocket) {
+    let active;
+    let primed = initialSocket;
+    const sessions = new Set();
+    const draining = new WeakSet();
+    const authority = `https://${formatAuthority(target.hostname, target.port)}`;
+    const open = () => {
+        const connection = primed && !primed.destroyed ? primed : undefined;
+        if (primed && primed !== connection)
+            primed.destroy();
+        primed = undefined;
+        const session = connectHttp2(authority, {
+            ...(isIP(target.hostname) ? {} : { servername: target.hostname }),
+            ...(target.upstreamCA ? { ca: target.upstreamCA } : {}),
+            settings: { enablePush: false },
+            maxSessionMemory: 16,
+            ...(connection ? { createConnection: () => connection } : {}),
+        });
+        sessions.add(session);
+        const ready = new Promise((resolve, reject) => {
+            let settled = false;
+            const timer = setTimeout(() => {
+                finish(new Error('upstream HTTP/2 SETTINGS timed out'));
+            }, HTTP2_SETTINGS_TIMEOUT_MS);
+            timer.unref?.();
+            const cleanup = () => {
+                clearTimeout(timer);
+                session.removeListener('remoteSettings', onSettings);
+                session.removeListener('error', onError);
+                session.removeListener('close', onClose);
+            };
+            const finish = (error, settings) => {
+                if (settled)
+                    return;
+                settled = true;
+                cleanup();
+                if (error)
+                    reject(error);
+                else
+                    resolve(settings);
+            };
+            const onSettings = settings => finish(undefined, settings);
+            const onError = error => finish(error);
+            const onClose = () => finish(new Error('upstream HTTP/2 session closed before SETTINGS'));
+            session.once('remoteSettings', onSettings);
+            session.once('error', onError);
+            session.once('close', onClose);
+        });
+        // A rejected readiness promise is otherwise unhandled when the client
+        // closes a tunnel before issuing its first request.
+        ready.catch(() => { });
+        session.on('connect', () => {
+            if (session.alpnProtocol !== 'h2') {
+                session.destroy(new Error(`upstream did not negotiate HTTP/2 (ALPN=${String(session.alpnProtocol)})`));
+            }
+        });
+        session.on('goaway', () => {
+            draining.add(session);
+            if (active?.session === session)
+                active = undefined;
+        });
+        // A ClientHttp2Session error is otherwise process-fatal even though
+        // each affected stream also reports its own error to the response path.
+        session.on('error', err => {
+            logForDebugging(`[tls-terminate] upstream HTTP/2 session ${target.hostname}:${target.port} failed: ${err.message}`, { level: 'error' });
+        });
+        session.once('close', () => {
+            sessions.delete(session);
+            if (active?.session === session)
+                active = undefined;
+        });
+        return { session, ready };
+    };
+    const current = () => {
+        if (!active ||
+            active.session.closed ||
+            active.session.destroyed ||
+            draining.has(active.session)) {
+            active = open();
+        }
+        return active;
+    };
+    const canRetry = (record, err) => record.session.closed ||
+        record.session.destroyed ||
+        draining.has(record.session) ||
+        new Set([
+            'ERR_HTTP2_GOAWAY_SESSION',
+            'ERR_HTTP2_INVALID_SESSION',
+            'ERR_HTTP2_SESSION_ERROR',
+        ]).has(err?.code);
+    return {
+        async prime() {
+            const settings = await current().ready;
+            return settings?.enableConnectProtocol === true;
+        },
+        async request(headers, options) {
+            let record = current();
+            try {
+                const settings = await record.ready;
+                if (headers[':protocol'] !== undefined &&
+                    settings?.enableConnectProtocol !== true) {
+                    throw new Error('upstream does not enable extended CONNECT');
+                }
+                return record.session.request(headers, options);
+            }
+            catch (err) {
+                // A GOAWAY/close may race the state check. A request() throw is
+                // known to be pre-wire, so opening one replacement is safe even
+                // for non-idempotent methods.
+                if (!canRetry(record, err))
+                    throw err;
+                draining.add(record.session);
+                active = open();
+                record = active;
+                const settings = await record.ready;
+                if (headers[':protocol'] !== undefined &&
+                    settings?.enableConnectProtocol !== true) {
+                    throw new Error('upstream does not enable extended CONNECT');
+                }
+                return record.session.request(headers, options);
+            }
+        },
+        destroy() {
+            active = undefined;
+            primed?.destroy();
+            primed = undefined;
+            for (const session of sessions)
+                session.destroy();
+            sessions.clear();
+        },
+    };
+}
+function createHttp1Agent(initialSocket) {
+    const agent = new HttpsAgent({ keepAlive: true, maxFreeSockets: 1 });
+    let primed = initialSocket;
+    const createConnection = agent.createConnection.bind(agent);
+    agent.createConnection = (options, callback) => {
+        const connection = primed;
+        primed = undefined;
+        if (connection && !connection.destroyed)
+            return connection;
+        connection?.destroy();
+        return createConnection(options, callback);
+    };
+    return {
+        agent,
+        destroy() {
+            primed?.destroy();
+            primed = undefined;
+            agent.destroy();
+        },
+    };
+}
+/**
+ * Terminate the client's TLS on `socket`, parse decrypted HTTP/2 or HTTP/1.1,
+ * and forward each request to `target` over an upstream TLS connection.
  *
  * Preconditions: the caller has already validated `target` against the
  * domain allowlist; this function does not re-check it.
@@ -128,25 +512,96 @@ function relayPullMode(src, dst) {
  * request handler close over `target` (which carries the originally-
  * requested host:port) without socket-keyed lookups.
  */
-export function terminateAndForward(ca, filterRequest, mutateHeaders, getBodySubstitutions, socket, head, target, planSigv4, maxSigv4BodyBytes = MAX_SIGV4_RESIGN_BODY_BYTES) {
-    // ALPN advertises HTTP/1.1 only — terminating HTTP/2 would require a frame
-    // parser; clients negotiate down. The base secureContext covers clients
-    // that don't send SNI; SNICallback covers everyone else.
-    const baseLeaf = mintLeafCert(ca, target.hostname);
-    const inner = createHttpsServer({
-        ALPNProtocols: ['http/1.1'],
-        cert: baseLeaf.certPem,
-        key: baseLeaf.keyPem,
-        SNICallback: (servername, cb) => {
-            try {
-                cb(null, secureContextFor(ca, servername || target.hostname));
-            }
-            catch (err) {
-                cb(err);
-            }
-        },
-    });
-    inner.on('request', (req, res) => {
+export async function terminateAndForward(ca, filterRequest, mutateHeaders, getBodySubstitutions, socket, head, target, planSigv4, maxSigv4BodyBytes = MAX_SIGV4_RESIGN_BODY_BYTES) {
+    // The compatibility API gives one request handler for HTTP/2 and HTTP/1.1;
+    // forwardHttp2Upstream preserves h2 end-to-end while the existing HTTPS
+    // path handles clients that negotiate down. Cursor's Connect transport
+    // requires native h2 (including bidi streams), while Codex may use a
+    // classic HTTP/1.1 WebSocket upgrade, so both protocols are part of the
+    // provider-credential compatibility boundary.
+    const hello = await collectClientHello(socket, head);
+    const supportedAlpn = [
+        ...new Set(hello.alpnProtocols.filter(protocol => protocol === 'h2' || protocol === 'http/1.1')),
+    ];
+    if (hello.alpnProtocols.length > 0 && supportedAlpn.length === 0) {
+        throw new Error('client offered no supported HTTP application protocol');
+    }
+    const upstreamTls = await connectUpstreamTls(target, supportedAlpn, socket);
+    if (socket.destroyed) {
+        upstreamTls.destroy();
+        return;
+    }
+    const negotiatedAlpn = upstreamTls.alpnProtocol || false;
+    if (negotiatedAlpn !== false &&
+        negotiatedAlpn !== 'h2' &&
+        negotiatedAlpn !== 'http/1.1') {
+        upstreamTls.destroy();
+        throw new Error(`upstream selected unsupported ALPN ${String(negotiatedAlpn)}`);
+    }
+    let baseLeaf;
+    try {
+        baseLeaf = mintLeafCert(ca, target.hostname);
+    }
+    catch (error) {
+        upstreamTls.destroy();
+        throw error;
+    }
+    const upstreamHttp2 = negotiatedAlpn === 'h2' ? createHttp2SessionPool(target, upstreamTls) : null;
+    const upstreamHttp1 = negotiatedAlpn === 'h2' ? null : createHttp1Agent(upstreamTls);
+    let enableConnectProtocol = false;
+    if (upstreamHttp2) {
+        try {
+            // Mirror the upstream capability before the downstream SETTINGS
+            // frame is sent. Advertising extended CONNECT without first
+            // observing the peer's opt-in would violate RFC 8441 and turn a
+            // later reconnect into a protocol-dependent credential oracle.
+            enableConnectProtocol = await upstreamHttp2.prime();
+        }
+        catch (error) {
+            upstreamHttp2.destroy();
+            throw error;
+        }
+    }
+    let inner;
+    try {
+        inner = createHttp2SecureServer({
+            allowHTTP1: true,
+            // http2.createSecureServer overwrites ALPNProtocols with its own
+            // h2+HTTP/1 default unless ALPNCallback is present. Select only the
+            // protocol negotiated upstream with this client's offer; undefined
+            // intentionally mirrors a no-ALPN HTTP/1.1 fallback.
+            ALPNCallback: ({ protocols }) => negotiatedAlpn && protocols.includes(negotiatedAlpn)
+                ? negotiatedAlpn
+                : undefined,
+            cert: baseLeaf.certPem,
+            key: baseLeaf.keyPem,
+            settings: {
+                enablePush: false,
+                ...(enableConnectProtocol ? { enableConnectProtocol: true } : {}),
+                maxConcurrentStreams: 100,
+                maxHeaderListSize: 64 * 1024,
+            },
+            maxSessionMemory: 16,
+            SNICallback: (_servername, cb) => {
+                try {
+                    // The CONNECT target is the policy-authoritative identity. Never
+                    // mint for client-controlled SNI: doing so both lets one tunnel
+                    // obtain trusted leaves for unrelated names and permits an
+                    // unbounded RSA-mint/cache CPU+memory DoS.
+                    cb(null, secureContextFor(ca, target.hostname));
+                }
+                catch (err) {
+                    cb(err);
+                }
+            },
+        });
+    }
+    catch (error) {
+        upstreamHttp2?.destroy();
+        upstreamHttp1?.destroy();
+        throw error;
+    }
+    const handleParsedRequest = (req, res) => {
         // A client abort mid-request destroys req/res with an error; with no
         // listener it escapes as an uncaughtException (the runtime emits it
         // from node:_http_server).
@@ -160,8 +615,14 @@ export function terminateAndForward(ca, filterRequest, mutateHeaders, getBodySub
                 level: 'error',
             });
         });
-        forwardUpstreamGuarded(filterRequest, mutateHeaders, getBodySubstitutions, req, res, target, planSigv4, maxSigv4BodyBytes);
-    });
+        forwardUpstreamGuarded(filterRequest, mutateHeaders, getBodySubstitutions, req, res, target, planSigv4, maxSigv4BodyBytes, upstreamHttp2, upstreamHttp1);
+    };
+    inner.on('request', handleParsedRequest);
+    // Node's HTTP/2 compatibility layer emits CONNECT (including RFC 8441
+    // extended CONNECT) on a dedicated event and otherwise auto-replies 405.
+    // Route it through the same policy path; forwardUpstream recognizes
+    // `:protocol` and switches to opaque bidirectional relay after headers.
+    inner.on('connect', handleParsedRequest);
     inner.on('clientError', (err, sock) => {
         // Post-handshake parse errors / resets on the loopback leg would
         // otherwise escalate to an uncaughtException, same class as the
@@ -173,24 +634,52 @@ export function terminateAndForward(ca, filterRequest, mutateHeaders, getBodySub
         logForDebugging(`[tls-terminate] client TLS error for ${target.hostname}: ${err.message}`, { level: 'error' });
         sock.destroy();
     });
-    inner.on('upgrade', (_req, sock) => {
-        // WebSocket / non-HTTP over TLS — out of scope for now.
-        logForDebugging('[tls-terminate] upgrade request refused', {
-            level: 'warn',
+    inner.on('sessionError', err => {
+        logForDebugging(`[tls-terminate] client HTTP/2 session error for ${target.hostname}: ${err.message}`, { level: 'error' });
+    });
+    inner.on('upgrade', (req, sock, upgradeHead) => {
+        forwardUpgrade(filterRequest, mutateHeaders, req, sock, upgradeHead, target, upstreamHttp1).catch(err => {
+            logForDebugging(`[tls-terminate] upgrade forwarding failed: ${err.message}`, {
+                level: 'error',
+            });
+            sock.destroy();
         });
-        sock.destroy();
     });
-    const sockPath = innerSocketPath();
+    let socketLocation;
+    try {
+        socketLocation = innerSocketPath();
+    }
+    catch (error) {
+        upstreamHttp2?.destroy();
+        upstreamHttp1?.destroy();
+        inner.close();
+        throw error;
+    }
+    const { directory: sockDirectory, socketPath: sockPath } = socketLocation;
+    let cleaned = false;
     const cleanup = () => {
+        if (cleaned)
+            return;
+        cleaned = true;
+        upstreamHttp2?.destroy();
+        upstreamHttp1?.destroy();
         inner.close();
-        unlink(sockPath, () => { });
+        unlink(sockPath, () => rmdir(sockDirectory, () => { }));
     };
     inner.on('error', err => {
         logForDebugging(`[tls-terminate] inner server listen failed: ${err.message}`, { level: 'error' });
         socket.destroy();
         cleanup();
     });
-    inner.listen(sockPath, () => {
+    // Arm before listen(): a client can close while the Unix listener is being
+    // created. Attaching only inside the callback misses that edge and leaks
+    // the preflight TLS socket, HTTP agent, server, and temp directory.
+    socket.once('close', cleanup);
+    const onListening = () => {
+        if (socket.destroyed) {
+            cleanup();
+            return;
+        }
         const loop = connect({ path: sockPath });
         loop.on('error', err => {
             logForDebugging(`[tls-terminate] inner loopback failed: ${err.message}`, {
@@ -202,18 +691,23 @@ export function terminateAndForward(ca, filterRequest, mutateHeaders, getBodySub
         loop.once('connect', () => {
             // The caller wrote `200 Connection Established` before sniffing for the
             // ClientHello; `head` holds whatever the sniff consumed.
-            if (head.length)
-                loop.write(head);
+            if (hello.head.length)
+                loop.write(hello.head);
             relayPullMode(socket, loop);
             loop.pipe(socket);
         });
         socket.on('error', () => loop.destroy());
-        socket.once('close', () => {
-            loop.destroy();
-            cleanup();
-        });
+        socket.once('close', () => loop.destroy());
         loop.once('close', () => socket.destroy());
-    });
+    };
+    try {
+        inner.listen(sockPath, onListening);
+    }
+    catch (error) {
+        socket.destroy();
+        cleanup();
+        throw error;
+    }
     inner.unref();
 }
 /**
@@ -233,10 +727,13 @@ function forwardUpstreamGuarded(...args) {
     // synchronous throw writing a denial to a client that already reset)
     // must not become an unhandledRejection.
     forwardUpstream(...args).catch(err => {
-        logForDebugging(`[tls-terminate] forwardUpstream failed: ${err.message}`, { level: 'error' });
+        logForDebugging(`[tls-terminate] forwardUpstream failed: ${err.message}`, {
+            level: 'error',
+        });
+        sendBadGateway(args[4], err);
     });
 }
-async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitutions, req, res, target, planSigv4, maxSigv4BodyBytes = MAX_SIGV4_RESIGN_BODY_BYTES) {
+async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitutions, req, res, target, planSigv4, maxSigv4BodyBytes = MAX_SIGV4_RESIGN_BODY_BYTES, upstreamHttp2, upstreamHttp1) {
     // req.url is the request-target verbatim. Inside a CONNECT tunnel almost
     // every client sends origin-form (`/path?q`), but RFC 7230 §5.3.2 also
     // permits absolute-form (`https://host/path`) and servers MUST accept it.
@@ -245,6 +742,11 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
     // the CONNECT-verified target stays authoritative (same rationale as the
     // Host-header note below).
     const path = originFormPath(req.url);
+    const extendedProtocol = req.httpVersionMajor === 2 &&
+        req.method === 'CONNECT' &&
+        typeof req.headers[':protocol'] === 'string'
+        ? req.headers[':protocol']
+        : undefined;
     let body = req;
     if (filterRequest) {
         const ac = new AbortController();
@@ -267,7 +769,9 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
         const host = target.port === 443
             ? target.hostname
             : `${target.hostname}:${target.port}`;
-        const out = await decideAndRespond(filterRequest, req, res, `https://${host}${path}`, ac.signal, target.onFilterRequestDeny);
+        const out = await decideAndRespond(filterRequest, req, res, `https://${host}${path}`, ac.signal, target.onFilterRequestDeny, extendedProtocol
+            ? { opaqueBody: true, protocol: extendedProtocol }
+            : undefined);
         if (out === null)
             return;
         body = out;
@@ -289,7 +793,7 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
     // verbatim (including ":port"), which never matches a SAN. Drop the host
     // header and let the runtime derive it from {host, port} — same wire value,
     // correct verification under both Node and Bun.
-    const fwdHeaders = stripHopByHop(req.headers);
+    const fwdHeaders = regularHeaders(req.headers);
     delete fwdHeaders.host;
     // SigV4 planning runs on the PRE-substitution headers (the trigger is
     // the fake access key id in the credential scope, which the header
@@ -302,11 +806,42 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
     // The upstream TLS handshake (rejectUnauthorized defaults to true)
     // completes before any HTTP bytes are written, so mutated headers never
     // reach an unverified server.
-    mutateHeaders?.(fwdHeaders, target.hostname);
+    const beforeMutation = snapshotHeaders(fwdHeaders);
+    mutateHeaders?.(fwdHeaders, target.hostname, target.port);
+    const mutatedHeaderNames = changedHeaderNames(beforeMutation, fwdHeaders);
+    if (extendedProtocol) {
+        if (!upstreamHttp2) {
+            throw new Error('HTTP/2 extended CONNECT received without an upstream HTTP/2 pool');
+        }
+        if (sigv4Plan?.action === 'deny') {
+            respondDenied(res, sigv4Plan.reason);
+            body.destroy();
+            return;
+        }
+        if (sigv4Plan?.action === 'resign') {
+            if (sigv4Plan.payloadHash === undefined) {
+                respondDenied(res, 'AWS SigV4 cannot re-sign an opaque extended CONNECT stream with a literal payload hash');
+                body.destroy();
+                return;
+            }
+            const bracketedHost = isIP(target.hostname) === 6 ? `[${target.hostname}]` : target.hostname;
+            const hostHeader = target.port === 443 ? bracketedHost : `${bracketedHost}:${target.port}`;
+            try {
+                sigv4Plan.apply(fwdHeaders, hostHeader, sigv4Plan.payloadHash);
+            }
+            catch (err) {
+                respondDenied(res, `AWS SigV4 re-signing failed: ${err.message}`);
+                body.destroy();
+                return;
+            }
+        }
+        await forwardHttp2ExtendedConnect(upstreamHttp2, req, res, target, path, extendedProtocol, fwdHeaders, mutatedHeaderNames);
+        return;
+    }
     // Masked-credential substitution in the request body, mirroring the
     // header substitution above. undefined → the bare pipe below, exactly as
     // before. May delete content-length from fwdHeaders (chunked fallback).
-    const bodyTransform = prepareBodySubstitution(getBodySubstitutions, req, fwdHeaders, target.hostname);
+    const bodyTransform = prepareBodySubstitution(getBodySubstitutions, req, fwdHeaders, target.hostname, target.port);
     // SigV4 re-signing runs after substitution so the new signature covers
     // the headers as they actually go upstream (real access key id, real
     // session token). Same denial surface as filterRequest.
@@ -382,13 +917,25 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
     // piped body bytes raw after complete-framed headers — a
     // request-smuggling primitive. The SigV4 buffered path is exempt:
     // end(bufferedBody) computes its own content-length.
-    const clientDeclaredBody = Boolean(req.headers['content-length'] || req.headers['transfer-encoding']);
-    if (clientDeclaredBody &&
+    const clientDeclaredBody = requestDeclaresBody(req);
+    const useHttp2 = req.httpVersionMajor === 2;
+    if (!useHttp2 &&
+        clientDeclaredBody &&
         bufferedBody === undefined &&
         fwdHeaders['content-length'] === undefined &&
         fwdHeaders['transfer-encoding'] === undefined) {
         fwdHeaders['transfer-encoding'] = 'chunked';
     }
+    if (useHttp2) {
+        if (!upstreamHttp2) {
+            throw new Error('HTTP/2 request received without an upstream HTTP/2 pool');
+        }
+        await forwardHttp2Upstream(upstreamHttp2, req, res, target, path, fwdHeaders, mutatedHeaderNames, body, bodyTransform, bufferedBody, clientDeclaredBody);
+        return;
+    }
+    if (!upstreamHttp1) {
+        throw new Error('HTTP/1.1 request received without an upstream HTTP/1.1 connection');
+    }
     // TODO(terminating-tls): honour parentProxy for the upstream leg.
     const upstream = httpsRequest({
         host: target.hostname,
@@ -408,7 +955,7 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
         // pool keyed on the proxy process. Also works around a Bun quirk where
         // the first request's `ca:` value is cached on the global agent and
         // subsequent calls with a different `ca:` are silently ignored.
-        agent: false,
+        agent: upstreamHttp1.agent,
     }, upRes => {
         // The response stream errors independently of the ClientRequest;
         // pipe() does not handle source errors.
@@ -417,7 +964,16 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
             res.destroy();
         });
         res.writeHead(upRes.statusCode ?? 502, stripHopByHop(upRes.headers));
-        upRes.pipe(res);
+        // Keep control of end() so parsed upstream trailers are attached before
+        // Node serializes the terminating chunk. pipe() alone silently drops
+        // trailers, which breaks gRPC/Connect error and metadata semantics.
+        upRes.pipe(res, { end: false });
+        upRes.once('end', () => {
+            const trailers = regularHeaders(upRes.trailers);
+            if (Object.keys(trailers).length > 0)
+                res.addTrailers(trailers);
+            res.end();
+        });
     });
     upstream.on('error', err => {
         logForDebugging(`[tls-terminate] upstream ${target.hostname}:${target.port} failed: ${err.message}`, { level: 'error' });
@@ -433,6 +989,7 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
     if (bufferedBody !== undefined) {
         // SigV4 literal-hash path: the buffered body already went through
         // bodyTransform above (when one applied), so send it verbatim.
+        addHttp1RequestTrailers(req, upstream);
         upstream.end(bufferedBody);
     }
     else if (bodyTransform) {
@@ -441,7 +998,7 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
         bodyTransform.on('error', err => upstream.destroy(err));
         body.on('error', err => bodyTransform.destroy(err));
         res.on('close', () => bodyTransform.destroy());
-        body.pipe(bodyTransform).pipe(upstream);
+        pipeRequestBody(body.pipe(bodyTransform), upstream, () => addHttp1RequestTrailers(req, upstream));
     }
     else {
         // pipe() does not handle source errors. `body` may be a tee branch
@@ -449,8 +1006,418 @@ async function forwardUpstream(filterRequest, mutateHeaders, getBodySubstitution
         // here as an 'error' that would otherwise escape as an
         // uncaughtException.
         body.on('error', err => upstream.destroy(err));
-        body.pipe(upstream);
+        pipeRequestBody(body, upstream, () => addHttp1RequestTrailers(req, upstream));
+    }
+}
+async function forwardHttp2Upstream(pool, req, res, target, path, fwdHeaders, mutatedHeaderNames, body, bodyTransform, bufferedBody, clientDeclaredBody) {
+    const authority = formatAuthority(target.hostname, target.port);
+    const headers = {
+        ':method': req.method ?? 'GET',
+        ':scheme': 'https',
+        ':authority': authority,
+        ':path': path,
+        ...fwdHeaders,
+    };
+    // RFC 9113 permits only `te: trailers` in HTTP/2. stripHopByHop removes TE
+    // generally, but Connect/gRPC peers use this exact value to advertise
+    // trailer support, so restore only the protocol-safe shape.
+    if (String(req.headers.te ?? '')
+        .trim()
+        .toLowerCase() === 'trailers') {
+        headers.te = 'trailers';
+    }
+    const markedSensitive = new Set([
+        ...(Array.isArray(req.headers[sensitiveHeaders])
+            ? req.headers[sensitiveHeaders]
+            : []),
+        ...mutatedHeaderNames,
+    ]);
+    const presentSensitive = [...markedSensitive]
+        .map(name => String(name).toLowerCase())
+        .filter(name => headers[name] !== undefined);
+    if (presentSensitive.length > 0)
+        headers[sensitiveHeaders] = [...new Set(presentSensitive)];
+    let upstream;
+    try {
+        upstream = await pool.request(headers, {
+            waitForTrailers: true,
+            // Node defaults GET/HEAD streams to END_STREAM on HEADERS. Mirror
+            // the client flag explicitly so legal HTTP/2 DATA on those methods
+            // is neither discarded nor written to an already-closed stream.
+            endStream: !clientDeclaredBody,
+        });
+    }
+    catch (err) {
+        sendBadGateway(res, err);
+        body.destroy();
+        return;
+    }
+    let responseStarted = false;
+    let responseFinished = false;
+    upstream.once('response', responseHeaders => {
+        responseStarted = true;
+        const status = Number(responseHeaders[':status'] ?? 502);
+        res.writeHead(status, regularHeaders(responseHeaders));
+        upstream.pipe(res, { end: false });
+    });
+    upstream.on('trailers', trailers => {
+        const forwarded = regularHeaders(trailers);
+        if (Object.keys(forwarded).length > 0 && !res.writableEnded) {
+            try {
+                res.addTrailers(forwarded);
+            }
+            catch (err) {
+                logForDebugging(`[tls-terminate] failed to forward HTTP/2 trailers: ${err.message}`, { level: 'error' });
+                res.destroy();
+            }
+        }
+    });
+    upstream.once('end', () => {
+        responseFinished = true;
+        if (responseStarted && !res.writableEnded)
+            res.end();
+    });
+    upstream.on('error', err => {
+        logForDebugging(`[tls-terminate] upstream HTTP/2 stream ${target.hostname}:${target.port} failed: ${err.message}`, { level: 'error' });
+        sendBadGateway(res, err);
+    });
+    upstream.once('close', () => {
+        if (!responseStarted && !responseFinished)
+            sendBadGateway(res, new Error('upstream HTTP/2 stream closed before response headers'));
+    });
+    upstream.once('wantTrailers', () => {
+        if (upstream.destroyed)
+            return;
+        try {
+            upstream.sendTrailers(regularHeaders(req.trailers));
+        }
+        catch (err) {
+            upstream.destroy(err);
+        }
+    });
+    req.once('aborted', () => {
+        if (!upstream.closed)
+            upstream.close(http2Constants.NGHTTP2_CANCEL);
+    });
+    res.once('close', () => {
+        if (!upstream.closed)
+            upstream.close(http2Constants.NGHTTP2_CANCEL);
+    });
+    if (bufferedBody !== undefined) {
+        upstream.end(bufferedBody);
+    }
+    else if (bodyTransform) {
+        bodyTransform.on('error', err => upstream.destroy(err));
+        body.on('error', err => bodyTransform.destroy(err));
+        res.on('close', () => bodyTransform.destroy());
+        pipeRequestBody(body.pipe(bodyTransform), upstream);
+    }
+    else {
+        body.on('error', err => upstream.destroy(err));
+        pipeRequestBody(body, upstream);
+    }
+}
+async function forwardHttp2ExtendedConnect(pool, req, res, target, path, protocol, fwdHeaders, mutatedHeaderNames) {
+    const headers = {
+        ':method': 'CONNECT',
+        ':protocol': protocol,
+        ':scheme': 'https',
+        ':authority': formatAuthority(target.hostname, target.port),
+        ':path': path,
+        ...fwdHeaders,
+    };
+    const markedSensitive = new Set([
+        ...(Array.isArray(req.headers[sensitiveHeaders])
+            ? req.headers[sensitiveHeaders]
+            : []),
+        ...mutatedHeaderNames,
+    ]);
+    const presentSensitive = [...markedSensitive]
+        .map(name => String(name).toLowerCase())
+        .filter(name => headers[name] !== undefined);
+    if (presentSensitive.length > 0)
+        headers[sensitiveHeaders] = [...new Set(presentSensitive)];
+    let upstream;
+    try {
+        upstream = await pool.request(headers, { endStream: false });
+    }
+    catch (err) {
+        sendBadGateway(res, err);
+        req.destroy();
+        return;
+    }
+    let responseStarted = false;
+    let responseFinished = false;
+    upstream.once('response', responseHeaders => {
+        responseStarted = true;
+        res.writeHead(Number(responseHeaders[':status'] ?? 502), regularHeaders(responseHeaders));
+        upstream.pipe(res, { end: false });
+    });
+    upstream.on('trailers', trailers => {
+        const forwarded = regularHeaders(trailers);
+        if (Object.keys(forwarded).length > 0 && !res.writableEnded)
+            res.addTrailers(forwarded);
+    });
+    upstream.once('end', () => {
+        responseFinished = true;
+        if (responseStarted && !res.writableEnded)
+            res.end();
+    });
+    upstream.on('error', err => {
+        logForDebugging(`[tls-terminate] upstream HTTP/2 extended CONNECT ${target.hostname}:${target.port} failed: ${err.message}`, { level: 'error' });
+        sendBadGateway(res, err);
+    });
+    upstream.once('close', () => {
+        if (!responseStarted && !responseFinished)
+            sendBadGateway(res, new Error('upstream extended CONNECT closed before response headers'));
+    });
+    req.once('aborted', () => {
+        if (!upstream.closed)
+            upstream.close(http2Constants.NGHTTP2_CANCEL);
+    });
+    res.once('close', () => {
+        if (!upstream.closed)
+            upstream.close(http2Constants.NGHTTP2_CANCEL);
+    });
+    req.on('error', err => upstream.destroy(err));
+    pipeRequestBody(req, upstream);
+}
+function sendBadGateway(res, err) {
+    if (res.writableEnded || res.destroyed)
+        return;
+    if (!res.headersSent) {
+        res.writeHead(502, { 'Content-Type': 'text/plain' });
+        res.end('Bad Gateway');
+    }
+    else {
+        res.destroy(err);
+    }
+}
+/** Pipe a request stream while retaining control of destination.end(). */
+function pipeRequestBody(source, destination, beforeEnd) {
+    let ended = false;
+    const finish = () => {
+        if (ended)
+            return;
+        ended = true;
+        beforeEnd?.();
+        destination.end();
+    };
+    if (source.readableEnded) {
+        finish();
+        return;
+    }
+    source.once('end', finish);
+    source.pipe(destination, { end: false });
+}
+function addHttp1RequestTrailers(req, upstream) {
+    const trailers = regularHeaders(req.trailers);
+    if (Object.keys(trailers).length > 0)
+        upstream.addTrailers(trailers);
+}
+/** Strip hop-by-hop and HTTP/2 pseudo-headers from an end-to-end header set. */
+function regularHeaders(input) {
+    const stripped = stripHopByHop(input ?? {});
+    for (const [name, value] of Object.entries(stripped)) {
+        if (name.startsWith(':') || value === undefined)
+            delete stripped[name];
+    }
+    return stripped;
+}
+function snapshotHeaders(headers) {
+    return new Map(Object.entries(headers).map(([name, value]) => [
+        name.toLowerCase(),
+        Array.isArray(value) ? [...value] : value,
+    ]));
+}
+function changedHeaderNames(before, after) {
+    const names = new Set([
+        ...before.keys(),
+        ...Object.keys(after).map(name => name.toLowerCase()),
+    ]);
+    const changed = [];
+    for (const name of names) {
+        const prior = before.get(name);
+        const next = after[name];
+        if (JSON.stringify(prior) !== JSON.stringify(next))
+            changed.push(name);
+    }
+    return changed;
+}
+function formatAuthority(hostname, port) {
+    const host = isIP(hostname) === 6 ? `[${hostname}]` : hostname;
+    return port === 443 ? host : `${host}:${port}`;
+}
+/**
+ * Forward a classic HTTP/1.1 WebSocket upgrade through the terminating proxy.
+ * Credential substitution applies only to the verified handshake headers;
+ * post-101 bytes remain opaque (a sentinel in a frame stays fake).
+ */
+async function forwardUpgrade(filterRequest, mutateHeaders, req, socket, head, target, upstreamHttp1) {
+    socket.pause();
+    const method = req.method ?? 'GET';
+    const path = originFormPath(req.url);
+    const url = `https://${formatAuthority(target.hostname, target.port)}${path}`;
+    const upgrade = String(req.headers.upgrade ?? '').trim();
+    if (method !== 'GET' || upgrade.toLowerCase() !== 'websocket') {
+        denyUpgrade(socket, target, method, url, 'only HTTP/1.1 WebSocket upgrades are supported');
+        return;
+    }
+    if (req.headers['content-length'] !== undefined ||
+        req.headers['transfer-encoding'] !== undefined) {
+        denyUpgrade(socket, target, method, url, 'WebSocket upgrade requests with a body are denied');
+        return;
+    }
+    if (!upstreamHttp1) {
+        denyUpgrade(socket, target, method, url, 'upstream did not negotiate HTTP/1.1');
+        return;
+    }
+    if (filterRequest) {
+        const abort = new AbortController();
+        socket.once('close', () => abort.abort());
+        let decision;
+        try {
+            decision = await filterRequest(new Request(url, {
+                method,
+                headers: policyHeaders(req.headers),
+                signal: abort.signal,
+            }), {
+                hasBody: false,
+                body: null,
+                httpVersion: req.httpVersion,
+            });
+        }
+        catch (err) {
+            decision = {
+                action: 'deny',
+                reason: `sandbox policy check failed: ${err.message}`,
+            };
+        }
+        if (decision.action !== 'allow') {
+            denyUpgrade(socket, target, method, url, decision.reason ?? DEFAULT_DENY_REASON);
+            return;
+        }
+        if (socket.destroyed)
+            return;
+    }
+    const headers = regularHeaders(req.headers);
+    delete headers.host;
+    mutateHeaders?.(headers, target.hostname, target.port);
+    // These are intentionally restored after hop-by-hop stripping: once the
+    // upstream confirms 101 they define the protocol switch itself.
+    headers.connection = 'Upgrade';
+    headers.upgrade = 'websocket';
+    const upstream = httpsRequest({
+        host: target.hostname,
+        port: target.port,
+        path,
+        method,
+        headers,
+        ...(isIP(target.hostname) ? {} : { servername: target.hostname }),
+        ...(target.upstreamCA ? { ca: target.upstreamCA } : {}),
+        agent: upstreamHttp1.agent,
+    });
+    let settled = false;
+    upstream.once('upgrade', (response, upstreamSocket, upstreamHead) => {
+        settled = true;
+        if (socket.destroyed) {
+            upstreamSocket.destroy();
+            return;
+        }
+        const responseHeaders = regularHeaders(response.headers);
+        responseHeaders.connection = 'Upgrade';
+        responseHeaders.upgrade = String(response.headers.upgrade ?? 'websocket');
+        writeHttp1Head(socket, response.statusCode ?? 101, response.statusMessage ?? 'Switching Protocols', responseHeaders);
+        socket.pause();
+        upstreamSocket.pause();
+        if (head.length > 0)
+            upstreamSocket.write(head);
+        if (upstreamHead.length > 0)
+            socket.write(upstreamHead);
+        socket.on('error', () => upstreamSocket.destroy());
+        upstreamSocket.on('error', () => socket.destroy());
+        socket.once('close', () => upstreamSocket.destroy());
+        upstreamSocket.once('close', () => socket.destroy());
+        relayPullMode(socket, upstreamSocket);
+        relayPullMode(upstreamSocket, socket);
+    });
+    upstream.once('response', response => {
+        settled = true;
+        if (socket.destroyed) {
+            response.destroy();
+            return;
+        }
+        const responseHeaders = regularHeaders(response.headers);
+        responseHeaders.connection = 'close';
+        writeHttp1Head(socket, response.statusCode ?? 502, response.statusMessage ?? '', responseHeaders);
+        response.on('error', () => socket.destroy());
+        socket.once('close', () => response.destroy());
+        response.pipe(socket, { end: false });
+        response.once('end', () => socket.end());
+    });
+    upstream.once('error', err => {
+        logForDebugging(`[tls-terminate] upstream WebSocket ${target.hostname}:${target.port} failed: ${err.message}`, { level: 'error' });
+        if (!settled && !socket.destroyed) {
+            writeSocketError(socket, 502, 'Bad Gateway');
+        }
+        else {
+            socket.destroy();
+        }
+    });
+    socket.once('close', () => {
+        if (!settled)
+            upstream.destroy();
+    });
+    upstream.end();
+}
+function policyHeaders(input) {
+    const headers = new Headers();
+    for (const [name, value] of Object.entries(input)) {
+        if (name.startsWith(':') || value === undefined)
+            continue;
+        if (Array.isArray(value)) {
+            for (const entry of value)
+                headers.append(name, entry);
+        }
+        else {
+            headers.append(name, String(value));
+        }
+    }
+    return headers;
+}
+function denyUpgrade(socket, target, method, url, reason) {
+    target.onFilterRequestDeny?.(method, url, reason);
+    logForDebugging(`[request-filter] deny: ${reason}`);
+    writeSocketError(socket, 403, reason, {
+        'Content-Type': 'text/plain',
+        'X-Proxy-Error': 'blocked-by-sandbox-runtime',
+    });
+}
+function writeSocketError(socket, status, reason, headers = {}) {
+    if (socket.destroyed)
+        return;
+    const body = Buffer.from(`${reason}\n`);
+    writeHttp1Head(socket, status, status === 403 ? 'Forbidden' : 'Bad Gateway', {
+        ...headers,
+        'Content-Length': String(body.length),
+        Connection: 'close',
+    });
+    socket.end(body);
+}
+function writeHttp1Head(socket, status, statusMessage, headers) {
+    const cleanMessage = String(statusMessage).replace(/[\r\n]/g, ' ');
+    const lines = [`HTTP/1.1 ${status} ${cleanMessage}`];
+    for (const [name, value] of Object.entries(headers)) {
+        if (!/^[!#$%&'*+.^_`|~0-9A-Za-z-]+$/.test(name) || value === undefined)
+            continue;
+        for (const entry of Array.isArray(value) ? value : [value]) {
+            const text = String(entry);
+            if (/[\r\n]/.test(text))
+                continue;
+            lines.push(`${name}: ${text}`);
+        }
     }
+    socket.write(`${lines.join('\r\n')}\r\n\r\n`);
 }
 /**
  * Read a request body fully into memory (for SigV4 body-hash re-signing).
@@ -462,7 +1429,7 @@ function collectBody(body, maxBytes) {
     return new Promise((resolve, reject) => {
         const chunks = [];
         let total = 0;
-        const onData = (c) => {
+        const onData = c => {
             total += c.length;
             if (total > maxBytes) {
                 chunks.length = 0;
@@ -490,9 +1457,12 @@ function originFormPath(reqUrl) {
         return raw;
     }
 }
-let sockSeq = 0;
 function innerSocketPath() {
-    // Keep it short — macOS sun_path is 104 bytes.
-    return join(tmpdir(), `srt-tt-${process.pid}-${(sockSeq++).toString(36)}.sock`);
+    // A socket directly under a shared temp directory is connectable by other
+    // local users before chmod can run, which would expose this tunnel's
+    // credential-mutating request handler. mkdtemp is atomic and mode 0700;
+    // the one-character socket name also stays below macOS' 104-byte sun_path.
+    const directory = mkdtempSync(join(tmpdir(), 'srt-tt-'));
+    return { directory, socketPath: join(directory, 's') };
 }
 //# sourceMappingURL=tls-terminate-proxy.js.map
